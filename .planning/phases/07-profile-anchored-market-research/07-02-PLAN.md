---
phase: 07-profile-anchored-market-research
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/skills/refinar-perfil/SKILL.md
  - .agents/skills/atualizar-roles-map/SKILL.md
autonomous: true
requirements:
  - PESQ-04

must_haves:
  truths:
    - "O /refinar-perfil Step 2 encontra arquivos -resumo.json tanto em research/{profileId}/ (novo padrao) quanto em research/ (legado flat)"
    - "Arquivos legados exibidos com etiqueta '(legado)' na lista do Step 2"
    - "O path do arquivo selecionado no /refinar-perfil e validado contra researchDir antes de qualquer leitura"
    - "O Step 6.5 do /refinar-perfil resolve vagasFile relativo a subpasta do profileId (nao ao root de research/)"
    - "O SKILL.md do /atualizar-roles-map tem aviso de descontinuado no frontmatter e bloco de migracao no topo do Execution Flow"
    - "A migration note do /atualizar-roles-map aponta para /pesquisar-mercado como substituto"
  artifacts:
    - path: ".agents/skills/refinar-perfil/SKILL.md"
      provides: "File discovery recursivo de dois niveis para encontrar -resumo.json em subpastas"
      contains: "collectResumos"
    - path: ".agents/skills/atualizar-roles-map/SKILL.md"
      provides: "Skill marcada como descontinuada com migration note"
      contains: "DESCONTINUADO"
  key_links:
    - from: "/refinar-perfil Step 2"
      to: "DATA_PATH/research/{profileId}/-resumo.json"
      via: "funcao collectResumos com walk de dois niveis"
      pattern: "collectResumos"
    - from: "/refinar-perfil Step 6.5"
      to: "DATA_PATH/research/{profileId}/{vagasFile}"
      via: "profileId extraido do resumo.json selecionado"
      pattern: "profileId.*vagasFile"
---

<objective>
Dois entregaveis independentes que podem ser implementados em paralelo ao Plan 01:

1. Corrigir o `/refinar-perfil` Step 2 para descobrir arquivos `-resumo.json` na nova estrutura
   de subpastas `research/{profileId}/` sem quebrar a leitura de arquivos legados flat em `research/`.

2. Marcar a skill `/atualizar-roles-map` como descontinuada com migration note apontando para
   `/pesquisar-mercado`.

Purpose: Implementar PESQ-04 e corrigir a integracao critica do /refinar-perfil que quebraria
apos a Phase 7 sem este fix.
Output: refinar-perfil/SKILL.md com discovery recursivo; atualizar-roles-map/SKILL.md com aviso de
descontinuado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-profile-anchored-market-research/07-CONTEXT.md
@.planning/phases/07-profile-anchored-market-research/07-PATTERNS.md
</context>

<interfaces>
<!-- Contratos extraidos dos arquivos fonte -->

Do refinar-perfil/SKILL.md — bloco atual de discovery (Step 2, aprox. linhas 88-118):
```javascript
// PADRAO ATUAL (a ser substituido):
const files = fs.readdirSync(researchDir)
  .filter(f => f.endsWith('-resumo.json'))
  .sort().reverse();
```

Novo padrao de discovery recursivo de dois niveis (Pattern 13 do 07-PATTERNS.md):
```javascript
function collectResumos(dir) {
  const results = [];
  let entries;
  try { entries = fs.readdirSync(dir, { withFileTypes: true }); }
  catch (e) { return results; }
  for (const entry of entries) {
    const fullPath = path.join(dir, entry.name);
    if (entry.isDirectory()) {
      // Subdiretorio — percorrer um nivel (pastas de perfil sao UUID-named, depth 1)
      let subEntries;
      try { subEntries = fs.readdirSync(fullPath, { withFileTypes: true }); }
      catch (e) { continue; }
      for (const subEntry of subEntries) {
        if (subEntry.isFile() && subEntry.name.endsWith('-resumo.json')) {
          results.push({ file: subEntry.name, dir: fullPath, profileId: entry.name });
        }
      }
    } else if (entry.isFile() && entry.name.endsWith('-resumo.json')) {
      // Arquivo legado flat em research/ root
      results.push({ file: entry.name, dir: dir, profileId: null });
    }
  }
  return results.sort((a, b) => b.file.localeCompare(a.file)); // mais recente primeiro
}
```

Formato de display atualizado:
```
Pesquisas de mercado disponiveis:
1. a1b2c3d4 / 2026-04-26-resumo.json — hoje
2. a1b2c3d4 / 2026-04-22-resumo.json — 4 dias atras
3. senior-pd-java-2026-04-18-resumo.json — 8 dias atras (legado)

Carregar pesquisa como contexto? (numero ou Enter para pular)
```

Path de leitura corrigido para Step 6.5 (vagasFile relativo ao profileId):
```javascript
// ANTES (bugado apos Phase 7):
const vagasPath = path.join(process.env.DATA_PATH, 'research', '{vagasFile}');
// DEPOIS (correto):
const vagasPath = path.join(process.env.DATA_PATH, 'research', selectedResumo.profileId, '{vagasFile}');
// Fallback legado (profileId == null):
const vagasPath = path.join(process.env.DATA_PATH, 'research', '{vagasFile}');
```

Padrao de deprecacao (Pattern 11 do 07-PATTERNS.md):
- Frontmatter description: prepend "[DESCONTINUADO — use /pesquisar-mercado]"
- Topo do Execution Flow: bloco de migration note antes do Step 0
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir discovery de -resumo.json no /refinar-perfil para subpastas</name>
  <files>.agents/skills/refinar-perfil/SKILL.md</files>

  <read_first>
    - .agents/skills/refinar-perfil/SKILL.md (ler completo — especialmente Step 2 linhas 86-158 e Step 6.5 linhas 393-451)
    - .planning/phases/07-profile-anchored-market-research/07-CONTEXT.md (secao "Critical integration finding")
    - .planning/phases/07-profile-anchored-market-research/07-PATTERNS.md (Pattern 12 — bloco atual, Pattern 13 — bloco corrigido)
  </read_first>

  <action>
Modificar o arquivo .agents/skills/refinar-perfil/SKILL.md em dois pontos cirurgicos:

--- MODIFICACAO 1: Step 2 — substituir bloco de discovery de -resumo.json ---

Localizar o bloco node -e do Step 2 que contem:
```javascript
const files = fs.readdirSync(researchDir)
  .filter(f => f.endsWith('-resumo.json'))
  .sort().reverse();
```

Substituir TODO o bloco node -e do Step 2 (o bloco que lista pesquisas de mercado disponiveis)
pelo seguinte bloco completo. Manter o texto ao redor (paragrafo "Pergunta sobre pesquisa de
mercado", o texto antes e depois do bloco bash) — apenas o bloco bash node -e e substituido:

```bash
node -e '
const fs = require("fs");
const path = require("path");
const researchDir = path.join(process.env.DATA_PATH || "./data", "research");

function collectResumos(dir) {
  const results = [];
  let entries;
  try { entries = fs.readdirSync(dir, { withFileTypes: true }); }
  catch (e) { return results; }
  for (const entry of entries) {
    const fullPath = path.join(dir, entry.name);
    if (entry.isDirectory()) {
      let subEntries;
      try { subEntries = fs.readdirSync(fullPath, { withFileTypes: true }); }
      catch (e) { continue; }
      for (const subEntry of subEntries) {
        if (subEntry.isFile() && subEntry.name.endsWith("-resumo.json")) {
          results.push({ file: subEntry.name, dir: fullPath, profileId: entry.name });
        }
      }
    } else if (entry.isFile() && entry.name.endsWith("-resumo.json")) {
      results.push({ file: entry.name, dir: dir, profileId: null });
    }
  }
  return results.sort((a, b) => b.file.localeCompare(a.file));
}

const resumos = collectResumos(researchDir);
if (resumos.length === 0) {
  console.log("(Nenhuma pesquisa de mercado disponivel — execute /pesquisar-mercado para criar uma)");
} else {
  console.log("Pesquisas de mercado disponiveis:");
  const today = new Date().toISOString().split("T")[0];
  resumos.forEach((r, i) => {
    const match = r.file.match(/(\d{4}-\d{2}-\d{2})/);
    const fileDate = match ? match[1] : null;
    let ageLabel = fileDate || r.file;
    if (fileDate) {
      const diff = Math.floor((new Date(today) - new Date(fileDate)) / (1000 * 60 * 60 * 24));
      if (diff === 0) ageLabel = "hoje";
      else if (diff === 1) ageLabel = "ontem";
      else ageLabel = diff + " dias atras";
    }
    const prefix = r.profileId ? r.profileId.substring(0, 8) + " / " : "";
    const legadoSuffix = r.profileId ? "" : " (legado)";
    console.log((i + 1) + ". " + prefix + r.file + " — " + ageLabel + legadoSuffix);
  });
}
'
```

Atualizar o texto do exemplo de exibicao que aparece apos o bloco bash:

```
Pesquisas de mercado disponiveis:
1. a1b2c3d4 / 2026-04-26-resumo.json — hoje
2. a1b2c3d4 / 2026-04-22-resumo.json — 4 dias atras
3. senior-pd-java-2026-04-18-resumo.json — 8 dias atras (legado)

Carregar pesquisa como contexto? (numero ou Enter para pular)
```

Substituir o bloco node -e de CARREGAMENTO do arquivo selecionado (o bloco que usa
`path.resolve(researchDir, '{selectedFile}')`) pelo seguinte bloco que usa o path completo
do objeto `resumos[selecao - 1]`:

```bash
node -e '
const fs = require("fs");
const path = require("path");
const researchDir = path.join(process.env.DATA_PATH || "./data", "research");
const selectedDir = "{selectedDir}";   // entry.dir do objeto selecionado
const selectedFile = "{selectedFile}"; // entry.file do objeto selecionado
const filePath = path.resolve(selectedDir, selectedFile);
if (!filePath.startsWith(researchDir)) {
  console.error("Path invalido — abortando");
  process.exit(1);
}
console.log(fs.readFileSync(filePath, "utf8"));
'
```

Instrucao ao executor: `{selectedDir}` e `{selectedFile}` sao os campos `dir` e `file` do
objeto `resumos[numero - 1]` obtido pela funcao `collectResumos`. O `profileId` do objeto
selecionado deve ser registrado em memoria junto com `marketResearch` para uso no Step 6.5.

--- MODIFICACAO 2: Step 6.5 — corrigir path do vagasFile ---

Localizar o bloco bash do Step 6.5 que carrega o `-vagas.json`:
```javascript
const vagasPath = path.join(process.env.DATA_PATH, 'research', '{vagasFile}');
```

Substituir APENAS essa linha de construcao do vagasPath por:
```javascript
// Se a pesquisa selecionada e do novo formato (tem profileId), usar subpasta
const selectedProfileId = "{profileId do resumo selecionado ou null}";
const vagasPath = selectedProfileId
  ? path.join(process.env.DATA_PATH, 'research', selectedProfileId, '{vagasFile}')
  : path.join(process.env.DATA_PATH, 'research', '{vagasFile}'); // fallback legado
```

O `profileId` aqui e o campo `profileId` do objeto selecionado em memoria (registrado no Step 2).
Para resumos no novo formato, o campo `profileId` do JSON tambem esta disponivel — pode ser
obtido de `marketResearch.profileId` apos o carregamento.

--- NOTES FOR AGENT — ADICIONAR ---

Adicionar apos a nota existente sobre "Pesquisa de mercado — carregamento apenas do -resumo.json":

```
- **Discovery de -resumo.json pos-Phase 7:** A funcao `collectResumos` percorre dois niveis:
  (1) arquivos -resumo.json diretamente em `research/` (legado flat — exibidos com "(legado)");
  (2) arquivos -resumo.json em `research/{profileId}/` (novo formato — exibidos com UUID curto como prefixo).
  O path de leitura usa `entry.dir` do objeto retornado, nao concatenacao manual de researchDir + filename.

- **Step 6.5 pos-Phase 7:** O vagasFile de resumos no novo formato esta em `research/{profileId}/`,
  nao em `research/`. Usar `marketResearch.profileId` (ou o `profileId` registrado no Step 2) para
  montar o path correto. Resumos legados (profileId null) continuam no path antigo — fallback preservado.
```

--- METADATA FOOTER — ATUALIZAR ---

Adicionar nova entrada Updated apos as existentes:
```
**Updated:** 2026-04-26 (v3) — Phase 7: Step 2 atualizado com discovery recursivo de dois niveis
(research/{profileId}/ e research/ legado); Step 6.5 com path de vagasFile corrigido para subpastas;
Notes expandidas com guardrails do novo discovery
```
  </action>

  <acceptance_criteria>
    - grep "collectResumos" .agents/skills/refinar-perfil/SKILL.md retorna match
    - grep "withFileTypes" .agents/skills/refinar-perfil/SKILL.md retorna match (walk com isDirectory)
    - grep "legado" .agents/skills/refinar-perfil/SKILL.md retorna match (etiqueta de arquivo legado)
    - grep "selectedProfileId\|profileId.*vagasFile\|selectedDir" .agents/skills/refinar-perfil/SKILL.md retorna match
    - grep "readdirSync(researchDir)" .agents/skills/refinar-perfil/SKILL.md retorna vazio (bloco antigo removido)
    - grep "discovery recursivo" .agents/skills/refinar-perfil/SKILL.md retorna match (nota adicionada)
    - grep "v3" .agents/skills/refinar-perfil/SKILL.md retorna match (metadata footer atualizado)
  </acceptance_criteria>

  <verify>
    <automated>grep -c "collectResumos" .agents/skills/refinar-perfil/SKILL.md && grep -c "withFileTypes" .agents/skills/refinar-perfil/SKILL.md && ! grep -q "\.filter(f => f\.endsWith.*-resumo\.json')" .agents/skills/refinar-perfil/SKILL.md && echo "PASS: discovery recursivo implementado, bloco flat removido"</automated>
  </verify>

  <done>
    Step 2 do /refinar-perfil usa collectResumos para percorrer dois niveis.
    Arquivos legados exibidos com "(legado)". Path de leitura usa entry.dir do objeto.
    Step 6.5 resolve vagasFile relativo ao profileId quando disponivel.
    Notes atualizadas. Metadata footer com v3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Marcar /atualizar-roles-map como descontinuado com migration note</name>
  <files>.agents/skills/atualizar-roles-map/SKILL.md</files>

  <read_first>
    - .agents/skills/atualizar-roles-map/SKILL.md (ler completo — especialmente frontmatter linhas 1-10 e inicio do Execution Flow linhas 28-50)
    - .planning/phases/07-profile-anchored-market-research/07-CONTEXT.md (D-09 — descontinuacao)
    - .planning/phases/07-profile-anchored-market-research/07-PATTERNS.md (Pattern 11 — estrutura da deprecation notice)
  </read_first>

  <action>
Modificar o arquivo .agents/skills/atualizar-roles-map/SKILL.md em dois pontos:

--- MODIFICACAO 1: Frontmatter description ---

Substituir o campo `description` no frontmatter pelo seguinte (manter `name` e `command` inalterados):

```yaml
description: |
  [DESCONTINUADO — use /pesquisar-mercado] Esta skill foi absorvida pelo /pesquisar-mercado
  a partir da Phase 7. O step de pesquisa em guias salariais (Robert Half, Glassdoor BR,
  Catho, Revelo) agora faz parte do fluxo de /pesquisar-mercado, ancorado ao perfil
  selecionado, e gera o campo salaryGuide no -resumo.json. Ver migration note abaixo.
```

--- MODIFICACAO 2: Migration note no topo do Execution Flow ---

Logo apos o titulo "## Execution Flow" e ANTES do "### Step 0: Carregar Variaveis de Ambiente",
inserir o seguinte bloco de aviso:

```markdown
> **SKILL DESCONTINUADA (Phase 7)**
>
> Esta skill foi absorvida por `/pesquisar-mercado` a partir da Phase 7 do milestone v1.1.
> O step de pesquisa em guias salariais (Robert Half, Glassdoor BR, Catho, Revelo)
> agora e um step OBRIGATORIO do `/pesquisar-mercado`, ancorado ao perfil selecionado.
>
> **Migracao:** Execute `/pesquisar-mercado` — o step de guias salariais roda automaticamente
> apos a coleta de vagas. O campo `salaryGuide` no `-resumo.json` por perfil substitui o
> `data/research/roles-map.json` como repositorio de faixas salariais curadas.
>
> **O que muda:**
> - Antes: `/atualizar-roles-map` atualiza `data/research/roles-map.json` (arquivo global)
> - Agora: `/pesquisar-mercado` gera `data/research/{profileId}/{date}-resumo.json` com
>   campo `salaryGuide` (ancorado ao perfil, acumulado por data)
>
> Este arquivo e mantido como referencia historica das tecnicas de scraping de guias salariais.
> Nao executar esta skill para novos levantamentos salariais.
```

--- MODIFICACAO 3: Metadata footer ---

Adicionar nova entrada Updated apos "Last updated: 2026-04-22":
```
**Updated:** 2026-04-26 — Phase 7: skill descontinuada. Tecnicas de scraping absorvidas pelo
/pesquisar-mercado. Ver migration note acima.
**Status:** Deprecated — use /pesquisar-mercado
```

Manter todo o restante do arquivo intacto (Steps 0-6, Notes, Troubleshooting, Related Skills).
  </action>

  <acceptance_criteria>
    - grep "DESCONTINUADO" .agents/skills/atualizar-roles-map/SKILL.md retorna match (minimo 2 ocorrencias — frontmatter e bloco de aviso)
    - grep "SKILL DESCONTINUADA" .agents/skills/atualizar-roles-map/SKILL.md retorna match
    - grep "salaryGuide" .agents/skills/atualizar-roles-map/SKILL.md retorna match (migration note menciona o campo substituto)
    - grep "/pesquisar-mercado" .agents/skills/atualizar-roles-map/SKILL.md retorna pelo menos 3 ocorrencias (frontmatter + migration note + metadata)
    - grep "Deprecated" .agents/skills/atualizar-roles-map/SKILL.md retorna match (metadata footer atualizado)
    - grep "### Step 0: Carregar" .agents/skills/atualizar-roles-map/SKILL.md retorna match (steps originais preservados)
    - O arquivo ainda contem os Steps 1-6 originais intactos (referencia historica preservada)
  </acceptance_criteria>

  <verify>
    <automated>grep -c "DESCONTINUADO" .agents/skills/atualizar-roles-map/SKILL.md && grep -c "/pesquisar-mercado" .agents/skills/atualizar-roles-map/SKILL.md && grep -q "### Step 0" .agents/skills/atualizar-roles-map/SKILL.md && echo "PASS: deprecacao inserida, steps originais preservados"</automated>
  </verify>

  <done>
    Frontmatter description prepend com "[DESCONTINUADO — use /pesquisar-mercado]".
    Migration note inserida no topo do Execution Flow antes do Step 0.
    Metadata footer atualizado com Status: Deprecated.
    Todos os steps originais preservados como referencia historica.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gestor -> numero de selecao (Step 2) | Selecao de resumo por indice — nunca por path digitado |
| indice selecionado -> filesystem | Path construido a partir do objeto da lista interna, validado com startsWith(researchDir) |
| profileId do resumo -> vagasFile path | profileId extraido do JSON do resumo carregado — nunca do gestor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Tampering | Path de leitura do -resumo.json no refinar-perfil | mitigate | filePath construido de entry.dir + entry.file (lista interna), validado com startsWith(researchDir) |
| T-07-08 | Elevation of Privilege | Directory traversal no walk de dois niveis (collectResumos) | mitigate | Walk limitado a exatamente dois niveis (research/ e research/{uuid}/) — sem recursao profunda |
| T-07-09 | Tampering | Path do vagasFile no Step 6.5 | mitigate | vagasPath construido a partir de profileId do JSON carregado (nao do gestor), verificado com path.resolve |
| T-07-10 | Information Disclosure | Conteudo de -vagas.json exibido no Step 6.5 | accept | Vagas sao dados publicos de empregadores; PII de candidatos nunca persistida (guardrail existente) |
</threat_model>

<verification>
Verificacoes apos execucao do plano:

1. `grep "collectResumos" .agents/skills/refinar-perfil/SKILL.md` retorna match
2. `grep "withFileTypes" .agents/skills/refinar-perfil/SKILL.md` retorna match
3. Bloco antigo `fs.readdirSync(researchDir).filter(f => f.endsWith('-resumo.json'))` nao existe mais
4. `grep "legado" .agents/skills/refinar-perfil/SKILL.md` retorna match
5. `grep "DESCONTINUADO" .agents/skills/atualizar-roles-map/SKILL.md` retorna pelo menos 2 ocorrencias
6. `grep "SKILL DESCONTINUADA" .agents/skills/atualizar-roles-map/SKILL.md` retorna match
7. `grep "### Step 0" .agents/skills/atualizar-roles-map/SKILL.md` retorna match (steps preservados)
8. `.claude/skills/refinar-perfil/SKILL.md` e `.cursor/skills/refinar-perfil/SKILL.md` sao o mesmo inode (symlinks)
9. `.claude/skills/atualizar-roles-map/SKILL.md` e `.cursor/skills/atualizar-roles-map/SKILL.md` sao o mesmo inode (symlinks)
</verification>

<success_criteria>
- /refinar-perfil Step 2: collectResumos percorre dois niveis, arquivos legados com "(legado)", path de leitura usa entry.dir
- /refinar-perfil Step 6.5: vagasPath inclui profileId quando disponivel (novo formato)
- /refinar-perfil Notes: duas notas adicionadas sobre discovery pos-Phase 7 e Step 6.5 pos-Phase 7
- /atualizar-roles-map frontmatter: description inicia com "[DESCONTINUADO — use /pesquisar-mercado]"
- /atualizar-roles-map Execution Flow: migration note inserida antes do Step 0 com instrucao de migracao
- /atualizar-roles-map metadata: Status: Deprecated
- Todos os steps originais do /atualizar-roles-map preservados intactos (referencia historica)
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/07-profile-anchored-market-research/07-02-SUMMARY.md`
com resumo do que foi implementado, decisoes tomadas e quaisquer desvios do plano.
</output>
