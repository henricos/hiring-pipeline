---
phase: 04-ai-assisted-profile-refinement
plan: "04"
type: execute
wave: 1
depends_on: ["04-02", "04-03"]
files_modified: []
autonomous: false
requirements: [IA-01, IA-02, IA-03]

must_haves:
  truths:
    - "Base de desenvolvimento zerada — nenhum JSON antigo de perfil/vaga/settings em data/"
    - "3 perfis reais criados via web app usando os XLSXs de data/examples/ como referência"
    - "settings.json recriado com dados reais da área (incluindo aiProfileInstructions preenchido)"
    - "Web app funciona corretamente após zeragem — DynamicListField renderiza perfis existentes"
  artifacts:
    - path: "data/profiles/"
      provides: "3 perfis reais (sem os 2 JSONs antigos de teste)"
      contains: "responsibilities.*\\["
    - path: "data/settings.json"
      provides: "Settings real com aiProfileInstructions"
      contains: "aiProfileInstructions"
  key_links:
    - from: "data/profiles/*.json"
      to: "src/lib/profile.ts"
      via: "ProfileRepository.findById()"
      pattern: "responsibilities.*\\["
---

<objective>
Zeragem da base de desenvolvimento e recriação dos dados a partir dos exemplos reais.

Purpose: Eliminar os JSONs de teste com schema antigo (string nos campos descritivos) para que a base dev reflita exatamente o schema migrado (string[]). Sem essa zeragem, o DynamicListField receberia strings antigas e quebraria.
Output: data/ limpa com 3 perfis reais + settings real. Base dev pronta para validar a skill /refinar-perfil.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-ai-assisted-profile-refinement/04-CONTEXT.md
@.planning/phases/04-ai-assisted-profile-refinement/04-RESEARCH.md
@.planning/phases/04-ai-assisted-profile-refinement/04-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Zeragem da base de desenvolvimento (D-02)</name>
  <read_first>
    data/profiles/
    data/vacancies/
  </read_first>
  <action>
Esta task é executada pelo operador manualmente. O agente NÃO deve tentar executar esses comandos automaticamente — são remoções de dados que requerem confirmação humana.

**INSTRUÇÕES PARA O OPERADOR:**

Antes de prosseguir, confirme que os planos 04-02 e 04-03 foram concluídos com sucesso (npm test && npm run typecheck passando).

Execute os comandos abaixo na ordem:

```bash
# 1. Remover perfis antigos (schema string — incompatíveis com string[])
rm data/profiles/21707edc-ced6-464c-9126-c7bfbc848f27.json
rm data/profiles/6b3bbc18-8a5f-4eb4-a918-8d7e4190173a.json

# 2. Remover vaga órfã (referencia perfil removido)
rm data/vacancies/5adcb093-e148-45c9-9157-a17116f9f0ca.json

# 3. Remover settings antigos (sem campo aiProfileInstructions)
rm data/settings.json

# 4. Confirmar que diretórios estão vazios
ls data/profiles/
ls data/vacancies/
```

Os diretórios devem estar vazios após os comandos. Nenhum dado é perdido permanentemente — o histórico git contém os JSONs antigos.

Depois, acesse a web app:
- URL: http://localhost:3000/hiring-pipeline/settings
- Preencha os campos de configuração da área com dados reais
- Preencha "Instruções para IA montar perfil" com instruções do contexto da área P&D/Lyceum
- Salve e confirme que settings.json foi recriado

Confirme ao agente quando concluído.
  </action>
  <how-to-verify>
    Verifique que os comandos foram executados:
    - `ls data/profiles/` → saída vazia (sem arquivos .json)
    - `ls data/vacancies/` → saída vazia (sem arquivos .json)
    - `ls data/settings.json` → "No such file or directory"

    Em seguida, abra a web app em /settings e confirme que o formulário carrega vazio (sem dados antigos) e que o novo campo "Instruções para IA montar perfil" aparece na interface.
  </how-to-verify>
  <resume-signal>Digite "zerado" quando os arquivos antigos foram removidos e o /settings foi aberto sem erros.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Recriar perfis reais via web app</name>
  <read_first>
    data/examples/
  </read_first>
  <action>
Esta task é executada pelo operador manualmente. Os 3 XLSXs em data/examples/ servem como referência de CONTEÚDO para criar os perfis — eles não são importados automaticamente. O operador deve ler cada XLSX e preencher o formulário web com os dados reais.

**INSTRUÇÕES PARA O OPERADOR:**

1. Certifique-se de que a web app está rodando (npm run dev ou via Docker)
2. Acesse /settings e preencha todos os campos da área com valores reais ANTES de criar perfis
3. Inclua informações no campo "Instruções para IA montar perfil" — exemplo de conteúdo:
   "Área de P&D da Lyceum focada em produtos educacionais para o ensino superior. Priorizar candidatos com experiência em desenvolvimento de software para escala, familiaridade com contexto acadêmico ou EdTech, e perfil comunicativo para atuar com stakeholders não-técnicos."

4. Para cada um dos 3 XLSXs em data/examples/, crie um perfil:
   - Abra o XLSX como referência (via Excel/LibreOffice)
   - Acesse /profiles/new na web app
   - Preencha os campos com os dados do XLSX
   - **IMPORTANTE:** Os campos de responsabilidades, qualificações, competências e desafios agora são LISTAS — insira cada item separadamente usando o botão "+ Adicionar item"
   - Salve o perfil

5. Repita para os 3 XLSXs:
   - data/examples/cientista-dados.xlsx
   - data/examples/dev-frontend-pleno.xlsx
   - data/examples/dev-java-pleno.xlsx

**Validação após criação:**
```bash
ls data/profiles/
# deve listar 3 arquivos .json

# Verificar que os perfis têm arrays nos campos descritivos:
cat data/profiles/*.json | grep -A2 '"responsibilities"'
# deve mostrar arrays: "responsibilities": ["item1", "item2", ...]
```
  </action>
  <how-to-verify>
    - `ls data/profiles/` mostra exatamente 3 arquivos .json (os novos UUIDs)
    - Abrir um dos JSONs: `cat data/profiles/{uuid}.json` — confirmar que `responsibilities` é um array JSON (não string)
    - `cat data/settings.json` existe e contém `"aiProfileInstructions"`
    - A lista em /profiles na web app exibe os 3 novos perfis sem erros
    - Abrir um perfil para editar (/profiles/{id}/edit) e confirmar que o DynamicListField renderiza os itens corretamente como inputs individuais
  </how-to-verify>
  <resume-signal>Digite "perfis criados" quando os 3 perfis reais estiverem visíveis em /profiles e o DynamicListField renderizar os itens corretamente na edição.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operador → data/ | Remoção manual de arquivos de dados. Operação irreversível no filesystem (recuperável via git). |
| Web app → data/profiles/ | Novos JSONs escritos via ProfileRepository com schema string[] correto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W1c-01 | Tampering | Zeragem — remoção de arquivo errado | mitigate | Os UUIDs exatos a remover são listados explicitamente nas instruções (21707edc, 6b3bbc18, 5adcb093). O operador deve confirmar cada remoção. O repositório git preserva o histórico. |
| T-04-W1c-02 | Information Disclosure | data/examples/*.xlsx | accept | Dados de exemplo são documentos internos da empresa já presentes no repositório. Risco já assumido pelo operador ao incluir no repo. |
| T-04-W1c-03 | Tampering | JSON path traversal ao criar perfil | mitigate | ProfileRepository.save() usa ID gerado por crypto.randomUUID() — não deriva de input do usuário. Path traversal impossível via formulário web. |
</threat_model>

<verification>
Após esta wave (04-04 completo):
- `ls data/profiles/ | wc -l` → 3 (exatamente 3 perfis)
- `cat data/profiles/*.json | python3 -c "import sys,json; [print(json.load(open(f))['responsibilities'][:1]) for f in sys.argv[1:]]"` ou equivalente → exibe arrays
- `cat data/settings.json | grep aiProfileInstructions` → retorna match com valor preenchido
- Web app /profiles exibe 3 perfis sem erros de runtime
- Web app /profiles/{id}/edit renderiza DynamicListField com itens separados (não string única)
</verification>

<success_criteria>
Wave 1c completa quando:
- Dados antigos removidos (2 perfis, 1 vaga, settings)
- 3 perfis reais criados com schema string[] correto
- settings.json recriado com aiProfileInstructions preenchido
- DynamicListField renderiza dados existentes corretamente na edição de perfis
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-ai-assisted-profile-refinement/04-04-SUMMARY.md`
</output>
