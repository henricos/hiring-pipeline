---
phase: 04-ai-assisted-profile-refinement
plan: "03"
type: execute
wave: 1
depends_on: ["04-02"]
files_modified:
  - src/lib/settings.ts
  - src/app/actions/settings.ts
  - src/app/actions/profile.ts
  - src/components/settings/settings-form.tsx
  - src/components/profile/profile-form.tsx
autonomous: true
requirements: [IA-03]

must_haves:
  truths:
    - "settings.ts tem campo aiProfileInstructions?: string na interface AreaSettings"
    - "defaultSettings() retorna aiProfileInstructions como string vazia"
    - "SettingsForm exibe textarea 'Instruções para IA montar perfil' com seção própria"
    - "actions/settings.ts persiste aiProfileInstructions via updateSettings"
    - "ProfileForm usa DynamicListField nos 4 campos descritivos em vez de Textarea"
    - "actions/profile.ts usa formData.getAll() nos 4 campos e filtra vazios"
    - "npm test && npm run typecheck passam com zero erros"
  artifacts:
    - path: "src/lib/settings.ts"
      provides: "AreaSettings com aiProfileInstructions"
      contains: "aiProfileInstructions"
    - path: "src/components/settings/settings-form.tsx"
      provides: "Textarea aiProfileInstructions na seção Instruções para IA"
      contains: "aiProfileInstructions"
    - path: "src/components/profile/profile-form.tsx"
      provides: "DynamicListField nos 4 campos descritivos"
      contains: "DynamicListField"
    - path: "src/app/actions/profile.ts"
      provides: "formData.getAll() para os 4 campos"
      contains: "getAll"
  key_links:
    - from: "src/components/settings/settings-form.tsx"
      to: "src/app/actions/settings.ts"
      via: "updateSettings server action"
      pattern: "aiProfileInstructions"
    - from: "src/components/profile/profile-form.tsx"
      to: "src/app/actions/profile.ts"
      via: "DynamicListField hidden inputs → getAll()"
      pattern: "getAll.*responsibilities"
---

<objective>
Completar a Wave 1 integrando o campo aiProfileInstructions em Settings e atualizando as UIs do ProfileForm e SettingsForm.

Purpose: Fechar a cadeia completa da migração — schema migrado (04-02), agora as actions e formulários consomem os novos tipos. O campo aiProfileInstructions é o contexto P&D que a skill /refinar-perfil vai usar.
Output: Settings com novo campo persistido. ProfileForm com DynamicListField nos 4 campos. Actions com getAll() correto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-ai-assisted-profile-refinement/04-CONTEXT.md
@.planning/phases/04-ai-assisted-profile-refinement/04-RESEARCH.md
@.planning/phases/04-ai-assisted-profile-refinement/04-PATTERNS.md
@.planning/phases/04-ai-assisted-profile-refinement/04-02-SUMMARY.md

<interfaces>
<!-- Extraído de src/lib/settings.ts (estado atual) -->
export interface AreaSettings {
  managerName: string;
  godfather: string;
  immediateReport: string;
  mediateReport: string;
  teamComposition: string;
  englishLevel?: LanguageLevel;
  spanishLevel?: LanguageLevel;
  otherLanguage?: string;
  otherLanguageLevel?: LanguageLevel;
  additionalInfo?: string;
  systemsRequired?: string;
  networkFolders?: string;
  costCenter?: string;
  workSchedule?: WorkSchedule;
  workScheduleOther?: string;
  travelRequired?: boolean;
  workMode?: WorkMode;
  // ADICIONAR: aiProfileInstructions?: string   (D-14)
}

<!-- Extraído de src/app/actions/profile.ts (estado atual — a migrar) -->
// ANTES (linhas 36-41):
responsibilities: (formData.get("responsibilities") as string | null)?.trim() ?? "",
qualifications:   (formData.get("qualifications") as string | null)?.trim() ?? "",
behaviors:        (formData.get("behaviors") as string | null)?.trim() ?? "",
challenges:       (formData.get("challenges") as string | null)?.trim() ?? "",

// DEPOIS (D-03):
responsibilities: (formData.getAll("responsibilities") as string[]).map(s => s.trim()).filter(Boolean),
qualifications:   (formData.getAll("qualifications") as string[]).map(s => s.trim()).filter(Boolean),
behaviors:        (formData.getAll("behaviors") as string[]).map(s => s.trim()).filter(Boolean),
challenges:       (formData.getAll("challenges") as string[]).map(s => s.trim()).filter(Boolean),

<!-- DynamicListField interface (criado no 04-02) -->
interface DynamicListFieldProps {
  name: string;
  label: string;
  initialItems?: string[];
  required?: boolean;
  labelClassName?: string;
}
// Import: import { DynamicListField } from "@/components/ui/dynamic-list-field";

<!-- Padrão de seção em settings-form.tsx (linha 29) -->
const SECTION_HEADING_CLASS = "text-[1.125rem] font-medium text-on-surface mt-8 mb-4";
// Padrão de textarea com hint (linhas 147-154):
<div className="space-y-1.5">
  <Label htmlFor="teamComposition" className={LABEL_CLASS}>Composição da equipe</Label>
  <p className="text-[0.6875rem] text-on-surface/50">Estrutura da área...</p>
  <Textarea id="teamComposition" name="teamComposition" defaultValue={initialSettings.teamComposition} ... />
</div>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrar settings.ts + actions/settings.ts — campo aiProfileInstructions</name>
  <files>src/lib/settings.ts, src/app/actions/settings.ts</files>
  <read_first>
    src/lib/settings.ts
    src/app/actions/settings.ts
    src/__tests__/settings.test.ts
  </read_first>
  <behavior>
    - AreaSettings.aiProfileInstructions?: string existe na interface
    - defaultSettings() retorna aiProfileInstructions: "" (string vazia, nunca undefined no default)
    - updateSettings() lê formData.get("aiProfileInstructions") e persiste no objeto settings
    - O campo usa padrão de bloco de comentário "// ── Instruções para IA (Phase 4) ─────"
    - A action inclui aiProfileInstructions no objeto settings salvo
  </behavior>
  <action>
**Passo 1 — src/lib/settings.ts:**
Ler o arquivo. Localizar o final da interface AreaSettings (antes do `}`).
Adicionar após o bloco "── Dados da Vaga fixos por área":

```typescript
  // ── Instruções para IA (Phase 4 — D-14) ──────────────────────
  aiProfileInstructions?: string; // instruções para a skill /refinar-perfil
```

Localizar defaultSettings() e adicionar ao objeto retornado (antes do `}`):
```typescript
    aiProfileInstructions: "",
```

**Passo 2 — src/app/actions/settings.ts:**
Ler o arquivo. Localizar a construção do objeto `settings: AreaSettings` (por volta da linha 58).
Adicionar parsing do novo campo antes do bloco de construção do objeto:

```typescript
    const aiProfileInstructions = (formData.get("aiProfileInstructions") as string) || "";
```

Adicionar o campo no objeto settings (antes do `}`):
```typescript
      aiProfileInstructions,
```

Garantir que o objeto settings enviado para settingsRepository.save() inclui o campo.
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/hiring-pipeline && npm test -- src/__tests__/settings.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "aiProfileInstructions" src/lib/settings.ts retorna ao menos 2 matches (interface + default)
    - grep "aiProfileInstructions.*string" src/lib/settings.ts retorna match na interface
    - grep 'aiProfileInstructions: ""' src/lib/settings.ts retorna match em defaultSettings()
    - grep "aiProfileInstructions" src/app/actions/settings.ts retorna ao menos 2 matches (parsing + objeto)
    - npm test -- settings.test.ts passa com zero falhas (incluindo os novos casos da Wave 0 → GREEN)
    - npm run typecheck retorna zero erros
  </acceptance_criteria>
  <done>AreaSettings tem aiProfileInstructions. defaultSettings() retorna string vazia. updateSettings() persiste o campo. npm test -- settings.test.ts verde.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar SettingsForm — nova seção Instruções para IA</name>
  <files>src/components/settings/settings-form.tsx</files>
  <read_first>
    src/components/settings/settings-form.tsx
    src/lib/settings.ts
  </read_first>
  <action>
Ler src/components/settings/settings-form.tsx inteiro. Identificar:
- A constante SECTION_HEADING_CLASS (deve ser "text-[1.125rem] font-medium text-on-surface mt-8 mb-4")
- A constante LABEL_CLASS e INPUT_CLASS
- O ponto de inserção: ANTES do bloco de Feedback de erro/sucesso (o bloco com state?.error e showSuccess) e APÓS a última seção existente

Adicionar nova seção antes do bloco de feedback final:

```tsx
{/* ── Seção: Instruções para IA ──────────────────────────── */}
<h2 className={SECTION_HEADING_CLASS}>Instruções para IA</h2>
<div className="space-y-4">
  <div className="space-y-1.5">
    <Label htmlFor="aiProfileInstructions" className={LABEL_CLASS}>
      Instruções para IA montar perfil
    </Label>
    <p className="text-[0.6875rem] text-on-surface/50">
      Descreva o contexto da área, produtos, linguagem preferida e o que
      priorizar em candidatos. A IA usará estas instruções ao sugerir perfis.
    </p>
    <Textarea
      id="aiProfileInstructions"
      name="aiProfileInstructions"
      defaultValue={initialSettings.aiProfileInstructions ?? ""}
      placeholder="Ex: Área de P&D focada em produtos educacionais para o ensino superior. Priorizar candidatos com experiência em EdTech ou ambientes acadêmicos..."
      className={`${INPUT_CLASS} min-h-[120px] resize-y`}
    />
  </div>
</div>
```

Garantir que:
- O componente Textarea já está importado no arquivo (verificar imports)
- A prop `initialSettings.aiProfileInstructions` está acessível (initialSettings é AreaSettings)
- O campo usa `name="aiProfileInstructions"` para que updateSettings() capture via formData.get()
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/hiring-pipeline && npm run typecheck 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "aiProfileInstructions" src/components/settings/settings-form.tsx retorna ao menos 3 matches (id, name, defaultValue)
    - grep 'name="aiProfileInstructions"' src/components/settings/settings-form.tsx retorna match exato
    - grep 'Instruções para IA' src/components/settings/settings-form.tsx retorna match no heading
    - grep 'Instruções para IA montar perfil' src/components/settings/settings-form.tsx retorna match no Label
    - npm run typecheck retorna zero erros (AreaSettings agora tem o campo — sem erro de propriedade)
  </acceptance_criteria>
  <done>SettingsForm exibe textarea Instruções para IA montar perfil com seção própria. typecheck zero erros.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Atualizar ProfileForm + action/profile.ts — DynamicListField + getAll()</name>
  <files>src/components/profile/profile-form.tsx, src/app/actions/profile.ts</files>
  <read_first>
    src/components/profile/profile-form.tsx
    src/app/actions/profile.ts
    src/components/ui/dynamic-list-field.tsx
  </read_first>
  <behavior>
    - profile-form.tsx: os 4 campos descritivos (responsibilities, qualifications, behaviors, challenges) usam DynamicListField em vez de Textarea
    - DynamicListField recebe: name={campo}, label={texto}, initialItems={profile?.campo ?? []}, required (somente responsibilities)
    - DynamicListField é importado de "@/components/ui/dynamic-list-field"
    - Textarea permanece importado (ainda usado em internalNotes)
    - actions/profile.ts: os 4 campos usam formData.getAll() + .map(s => s.trim()).filter(Boolean)
  </behavior>
  <action>
**Passo 1 — src/app/actions/profile.ts:**
Ler o arquivo. Localizar a função extractProfileData (linhas 36-41 aprox).

Substituir os 4 campos de string por getAll():
```typescript
responsibilities: (formData.getAll("responsibilities") as string[])
  .map(s => s.trim())
  .filter(Boolean),
qualifications: (formData.getAll("qualifications") as string[])
  .map(s => s.trim())
  .filter(Boolean),
behaviors: (formData.getAll("behaviors") as string[])
  .map(s => s.trim())
  .filter(Boolean),
challenges: (formData.getAll("challenges") as string[])
  .map(s => s.trim())
  .filter(Boolean),
```

**Passo 2 — src/components/profile/profile-form.tsx:**
Ler o arquivo inteiro. Localizar as 4 ocorrências de Textarea para responsibilities, qualifications, behaviors, challenges (linhas ~278-328 aprox).

Adicionar import no bloco de imports existentes:
```typescript
import { DynamicListField } from "@/components/ui/dynamic-list-field";
```

Substituir cada bloco de Textarea pelos 4 campos. Usar o seguinte padrão para cada campo:

Para responsibilities (required):
```tsx
<DynamicListField
  name="responsibilities"
  label="Responsabilidades e atribuições"
  initialItems={profile?.responsibilities ?? []}
  required
  labelClassName={LABEL_CLASS}
/>
```

Para qualifications:
```tsx
<DynamicListField
  name="qualifications"
  label="Requisitos e qualificações"
  initialItems={profile?.qualifications ?? []}
  labelClassName={LABEL_CLASS}
/>
```

Para behaviors:
```tsx
<DynamicListField
  name="behaviors"
  label="Características e competências comportamentais"
  initialItems={profile?.behaviors ?? []}
  labelClassName={LABEL_CLASS}
/>
```

Para challenges:
```tsx
<DynamicListField
  name="challenges"
  label="Principais desafios"
  initialItems={profile?.challenges ?? []}
  labelClassName={LABEL_CLASS}
/>
```

ATENÇÃO: Textarea permanece importado — ainda é usado para internalNotes. Não remover o import.
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/hiring-pipeline && npm test && npm run typecheck 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep "DynamicListField" src/components/profile/profile-form.tsx retorna ao menos 5 matches (import + 4 usos)
    - grep 'name="responsibilities"' src/components/profile/profile-form.tsx retorna match no DynamicListField (não no Textarea removido)
    - grep 'Textarea' src/components/profile/profile-form.tsx retorna match somente para internalNotes (Textarea não foi usado nos 4 campos migrados)
    - grep 'formData.getAll("responsibilities")' src/app/actions/profile.ts retorna match
    - grep 'formData.getAll("qualifications")' src/app/actions/profile.ts retorna match
    - grep 'formData.getAll("behaviors")' src/app/actions/profile.ts retorna match
    - grep 'formData.getAll("challenges")' src/app/actions/profile.ts retorna match
    - grep 'formData.get("responsibilities")' src/app/actions/profile.ts NÃO retorna match (nenhuma linha usando .get() sem All nesses campos)
    - npm test passa com zero falhas (suite completa)
    - npm run typecheck retorna zero erros
  </acceptance_criteria>
  <done>ProfileForm usa DynamicListField nos 4 campos. action usa getAll() com trim+filter. Suite completa verde. typecheck zero erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SettingsForm → updateSettings | aiProfileInstructions trafega como texto livre via FormData |
| DynamicListField (hidden inputs) → extractProfileData | Arrays de strings chegam via formData.getAll() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W1b-01 | Tampering | aiProfileInstructions — injeção de prompt via settings | accept | App single-user atrás de next-auth. O campo é texto que o próprio gestor escreve para instruir a IA. Não há avaliação de código — apenas injeção de texto no prompt da skill CLI. Risco documentado em RESEARCH.md como baixo. |
| T-04-W1b-02 | Tampering | DynamicListField → getAll() — itens duplicados | mitigate | A action usa .filter(Boolean) para filtrar strings vazias. Protege contra hidden inputs com value="" deixados no DOM. Acceptance criteria verifica que .get() não é mais usado. |
| T-04-W1b-03 | Tampering | formData.getAll() — injection via manipulação de campos | accept | Single-user app. Gestor é o único usuário. Dados são do próprio formulário web autenticado. |
</threat_model>

<verification>
Após esta wave (04-03 completo):
- `npm test` → suite completa verde (todos os novos casos da Wave 0 agora GREEN)
- `npm run typecheck` → zero erros
- grep "aiProfileInstructions" src/lib/settings.ts → match
- grep "DynamicListField" src/components/profile/profile-form.tsx → 5+ matches
- grep "getAll" src/app/actions/profile.ts → 4 matches (um por campo)
- A página /settings na web app deve exibir o novo campo "Instruções para IA montar perfil"
- A página /profiles/new deve exibir DynamicListField nos 4 campos (visualmente verificável após zeragem)
</verification>

<success_criteria>
Wave 1b completa quando:
- settings.ts + actions/settings.ts + SettingsForm integrados com aiProfileInstructions
- ProfileForm usa DynamicListField nos 4 campos descritivos
- actions/profile.ts usa getAll() com .filter(Boolean) nos 4 campos
- npm test && npm run typecheck — zero falhas e zero erros de tipo
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-ai-assisted-profile-refinement/04-03-SUMMARY.md`
</output>
