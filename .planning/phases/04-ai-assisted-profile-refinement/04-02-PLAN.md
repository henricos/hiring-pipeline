---
phase: 04-ai-assisted-profile-refinement
plan: "02"
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - src/lib/profile.ts
  - src/lib/excel-generator.ts
  - src/components/ui/dynamic-list-field.tsx
autonomous: true
requirements: [IA-01, IA-02]

must_haves:
  truths:
    - "profile.ts declara responsibilities, qualifications, behaviors, challenges como string[]"
    - "excel-generator.ts exporta serializeStringArray e usa nos 4 campos descritivos"
    - "DynamicListField existe e renderiza inputs com hidden name + Input visível sem name"
    - "npm test && npm run typecheck passam com zero erros"
  artifacts:
    - path: "src/lib/profile.ts"
      provides: "JobProfile com 4 campos descritivos como string[]"
      contains: "responsibilities: string[]"
    - path: "src/lib/excel-generator.ts"
      provides: "Helper serializeStringArray exportado"
      exports: ["serializeStringArray"]
    - path: "src/components/ui/dynamic-list-field.tsx"
      provides: "Componente de lista dinâmica com hidden inputs"
      contains: "DynamicListField"
  key_links:
    - from: "src/lib/excel-generator.ts"
      to: "src/lib/profile.ts"
      via: "import type { JobProfile }"
      pattern: "serializeStringArray.*responsibilities"
    - from: "src/components/ui/dynamic-list-field.tsx"
      to: "src/components/ui/input.tsx"
      via: "import { Input }"
      pattern: "input type=\"hidden\" name=\\{name\\}"
---

<objective>
Executar a migração de schema string→string[] nos 4 campos descritivos do JobProfile e criar o componente DynamicListField.
Esta é a Wave 1a — fundação que todas as outras tasks desta wave dependem.

Purpose: Tornar os testes RED da Wave 0 ficarem GREEN, estabelecendo os tipos corretos que o ProfileForm e o ExcelGenerator vão consumir.
Output: profile.ts migrado, serializeStringArray criada no excel-generator.ts, DynamicListField disponível em src/components/ui/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-ai-assisted-profile-refinement/04-CONTEXT.md
@.planning/phases/04-ai-assisted-profile-refinement/04-RESEARCH.md
@.planning/phases/04-ai-assisted-profile-refinement/04-PATTERNS.md
@.planning/phases/04-ai-assisted-profile-refinement/04-01-SUMMARY.md

<interfaces>
<!-- Tipos atuais que serão migrados — extraídos de src/lib/profile.ts -->

// ANTES (estado atual em profile.ts linhas 44-47):
responsibilities: string; // Responsabilidades e atribuições
qualifications: string;   // Requisitos e qualificações (obrigatórios + diferenciais)
behaviors: string;        // Características e competências comportamentais
challenges: string;       // Principais desafios

// DEPOIS (D-01):
responsibilities: string[]; // Responsabilidades e atribuições — string[] desde Phase 4 (D-01)
qualifications: string[];   // Requisitos e qualificações — string[] desde Phase 4 (D-01)
behaviors: string[];        // Características e competências comportamentais — string[] desde Phase 4 (D-01)
challenges: string[];       // Principais desafios — string[] desde Phase 4 (D-01)

// Componentes shadcn/ui disponíveis (sem instalação adicional):
// src/components/ui/input.tsx   — Input component
// src/components/ui/button.tsx  — Button component
// src/components/ui/label.tsx   — Label component

// Padrão de classes visuais (extraído de profile-form.tsx):
const LABEL_CLASS = "text-[0.6875rem] font-semibold uppercase tracking-[0.05em] text-on-surface/60";
const INPUT_CLASS = "rounded-sm bg-surface-container-low focus-visible:bg-surface-container-lowest focus-visible:border-b-2 focus-visible:border-b-tertiary";

// excel-generator.ts linhas 363-366 — campos a atualizar:
[CELL_MAPPING.responsibilities]: profile.responsibilities ?? "",
[CELL_MAPPING.qualifications]:   profile.qualifications ?? "",
[CELL_MAPPING.behaviors]:        profile.behaviors ?? "",
[CELL_MAPPING.challenges]:       profile.challenges ?? "",
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrar profile.ts — string → string[] nos 4 campos descritivos</name>
  <files>src/lib/profile.ts</files>
  <read_first>
    src/lib/profile.ts
    src/__tests__/profile.test.ts
  </read_first>
  <behavior>
    - profile.responsibilities deve ser string[]
    - profile.qualifications deve ser string[]
    - profile.behaviors deve ser string[]
    - profile.challenges deve ser string[]
    - internalNotes permanece string (campo de texto livre, não lista)
    - Os comentários dos campos devem ser atualizados com "string[] desde Phase 4 (D-01)"
    - Todos os outros campos permanecem inalterados
  </behavior>
  <action>
Ler src/lib/profile.ts. Localizar as linhas 44-47 (bloco "Conteúdo descritivo"):

Substituir:
```typescript
responsibilities: string; // Responsabilidades e atribuições
qualifications: string;   // Requisitos e qualificações (obrigatórios + diferenciais)
behaviors: string;        // Características e competências comportamentais
challenges: string;       // Principais desafios
```

Por:
```typescript
responsibilities: string[]; // Responsabilidades e atribuições — string[] desde Phase 4 (D-01)
qualifications: string[];   // Requisitos e qualificações (obrigatórios + diferenciais) — string[] desde Phase 4 (D-01)
behaviors: string[];        // Características e competências comportamentais — string[] desde Phase 4 (D-01)
challenges: string[];       // Principais desafios — string[] desde Phase 4 (D-01)
```

Não alterar mais nada no arquivo.
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/hiring-pipeline && npm test -- src/__tests__/profile.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "responsibilities: string\[\]" src/lib/profile.ts retorna match
    - grep "qualifications: string\[\]" src/lib/profile.ts retorna match
    - grep "behaviors: string\[\]" src/lib/profile.ts retorna match
    - grep "challenges: string\[\]" src/lib/profile.ts retorna match
    - npm test -- profile.test.ts passa com zero falhas (RED → GREEN)
    - internalNotes?: string permanece inalterada (grep "internalNotes.*string" retorna linha sem [])
  </acceptance_criteria>
  <done>profile.ts com 4 campos descritivos como string[]. npm test -- profile.test.ts passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar serializeStringArray ao excel-generator.ts</name>
  <files>src/lib/excel-generator.ts</files>
  <read_first>
    src/lib/excel-generator.ts
    src/__tests__/excel-generator.test.ts
  </read_first>
  <behavior>
    - serializeStringArray(items: string[]): string é exportada
    - Para array vazio retorna ""
    - Para array com itens, mapeia cada um para "- {item}" e junta com "\n"
    - Filtra strings vazias antes de mapear (.filter(Boolean))
    - Para ["a", "b"] retorna "- a\n- b"
    - Para ["a", "", "b"] retorna "- a\n- b"
    - As 4 linhas de CELL_MAPPING (responsibilities, qualifications, behaviors, challenges) usam serializeStringArray em vez de ?? ""
  </behavior>
  <action>
Ler src/lib/excel-generator.ts. Localizar após a função toExcelDate (linha ~30).

Adicionar helper logo após toExcelDate:

```typescript
/**
 * Serializa string[] para formato de bullets de texto.
 * Formato de saída: "- item1\n- item2\n..." (D-05 — Phase 4)
 * Filtra itens vazios antes de serializar.
 */
export function serializeStringArray(items: string[]): string {
  if (!items || items.length === 0) return "";
  return items
    .filter(Boolean)
    .map(item => `- ${item}`)
    .join("\n");
}
```

Depois localizar as linhas 363-366 (cellValues dos 4 campos descritivos):

Substituir:
```typescript
[CELL_MAPPING.responsibilities]: profile.responsibilities ?? "",
[CELL_MAPPING.qualifications]:   profile.qualifications ?? "",
[CELL_MAPPING.behaviors]:        profile.behaviors ?? "",
[CELL_MAPPING.challenges]:       profile.challenges ?? "",
```

Por:
```typescript
[CELL_MAPPING.responsibilities]: serializeStringArray(profile.responsibilities),
[CELL_MAPPING.qualifications]:   serializeStringArray(profile.qualifications),
[CELL_MAPPING.behaviors]:        serializeStringArray(profile.behaviors),
[CELL_MAPPING.challenges]:       serializeStringArray(profile.challenges),
```

Não alterar mais nada no arquivo.
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/hiring-pipeline && npm test -- src/__tests__/excel-generator.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "export function serializeStringArray" src/lib/excel-generator.ts retorna match
    - grep "serializeStringArray(profile.responsibilities)" src/lib/excel-generator.ts retorna match
    - grep "serializeStringArray(profile.qualifications)" src/lib/excel-generator.ts retorna match
    - grep "serializeStringArray(profile.behaviors)" src/lib/excel-generator.ts retorna match
    - grep "serializeStringArray(profile.challenges)" src/lib/excel-generator.ts retorna match
    - npm test -- excel-generator.test.ts passa com zero falhas (RED → GREEN, incluindo os 4 novos casos)
    - npm run typecheck retorna zero erros
  </acceptance_criteria>
  <done>serializeStringArray exportada e usada nos 4 campos. npm test -- excel-generator.test.ts passa incluindo os novos casos.</done>
</task>

<task type="auto">
  <name>Task 3: Criar DynamicListField — componente de lista dinâmica</name>
  <files>src/components/ui/dynamic-list-field.tsx</files>
  <read_first>
    src/components/ui/input.tsx
    src/components/ui/button.tsx
    src/components/ui/label.tsx
    src/components/profile/profile-form.tsx
  </read_first>
  <action>
Criar src/components/ui/dynamic-list-field.tsx com o seguinte conteúdo exato (derivado do PATTERNS.md e das decisões D-04):

```typescript
"use client";

import { useState } from "react";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import { Label } from "@/components/ui/label";

const LABEL_CLASS =
  "text-[0.6875rem] font-semibold uppercase tracking-[0.05em] text-on-surface/60";
const INPUT_CLASS =
  "rounded-sm bg-surface-container-low focus-visible:bg-surface-container-lowest focus-visible:border-b-2 focus-visible:border-b-tertiary";

interface DynamicListFieldProps {
  name: string;        // nome do campo para FormData (repetido nos hidden inputs)
  label: string;       // texto do Label acima da lista
  initialItems?: string[];   // itens iniciais — array do perfil existente
  required?: boolean;  // se true, o primeiro input recebe required
  labelClassName?: string;   // sobrescreve LABEL_CLASS se fornecido
}

export function DynamicListField({
  name,
  label,
  initialItems,
  required,
  labelClassName,
}: DynamicListFieldProps) {
  // PITFALL CRÍTICO: initializar com [""] se array vazio — garante ao menos 1 input
  const [items, setItems] = useState<string[]>(
    initialItems && initialItems.length > 0 ? initialItems : [""]
  );

  const update = (index: number, value: string) =>
    setItems(prev => prev.map((item, i) => (i === index ? value : item)));

  const add = () => setItems(prev => [...prev, ""]);

  const remove = (index: number) => {
    if (items.length === 1) return; // manter ao menos 1 input
    setItems(prev => prev.filter((_, i) => i !== index));
  };

  return (
    <div className="space-y-1.5">
      <Label className={labelClassName ?? LABEL_CLASS}>{label}</Label>
      <div className="space-y-2">
        {items.map((item, index) => (
          <div key={index} className="flex gap-2">
            {/*
              PITFALL: o <Input> visível NÃO tem name.
              Somente o hidden input tem name — evita duplicação no FormData.
              A server action usa formData.getAll(name) para receber o array.
            */}
            <input type="hidden" name={name} value={item} />
            <Input
              value={item}
              onChange={e => update(index, e.target.value)}
              required={required && index === 0}
              className={`flex-1 ${INPUT_CLASS}`}
            />
            <Button
              type="button"
              variant="outline"
              size="icon"
              onClick={() => remove(index)}
              disabled={items.length === 1}
              className="rounded-sm shrink-0"
              aria-label="Remover item"
            >
              ×
            </Button>
          </div>
        ))}
        <Button
          type="button"
          variant="outline"
          onClick={add}
          className="rounded-sm text-[0.75rem] h-8 px-3"
        >
          + Adicionar item
        </Button>
      </div>
    </div>
  );
}
```

Verificar que o arquivo usa kebab-case no nome (dynamic-list-field.tsx) — correto.
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/hiring-pipeline && npm run typecheck 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo src/components/ui/dynamic-list-field.tsx existe
    - grep '"use client"' src/components/ui/dynamic-list-field.tsx retorna match
    - grep 'export function DynamicListField' src/components/ui/dynamic-list-field.tsx retorna match
    - grep 'type="hidden" name={name}' src/components/ui/dynamic-list-field.tsx retorna match (hidden input com name)
    - grep -v 'name={name}' src/components/ui/dynamic-list-field.tsx | grep '<Input' — o Input visível NÃO tem name=... (apenas value= e onChange=)
    - grep 'formData.getAll' src/components/ui/dynamic-list-field.tsx NÃO retorna match (esse uso fica na action)
    - npm run typecheck retorna zero erros
    - grep 'items.length === 1.*return' src/components/ui/dynamic-list-field.tsx retorna match (proteção 1 item mínimo)
  </acceptance_criteria>
  <done>DynamicListField criado em src/components/ui/dynamic-list-field.tsx. npm run typecheck passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProfileForm → Server Action | Array de strings do DynamicListField trafega via FormData (hidden inputs repetidos) |
| Server Action → excel-generator | profile.responsibilities[] é serializado em serializeStringArray antes de escapeXml |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W1a-01 | Tampering | DynamicListField hidden inputs | accept | App single-user atrás de autenticação next-auth. Inputs são dados do próprio gestor. Risco insignificante. |
| T-04-W1a-02 | Tampering | serializeStringArray → Excel XML | mitigate | serializeStringArray produz texto; escapeXml (já existente em excel-generator.ts) é aplicado sobre o resultado antes de inserir no XML — cadeia de sanitização mantida. Verificar que a chamada a escapeXml envolve serializeStringArray no cellValues. |
| T-04-W1a-03 | Tampering | DynamicListField — itens duplicados via name em Input visível | mitigate | Input visível NÃO tem name — somente o hidden input tem. Acceptance criteria verifica isso via grep. |
</threat_model>

<verification>
Após esta wave (04-02 completo):
- `npm test -- profile.test.ts` → verde (casos string[] passam)
- `npm test -- excel-generator.test.ts` → verde (serializeStringArray cases passam)
- `npm run typecheck` → zero erros
- `ls src/components/ui/dynamic-list-field.tsx` → arquivo existe
- grep "responsibilities: string\[\]" src/lib/profile.ts → retorna match
</verification>

<success_criteria>
Wave 1a completa quando:
- profile.ts tem 4 campos como string[]
- excel-generator.ts exporta serializeStringArray e usa nos 4 campos
- DynamicListField existe com hidden inputs e Input visível sem name
- Todos os testes existentes + novos da Wave 0 passam (GREEN)
- typecheck zero erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-ai-assisted-profile-refinement/04-02-SUMMARY.md`
</output>
