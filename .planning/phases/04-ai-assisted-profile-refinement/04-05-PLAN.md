---
phase: 04-ai-assisted-profile-refinement
plan: "05"
type: execute
wave: 2
depends_on: ["04-04"]
files_modified:
  - .agents/skills/refinar-perfil/SKILL.md
  - .agents/skills/abrir-vaga/SKILL.md
autonomous: true
requirements: [IA-01, IA-02, IA-03]

must_haves:
  truths:
    - "Skill /refinar-perfil existe em .agents/skills/refinar-perfil/SKILL.md"
    - "/refinar-perfil lê DATA_PATH/settings.json antes de gerar sugestões (D-16)"
    - "/refinar-perfil oferece menu de 3 modalidades: sugerir requisitos, melhorar descrição, refinar tudo (D-08)"
    - "/refinar-perfil usa fluxo antes/depois por campo com Aceitar/Rejeitar/Ajustar (D-09, D-10)"
    - "/refinar-perfil grava via node -e (não heredoc) para evitar problemas de escape (D-12)"
    - "/refinar-perfil valida ID do perfil contra lista antes de gravar (segurança — T-04-W2-01)"
    - "/abrir-vaga SKILL.md revisado — campos migrados para AreaSettings removidos do Step 3 (D-18)"
    - "/abrir-vaga Step 4 inclui costCenter, workSchedule, workMode, travelRequired como pré-carregados de settings"
  artifacts:
    - path: ".agents/skills/refinar-perfil/SKILL.md"
      provides: "Skill conversacional de refinamento de perfil via IA"
      contains: "aiProfileInstructions"
    - path: ".agents/skills/abrir-vaga/SKILL.md"
      provides: "Skill revisada contra schemas atuais — sem campos migrados para AreaSettings"
      contains: "costCenter.*settings"
  key_links:
    - from: ".agents/skills/refinar-perfil/SKILL.md"
      to: "DATA_PATH/settings.json"
      via: "cat $DATA_PATH/settings.json → aiProfileInstructions"
      pattern: "aiProfileInstructions"
    - from: ".agents/skills/refinar-perfil/SKILL.md"
      to: "DATA_PATH/profiles/{id}.json"
      via: "node -e fs.writeFileSync"
      pattern: "node -e"
---

<objective>
Criar a skill /refinar-perfil e revisar a skill /abrir-vaga contra os schemas pós-Phase 3.

Purpose: Entregar os 3 requisitos IA (IA-01, IA-02, IA-03) via skill CLI conversacional que contextualiza sugestões de IA com o perfil P&D/Lyceum definido em settings. Corrigir /abrir-vaga para não coletar campos que foram migrados para AreaSettings.
Output: .agents/skills/refinar-perfil/SKILL.md (novo) + .agents/skills/abrir-vaga/SKILL.md (revisado).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-ai-assisted-profile-refinement/04-CONTEXT.md
@.planning/phases/04-ai-assisted-profile-refinement/04-RESEARCH.md
@.planning/phases/04-ai-assisted-profile-refinement/04-PATTERNS.md
@.planning/phases/04-ai-assisted-profile-refinement/04-04-SUMMARY.md

<interfaces>
<!-- Padrão de frontmatter e estrutura de skill — extraído de .agents/skills/abrir-vaga/SKILL.md -->

Frontmatter YAML padrão:
---
name: {nome-da-skill}
description: |
  {descrição em 2-3 linhas}
command: /{nome-da-skill}
---

Seções obrigatórias de uma skill:
## Pre-Conditions
## Execution Flow
  ### Step N: {nome}
## Notes for Agent
## Troubleshooting
## Related Skills
---
**Skill created:** YYYY-MM-DD
**Updated:** —
**Status:** Ready for Claude Code integration

<!-- Campos de JobProfile que /refinar-perfil manipula (pós-migração string[]) -->
// Modalidade 1 — Sugerir requisitos/habilidades (IA-01):
//   Campos: qualifications (string[]), responsibilities (string[])
// Modalidade 2 — Melhorar descrição (IA-02):
//   Campos: behaviors (string[]), challenges (string[])
// Modalidade 3 — Refinar tudo: modalidade 1 + modalidade 2 em sequência

<!-- Campos migrados para AreaSettings que /abrir-vaga deve PARAR de coletar (D-18) -->
// Remover do Step 3 de /abrir-vaga:
//   - costCenter (linha 81 do SKILL.md atual)
//   - workSchedule (linhas 92-97)
//   - travelRequired (linha 107)
//   - workMode (linhas 99-105)
// Adicionar ao Step 4 (pré-carregamento de settings):
//   - costCenter (via settings.costCenter)
//   - workSchedule (via settings.workSchedule)
//   - workMode (via settings.workMode)
//   - travelRequired (via settings.travelRequired)

<!-- Campos do schema Vacancy atual — referência para validar /abrir-vaga Step 5 -->
// vacancy.ts (campos a confirmar no JSON gerado):
//   id, profileId, status, requestType, quantity,
//   salaryRange, confidential, budgeted, headcountIncrease,
//   replacedPerson (string|null), expectedHireDate, openedAt, closedAt
// NÃO incluir no JSON da vaga: costCenter, workSchedule, travelRequired, workMode
//   (esses campos são de AreaSettings, não de Vacancy)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar .agents/skills/refinar-perfil/SKILL.md</name>
  <files>.agents/skills/refinar-perfil/SKILL.md</files>
  <read_first>
    .agents/skills/abrir-vaga/SKILL.md
    src/lib/profile.ts
    src/lib/settings.ts
  </read_first>
  <action>
Criar o diretório .agents/skills/refinar-perfil/ e o arquivo SKILL.md com o seguinte conteúdo completo.

SEGURANÇA CRÍTICA: O Step de gravação deve validar que o ID escolhido pelo gestor existe na lista de perfis antes de gravar. Usar a lista obtida no Step 1 como source of truth — nunca aceitar ID digitado livremente.

```markdown
---
name: refinar-perfil
description: |
  Abre um perfil de vaga existente e oferece sugestões de IA para melhorar
  requisitos, habilidades e descrições. Apresenta antes/depois por campo e
  permite aceitar, rejeitar ou ajustar cada sugestão. Salva o JSON atualizado
  em DATA_PATH/profiles/. Use quando o gestor quiser enriquecer um perfil
  existente com sugestões contextualizadas para P&D/Lyceum.
command: /refinar-perfil
---

# SKILL: Refinar Perfil

Refinamento de perfil de vaga via sugestões de IA contextualizadas. O agente lê
o perfil existente, carrega as instruções de contexto da área e conduz sessão
conversacional de antes/depois por campo.

## Pre-Conditions

- DATA_PATH environment variable set and pointing to the data repository directory
- At least one job profile exists in DATA_PATH/profiles/*.json
- DATA_PATH/settings.json exists (recomendado — sem ele as sugestões não terão contexto P&D)

## Execution Flow

### Step 1: Listar Perfis Disponíveis

```bash
ls $DATA_PATH/profiles/
```

Ler cada arquivo .json e extrair o campo `title`. Exibir lista numerada:

```
Perfis disponíveis:
1. Cientista de Dados (id: abc123...)
2. Desenvolvedor Frontend Pleno (id: def456...)
3. Desenvolvedor Java Pleno (id: ghi789...)

Qual perfil você quer refinar? (número)
```

Aguardar a seleção do gestor. Registrar o ID do perfil selecionado a partir da lista
(NÃO aceitar ID digitado diretamente pelo gestor — usar somente IDs listados pelo `ls`).

### Step 2: Carregar Perfil e Contexto de Área

```bash
cat $DATA_PATH/profiles/{profile-id}.json
cat $DATA_PATH/settings.json
```

Extrair do perfil:
- `title` — título do cargo (usar no prompt da IA como contexto)
- `responsibilities[]` — array de responsabilidades
- `qualifications[]` — array de requisitos e qualificações
- `behaviors[]` — array de competências comportamentais
- `challenges[]` — array de principais desafios

Extrair de settings.json (se existir):
- `aiProfileInstructions` — injetar como contexto do sistema em TODAS as sugestões geradas

Exibir resumo:
```
Perfil carregado: {title}
Responsabilidades: {N} itens
Qualificações: {N} itens
Competências: {N} itens
Desafios: {N} itens

Contexto de área: {aiProfileInstructions ?? "(não configurado — acesse /settings para adicionar)"}
```

### Step 3: Menu de Modalidades

Exibir menu:

```
O que você quer refinar?

1. Sugerir requisitos e habilidades
   (Responsabilidades e Qualificações — IA-01)

2. Melhorar descrições
   (Competências comportamentais e Desafios — IA-02)

3. Refinar tudo
   (Modalidades 1 e 2 em sequência)

Escolha (1, 2 ou 3):
```

Aguardar seleção. Prosseguir conforme escolha:
- `1` → processar: responsibilities, qualifications
- `2` → processar: behaviors, challenges
- `3` → processar todos os 4 campos em sequência (na ordem: responsibilities, qualifications, behaviors, challenges)

### Step 4: Fluxo Antes/Depois por Campo

Para CADA campo da modalidade selecionada, executar o seguinte ciclo:

**4.1 — Exibir ANTES (conteúdo atual):**
```
── {nome do campo} ────────────────────────────────
ANTES (conteúdo atual):
  1. {item1}
  2. {item2}
  ...
```

**4.2 — Gerar sugestão de IA:**

Usar o seguinte contexto no prompt:
- System: "Você é um especialista em recrutamento para {title}. {aiProfileInstructions}"
- Pedido: "Analise e melhore a lista de {nome do campo} para este cargo. Retorne apenas a lista revisada, um item por linha, sem numeração."

**4.3 — Exibir DEPOIS (sugestão):**
```
DEPOIS (sugestão da IA):
  1. {sugestão1}
  2. {sugestão2}
  ...

[A]ceitar / [R]ejeitar / [J]ustar
```

**4.4 — Processar decisão do gestor:**
- `A` (aceitar) → substituir o campo no objeto de perfil em memória pela sugestão
- `R` (rejeitar) → manter o conteúdo original; prosseguir para o próximo campo
- `J` (ajustar) → pedir ao gestor que descreva o ajuste desejado; gerar nova sugestão; repetir 4.3-4.4

Repetir o ciclo até o gestor resolver todos os campos da modalidade.

### Step 5: Confirmar e Gravar

Antes de gravar, exibir resumo das alterações:

```
── Resumo das alterações ─────────────────────────
Campos aceitos:   responsibilities, qualifications
Campos rejeitados: behaviors
Campos sem mudança: challenges

Confirmar gravação? (S/N)
```

Se confirmado, gravar usando node (NÃO heredoc — evita problemas de escape com aspas e newlines):

```bash
node -e "
const fs = require('fs');
const filePath = '$DATA_PATH/profiles/{id}.json';
const profile = JSON.parse(fs.readFileSync(filePath, 'utf8'));
profile.responsibilities = {json_array_responsibilities};
profile.qualifications = {json_array_qualifications};
profile.behaviors = {json_array_behaviors};
profile.challenges = {json_array_challenges};
profile.updatedAt = new Date().toISOString();
fs.writeFileSync(filePath, JSON.stringify(profile, null, 2));
console.log('Perfil atualizado com sucesso.');
"
```

Substituir `{json_array_*}` pelo JSON.stringify() de cada array (apenas os campos alterados).
Campos rejeitados mantêm o valor original — não sobrescrever.

### Step 6: Confirmar Conclusão

```
Perfil atualizado com sucesso!
Arquivo: $DATA_PATH/profiles/{id}.json
Campos alterados: {lista dos campos aceitos}

Próximas ações:
- Abra /profiles/{id}/edit na web app para revisar o resultado
- Execute /abrir-vaga para criar uma vaga com este perfil refinado
```

## Notes for Agent

- **aiProfileInstructions é o contexto principal:** Ler settings.json ANTES de gerar qualquer sugestão. Injetar o campo como contexto do sistema ("Você é um especialista em {aiProfileInstructions}..."). Se settings.json não existir ou o campo estiver vazio, avisar o gestor e prosseguir com contexto genérico.
- **IDs são da lista, nunca do gestor:** Sempre usar o ID obtido do `ls` na Step 1. Nunca aceitar um UUID digitado livremente pelo gestor — isso previne path traversal e sobrescrita de arquivo errado.
- **Campos são string[] desde Phase 4 (D-01):** responsibilities, qualifications, behaviors, challenges são arrays. Ao exibir, mostrar como lista numerada. Ao gravar, usar JSON.stringify(array) no node -e.
- **node -e em vez de heredoc:** O heredoc shell tem problemas com aspas simples e duplas em conteúdo. O node -e lê e grava JSON diretamente, preservando escape correto.
- **Aceitar/Rejeitar/Ajustar:** O gestor tem controle total. A IA sugere — o gestor decide. "Ajustar" permite iterar quantas vezes o gestor quiser antes de aceitar ou rejeitar.
- **Sem backup explícito (D-13):** DATA_PATH é um repositório git. O histórico de versões está disponível via `git log` no diretório de dados.
- **Refinar tudo (Modalidade 3):** Processa os 4 campos na sequência: responsibilities → qualifications → behaviors → challenges. Ao final, exibe resumo consolidado de todos os campos antes de gravar.

## Troubleshooting

**"DATA_PATH não está definido"**
→ Configurar a variável de ambiente:
```bash
export DATA_PATH=/caminho/para/repo-de-dados
```

**"Nenhum perfil encontrado"**
→ Verificar que existe ao menos um .json em $DATA_PATH/profiles/. Se a base foi zerada, criar perfis via web app em /profiles/new.

**"settings.json não encontrado"**
→ Acessar /settings na web app e salvar as configurações da área. O campo "Instruções para IA montar perfil" é especialmente importante para sugestões contextualizadas.

**"Erro ao gravar o perfil"**
→ Verificar permissões em $DATA_PATH/profiles/. Confirmar que o arquivo {id}.json existe e não está corrompido: `cat $DATA_PATH/profiles/{id}.json | python3 -m json.tool`

**"A sugestão da IA não faz sentido para o nosso contexto"**
→ Preencher o campo "Instruções para IA montar perfil" em /settings com mais detalhes sobre a área P&D/Lyceum: produtos, linguagem, perfil de candidato ideal, jargões internos.

## Related Skills

- `/abrir-vaga` — criar vaga conversacionalmente a partir de um perfil refinado
- `/fechar-versao` — referência de estrutura e boas práticas de skill

---

**Skill created:** 2026-04-21
**Updated:** —
**Status:** Ready for Claude Code integration
```
  </action>
  <verify>
    <automated>ls /home/henrico/github/henricos/hiring-pipeline/.agents/skills/refinar-perfil/SKILL.md && grep -c "Step" /home/henrico/github/henricos/hiring-pipeline/.agents/skills/refinar-perfil/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo .agents/skills/refinar-perfil/SKILL.md existe
    - grep "command: /refinar-perfil" .agents/skills/refinar-perfil/SKILL.md retorna match
    - grep "aiProfileInstructions" .agents/skills/refinar-perfil/SKILL.md retorna ao menos 3 matches (leitura, injeção no prompt, nota para agente)
    - grep "node -e" .agents/skills/refinar-perfil/SKILL.md retorna match (gravação via node, não heredoc)
    - grep "NÃO aceitar ID digitado" .agents/skills/refinar-perfil/SKILL.md retorna match (segurança de path traversal)
    - grep "\[A\]ceitar" .agents/skills/refinar-perfil/SKILL.md retorna match (D-10)
    - grep "\[R\]ejeitar" .agents/skills/refinar-perfil/SKILL.md retorna match (D-10)
    - grep "\[J\]ustar" .agents/skills/refinar-perfil/SKILL.md retorna match (D-10)
    - grep "Sugerir requisitos" .agents/skills/refinar-perfil/SKILL.md retorna match (modalidade 1 — IA-01)
    - grep "Melhorar descrições" .agents/skills/refinar-perfil/SKILL.md retorna match (modalidade 2 — IA-02)
    - grep "Refinar tudo" .agents/skills/refinar-perfil/SKILL.md retorna match (modalidade 3)
    - grep "responsibilities\[\]" .agents/skills/refinar-perfil/SKILL.md retorna match (campos como arrays)
    - grep "## Step" .agents/skills/refinar-perfil/SKILL.md retorna ao menos 6 matches (Steps 1-6)
  </acceptance_criteria>
  <done>SKILL.md criado com fluxo completo de 6 steps, menu de 3 modalidades, ciclo antes/depois, gravação via node -e, e segurança de ID validado contra lista.</done>
</task>

<task type="auto">
  <name>Task 2: Revisar .agents/skills/abrir-vaga/SKILL.md contra schemas atuais (D-18)</name>
  <files>.agents/skills/abrir-vaga/SKILL.md</files>
  <read_first>
    .agents/skills/abrir-vaga/SKILL.md
    src/lib/vacancy.ts
    src/lib/settings.ts
  </read_first>
  <action>
Ler .agents/skills/abrir-vaga/SKILL.md inteiro. Ler src/lib/vacancy.ts para confirmar o schema atual.

**Discrepâncias a corrigir (D-18):**

**1. Step 3 — Remover coleta de campos migrados para AreaSettings:**
Localizar a seção "### Step 3: Collect Vacancy-Specific Data". Os seguintes blocos de coleta devem ser REMOVIDOS completamente:

- Bloco "Centro de custo":
  ```
  Centro de custo (ou deixe em branco): ?
  ```
  (costCenter foi para AreaSettings)

- Bloco "Horário e Modalidade":
  ```
  Horário:
    1. Das 08h às 17h
    2. Das 09h às 18h
    3. Outro
  Escolha: ?

  Modalidade:
    1. Presencial
    2. Remoto
    3. Híbrido
  Escolha: ?
  ```
  (workSchedule e workMode foram para AreaSettings)

- Linha de disponibilidade para viagens:
  ```
  Disponibilidade para viagens? (Sim/Não)
  ```
  (travelRequired foi para AreaSettings)

**2. Step 4 — Adicionar pré-carregamento dos campos de AreaSettings:**
Localizar a seção "### Step 4: Pre-Fill with Settings". Adicionar ao bloco de campos pré-carregados:

```
Centro de custo (via settings): [costCenter]
Horário de trabalho (via settings): [workSchedule]
Modalidade (via settings): [workMode]
Viagens necessárias (via settings): [travelRequired]
```

Atualizar o texto de confirmação para incluir esses campos:
```
Estes dados foram preenchidos automaticamente a partir das configurações da área.
Quer alterar algum? (Sim/Não)
```

**3. Step 5 — Corrigir JSON da vaga:**
Localizar o template JSON no Step 5 (cat heredoc). Remover os campos que não pertencem mais ao schema Vacancy:
- Remover linha: `"costCenter": "{costCenter}",`
- Remover linha: `"workSchedule": "{workSchedule}",`
- Remover linha: `"travelRequired": {travelRequired},`
- Remover linha: `"workMode": "{workMode}",`

Verificar contra src/lib/vacancy.ts que o JSON resultante corresponde aos campos corretos de Vacancy.

**4. Notes for Agent — atualizar contexto:**
Adicionar nota explicando a mudança:
```
- **Campos de AreaSettings:** costCenter, workSchedule, workMode, travelRequired vêm de
  settings.json (migrados na Phase 3 — GAP-12). Não coletar do gestor — pré-carregar de settings.
```

Atualizar o campo `**Updated:**` no rodapé para `2026-04-21 (Phase 4 — D-18: auditoria contra schemas pós-GAP-12)`.
  </action>
  <verify>
    <automated>grep -n "costCenter\|workSchedule\|workMode\|travelRequired" /home/henrico/github/henricos/hiring-pipeline/.agents/skills/abrir-vaga/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "costCenter" .agents/skills/abrir-vaga/SKILL.md retorna somente matches no Step 4 (pré-carregamento) e Notes — NENHUM match no Step 3 de coleta interativa
    - grep "workSchedule" .agents/skills/abrir-vaga/SKILL.md retorna somente matches no Step 4 — NENHUM match nos blocos de menu "1. Das 08h às 17h" interativos no Step 3
    - grep "travelRequired" .agents/skills/abrir-vaga/SKILL.md retorna somente matches no Step 4 — NENHUM match como pergunta interativa no Step 3
    - grep "workMode" .agents/skills/abrir-vaga/SKILL.md retorna somente matches no Step 4 — NENHUM match nos blocos de menu de Modalidade no Step 3
    - grep '"costCenter"' .agents/skills/abrir-vaga/SKILL.md NÃO retorna match no template JSON do Step 5 (campo removido da vaga)
    - grep '"workSchedule"' .agents/skills/abrir-vaga/SKILL.md NÃO retorna match no template JSON do Step 5
    - grep "2026-04-21" .agents/skills/abrir-vaga/SKILL.md retorna match no rodapé Updated
    - grep "GAP-12" .agents/skills/abrir-vaga/SKILL.md retorna match nas Notes
  </acceptance_criteria>
  <done>/abrir-vaga SKILL.md revisado — campos AreaSettings removidos do Step 3, adicionados ao Step 4 como pré-carregados, removidos do JSON do Step 5. Updated marcado com data e referência D-18.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validação manual das skills em cenário real</name>
  <action>
As tasks 1 e 2 desta wave criaram/atualizaram os arquivos SKILL.md. Esta task valida que as skills funcionam em cenário real com a base dev zerada e recriada (04-04).

**Validação automática prévia (executar antes de marcar como concluído):**

```bash
# Confirmar que os arquivos existem e têm o conteúdo esperado
ls .agents/skills/refinar-perfil/SKILL.md
ls .agents/skills/abrir-vaga/SKILL.md

# Confirmar que aiProfileInstructions está em settings.json
cat $DATA_PATH/settings.json | grep aiProfileInstructions
```
  </action>
  <how-to-verify>
**Teste 1 — /refinar-perfil (IA-01, IA-02, IA-03):**
1. Em uma sessão nova do Claude Code, execute `/refinar-perfil`
2. Selecione um dos 3 perfis criados na Task 2 do 04-04
3. Confirme que o contexto P&D (aiProfileInstructions) aparece no Step 2
4. Escolha modalidade 1 (Sugerir requisitos e habilidades)
5. Confirme que o fluxo antes/depois aparece para responsibilities e qualifications
6. Aceite ao menos uma sugestão
7. Rejeite ao menos uma sugestão
8. Confirme gravação e verifique que o JSON foi atualizado:
   ```bash
   cat $DATA_PATH/profiles/{id}.json | grep -A5 '"responsibilities"'
   ```
9. Abra o perfil na web app (/profiles/{id}/edit) e confirme que o DynamicListField exibe os itens aceitos

**Teste 2 — /abrir-vaga revisado (D-18):**
1. Em uma sessão nova do Claude Code, execute `/abrir-vaga`
2. Selecione um dos perfis existentes
3. Confirme que o Step 3 NÃO pergunta sobre costCenter, workSchedule, workMode ou travelRequired
4. Confirme que o Step 4 mostra esses valores pré-carregados de settings.json
5. Complete a vaga até o Step 5 (criação do JSON)
6. Verifique o JSON criado:
   ```bash
   cat $DATA_PATH/vacancies/{uuid}.json
   ```
   - Deve NÃO ter costCenter, workSchedule, travelRequired, workMode no JSON
   - Deve ter: id, profileId, status, requestType, quantity, salaryRange, confidential, budgeted, headcountIncrease, expectedHireDate, openedAt, closedAt
  </how-to-verify>
  <resume-signal>Digite "skills validadas" quando ambos os testes passarem — /refinar-perfil gerou sugestão e gravou JSON, /abrir-vaga não coletou campos de AreaSettings.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agente de IA → DATA_PATH/profiles/ | Skill lê e escreve JSON diretamente no filesystem via node -e |
| aiProfileInstructions → prompt da IA | Conteúdo do settings.json é injetado no system prompt do agente de IA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W2-01 | Tampering | /refinar-perfil — path traversal via ID de perfil | mitigate | SKILL.md instrui explicitamente: "Nunca aceitar ID digitado livremente pelo gestor — usar somente IDs listados pelo ls". IDs vêm do `ls $DATA_PATH/profiles/` — bounded set. Note de agent reforça a regra. Acceptance criteria verifica a instrução no arquivo. |
| T-04-W2-02 | Tampering | /refinar-perfil — sobrescrita de arquivo com ID errado | mitigate | Step 5 exibe resumo de campos alterados e pede confirmação (S/N) antes de gravar. O filePath no node -e é derivado do ID selecionado na lista, não de input do gestor. |
| T-04-W2-03 | Tampering | aiProfileInstructions → prompt injection | accept | App single-user. O gestor escreve as próprias instruções. Conteúdo não é avaliado como código — apenas injetado como texto no contexto. Risco documentado como baixo no RESEARCH.md. |
| T-04-W2-04 | Tampering | /abrir-vaga — JSON da vaga com campos extras | mitigate | Revisão D-18 remove costCenter, workSchedule, travelRequired, workMode do template JSON do Step 5. Acceptance criteria verifica ausência via grep. |
| T-04-W2-05 | Repudiation | Alterações de perfil via skill sem auditoria | accept | D-13 define que DATA_PATH é git. O histórico de alterações está disponível via `git log` no repositório de dados. SKILL.md documenta isso em Notes for Agent e Troubleshooting. |
</threat_model>

<verification>
Após esta wave (04-05 completo):
- `ls .agents/skills/refinar-perfil/SKILL.md` → arquivo existe
- `grep "aiProfileInstructions" .agents/skills/refinar-perfil/SKILL.md | wc -l` → ao menos 3 matches
- `grep "node -e" .agents/skills/refinar-perfil/SKILL.md` → match (gravação via node)
- `grep "costCenter" .agents/skills/abrir-vaga/SKILL.md` → match somente no Step 4 e Notes, não no Step 3
- Checkpoint manual: /refinar-perfil executa, gera sugestões, grava JSON
- Checkpoint manual: /abrir-vaga não coleta campos de AreaSettings
</verification>

<success_criteria>
Wave 2 completa quando:
- .agents/skills/refinar-perfil/SKILL.md criado com fluxo completo (Steps 1-6, 3 modalidades, antes/depois, validação de ID)
- .agents/skills/abrir-vaga/SKILL.md revisado (campos AreaSettings removidos do Step 3 e JSON do Step 5, adicionados ao Step 4)
- Checkpoint manual: /refinar-perfil aceita sugestão e grava JSON atualizado corretamente
- Checkpoint manual: /abrir-vaga não questiona costCenter/workSchedule/workMode/travelRequired
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-ai-assisted-profile-refinement/04-05-SUMMARY.md`
</output>
