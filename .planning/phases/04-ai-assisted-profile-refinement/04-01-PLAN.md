---
phase: 04-ai-assisted-profile-refinement
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/__tests__/profile.test.ts
  - src/__tests__/excel-generator.test.ts
  - src/__tests__/settings.test.ts
autonomous: true
requirements: [IA-01, IA-02, IA-03]

must_haves:
  truths:
    - "npm test falha com erros de tipo em profile.test.ts nos 4 campos descritivos (RED intencional)"
    - "npm test falha em excel-generator.test.ts no caso serializeStringArray (RED intencional)"
    - "npm test falha em settings.test.ts no caso aiProfileInstructions (RED intencional — campo ainda não existe)"
  artifacts:
    - path: "src/__tests__/profile.test.ts"
      provides: "Casos string[] para responsibilities, qualifications, behaviors, challenges"
      contains: "string[]"
    - path: "src/__tests__/excel-generator.test.ts"
      provides: "Caso serializeStringArray"
      contains: "serializeStringArray"
    - path: "src/__tests__/settings.test.ts"
      provides: "Caso aiProfileInstructions"
      contains: "aiProfileInstructions"
  key_links:
    - from: "src/__tests__/profile.test.ts"
      to: "src/lib/profile.ts"
      via: "import type { JobProfile }"
      pattern: "responsibilities.*string\\[\\]"
---

<objective>
Atualizar os testes existentes para refletirem os tipos corretos ANTES de alterar o código-fonte.
Esta é a fase RED do ciclo TDD — os testes devem falhar intencionalmente após este plano.

Purpose: Garantir que a migração de schema seja guiada por testes que falham por motivos corretos (tipo errado, símbolo ausente), não por bugs silenciosos.
Output: 3 arquivos de teste atualizados com novos casos que falham — prontos para ficarem GREEN na Wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-ai-assisted-profile-refinement/04-CONTEXT.md
@.planning/phases/04-ai-assisted-profile-refinement/04-RESEARCH.md
@.planning/phases/04-ai-assisted-profile-refinement/04-PATTERNS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Atualizar profile.test.ts — campos descritivos para string[]</name>
  <files>src/__tests__/profile.test.ts</files>
  <read_first>
    src/__tests__/profile.test.ts
    src/lib/profile.ts
  </read_first>
  <behavior>
    - Teste "Interface JobProfile — pode ser instanciada com todos os campos obrigatórios" deve usar string[] nos 4 campos descritivos: responsibilities, qualifications, behaviors, challenges
    - Novo teste "responsibilities é string[]": verifica Array.isArray(profile.responsibilities) === true
    - Novo teste "qualifications é string[]": verifica Array.isArray(profile.qualifications) === true
    - Novo teste "behaviors é string[]": verifica Array.isArray(profile.behaviors) === true
    - Novo teste "challenges é string[]": verifica Array.isArray(profile.challenges) === true
    - O teste de instanciação completa deve usar: responsibilities: ["Desenvolver APIs REST", "Revisar código"], qualifications: ["TypeScript avançado"], behaviors: ["Proatividade"], challenges: ["Escalar sistema"]
    - Qualquer teste que usava string simples nesses campos deve ser atualizado para string[]
  </behavior>
  <action>
Ler o arquivo src/__tests__/profile.test.ts completo. Localizar o bloco "Interface JobProfile" (por volta da linha 153).

Atualizar todos os literais de objeto JobProfile nos testes para usar string[] nos 4 campos:
- responsibilities: ["Desenvolver APIs REST", "Revisar código"]
- qualifications: ["TypeScript avançado", "Node.js (desejável)"]
- behaviors: ["Proatividade", "Trabalho em equipe"]
- challenges: ["Escalar o sistema legado"]

Adicionar bloco de testes após os existentes:

```typescript
describe("Campos descritivos são string[] (D-01 — Phase 4)", () => {
  const profile: JobProfile = {
    id: "test-id",
    title: "Dev Backend",
    suggestedTitle: "Desenvolvedor Back-End",
    experienceLevel: "3-5 anos",
    educationLevel: "Superior completo",
    postGraduateLevel: "Não exigido",
    certifications: "Não",
    responsibilities: ["Desenvolver APIs REST"],
    qualifications: ["TypeScript avançado"],
    behaviors: ["Proatividade"],
    challenges: ["Escalar sistema legado"],
    createdAt: "2026-04-21T00:00:00.000Z",
    updatedAt: "2026-04-21T00:00:00.000Z",
  };

  it("responsibilities é string[]", () => {
    expect(Array.isArray(profile.responsibilities)).toBe(true);
  });
  it("qualifications é string[]", () => {
    expect(Array.isArray(profile.qualifications)).toBe(true);
  });
  it("behaviors é string[]", () => {
    expect(Array.isArray(profile.behaviors)).toBe(true);
  });
  it("challenges é string[]", () => {
    expect(Array.isArray(profile.challenges)).toBe(true);
  });
});
```

Esses testes vão FALHAR porque profile.ts ainda declara `responsibilities: string`. Isso é INTENCIONAL (RED).
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/hiring-pipeline && npm test -- src/__tests__/profile.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>npm test falha em profile.test.ts com erro de tipo TypeScript nos 4 campos (string vs string[]). Os novos testes aparecem no output como falhos.</done>
</task>

<task type="tdd">
  <name>Task 2: Atualizar excel-generator.test.ts — caso serializeStringArray</name>
  <files>src/__tests__/excel-generator.test.ts</files>
  <read_first>
    src/__tests__/excel-generator.test.ts
    src/lib/excel-generator.ts
  </read_first>
  <behavior>
    - Importar serializeStringArray de "@/lib/excel-generator" (ainda não existe — causará falha de importação RED)
    - Caso 1: serializeStringArray(["a", "b"]) retorna "- a\n- b"
    - Caso 2: serializeStringArray(["a", "", "b"]) retorna "- a\n- b" (filtra vazios)
    - Caso 3: serializeStringArray([]) retorna ""
    - Caso 4: serializeStringArray(["item único"]) retorna "- item único"
    - Seguir o padrão describe/it existente do arquivo (ver padrão de escapeXml)
  </behavior>
  <action>
Ler src/__tests__/excel-generator.test.ts. Adicionar import de `serializeStringArray`:

```typescript
import { escapeXml, toExcelDate, serializeStringArray } from "@/lib/excel-generator";
```

Adicionar bloco de testes após os testes de escapeXml e toExcelDate:

```typescript
describe("serializeStringArray", () => {
  it('retorna "- a\\n- b" para ["a", "b"]', () => {
    expect(serializeStringArray(["a", "b"])).toBe("- a\n- b");
  });
  it("filtra itens vazios", () => {
    expect(serializeStringArray(["a", "", "b"])).toBe("- a\n- b");
  });
  it("retorna string vazia para array vazio", () => {
    expect(serializeStringArray([])).toBe("");
  });
  it("funciona com item único", () => {
    expect(serializeStringArray(["item único"])).toBe("- item único");
  });
});
```

Este teste vai FALHAR porque `serializeStringArray` ainda não está exportada em excel-generator.ts. Isso é INTENCIONAL (RED).
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/hiring-pipeline && npm test -- src/__tests__/excel-generator.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>npm test falha em excel-generator.test.ts com erro de importação ou "is not a function" para serializeStringArray. Outros testes existentes permanecem verdes.</done>
</task>

<task type="tdd">
  <name>Task 3: Atualizar settings.test.ts — caso aiProfileInstructions</name>
  <files>src/__tests__/settings.test.ts</files>
  <read_first>
    src/__tests__/settings.test.ts
    src/lib/settings.ts
  </read_first>
  <behavior>
    - Novo teste: AreaSettings aceita campo aiProfileInstructions?: string
    - Novo teste: defaultSettings() retorna objeto com aiProfileInstructions definido como string (pode ser "")
    - Campo é opcional — instância sem aiProfileInstructions deve ser válida
    - Seguir padrão de describe/it existente no arquivo
  </behavior>
  <action>
Ler src/__tests__/settings.test.ts. Adicionar bloco de testes:

```typescript
describe("AreaSettings — campo aiProfileInstructions (D-14 — Phase 4)", () => {
  it("aceita aiProfileInstructions como string opcional", () => {
    const settings: AreaSettings = {
      managerName: "João",
      godfather: "Maria",
      immediateReport: "Carlos",
      mediateReport: "Ana",
      teamComposition: "5 devs",
      aiProfileInstructions: "Priorizar candidatos com experiência em produtos educacionais.",
    };
    expect(typeof settings.aiProfileInstructions).toBe("string");
  });

  it("aiProfileInstructions é opcional — AreaSettings sem o campo é válido", () => {
    const settings: AreaSettings = {
      managerName: "",
      godfather: "",
      immediateReport: "",
      mediateReport: "",
      teamComposition: "",
    };
    // Sem aiProfileInstructions — TypeScript deve aceitar
    expect(settings.aiProfileInstructions).toBeUndefined();
  });

  it("defaultSettings() inclui aiProfileInstructions como string vazia", () => {
    const defaults = defaultSettings();
    expect(typeof defaults.aiProfileInstructions).toBe("string");
  });
});
```

Garantir que `defaultSettings` está importada no arquivo. Se não estiver, adicionar ao import existente.

Este teste vai FALHAR porque `aiProfileInstructions` ainda não existe em AreaSettings. Isso é INTENCIONAL (RED).
  </action>
  <verify>
    <automated>cd /home/henrico/github/henricos/hiring-pipeline && npm test -- src/__tests__/settings.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>npm test falha em settings.test.ts com erro de tipo TypeScript — aiProfileInstructions não existe em AreaSettings. Outros testes existentes permanecem verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test files → source types | Os testes importam tipos de src/lib/ — falhas de importação são esperadas nesta wave |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W0-01 | Tampering | src/__tests__/*.test.ts | accept | Arquivos de teste são parte do repositório git com histórico. Único desenvolvedor no ambiente local. Risco insignificante. |
</threat_model>

<verification>
Após esta wave:
- `npm test -- profile.test.ts` → falha com erro de tipo nos 4 campos descritivos
- `npm test -- excel-generator.test.ts` → falha com serializeStringArray não encontrada
- `npm test -- settings.test.ts` → falha com aiProfileInstructions não existe em AreaSettings
- Todos os outros testes (fora dos 3 arquivos) → continuam verdes
</verification>

<success_criteria>
Wave 0 completa quando:
- Os 3 arquivos de teste foram atualizados
- Os novos casos falham por razões de tipo/importação corretas (não por erros de lógica)
- Os casos existentes nos mesmos arquivos continuam corretos (erros apenas nos novos casos)
- O objetivo RED está documentado no SUMMARY
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-ai-assisted-profile-refinement/04-01-SUMMARY.md`
</output>
