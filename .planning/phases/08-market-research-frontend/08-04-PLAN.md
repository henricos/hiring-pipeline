---
phase: "08"
plan: "04"
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - src/app/\(shell\)/profiles/\[id\]/page.tsx
  - src/components/profile/profile-list.tsx
autonomous: true
requirements: [VIZ-01, VIZ-02, VIZ-03]
user_setup: []

must_haves:
  truths:
    - "Rota /profiles/[id] (detail view) é acessível e renderiza ProfileDetailTabs com profile e researches"
    - "Click no card da lista navega para /profiles/[id] (nova tela detalhe), não /profiles/[id]/edit"
    - "Botões Editar e Deletar no card permanecem apontando para /profiles/[id]/edit (não alterados)"
    - "Page Server Component com await params, notFound() para ID inválido"
    - "Todos os testes de Page e ProfileList passam"
  artifacts:
    - path: "src/app/\(shell\)/profiles/\[id\]/page.tsx"
      provides: "Server Component async, carrega profile + researches, renderiza ProfileDetailTabs"
      min_lines: 30
    - path: "src/components/profile/profile-list.tsx"
      provides: "Mudança cirúrgica: onClick do card navega para /profiles/[id] (não /edit)"
      min_lines: 5 (modificação)
  key_links:
    - from: "page.tsx"
      to: "ProfileDetailTabs"
      via: "render <ProfileDetailTabs profile={profile} researches={researches} />"
      pattern: "ProfileDetailTabs"
    - from: "profile-list.tsx"
      to: "page.tsx"
      via: "router.push(/profiles/{id})"
      pattern: "router.push.*profiles.*\[id\]"
---

<objective>
Criar página de detalhe `/profiles/[id]` com Server Component async, e fazer mudança cirúrgica em profile-list.tsx para navegar para detalhe (não edição). Teste integrativo de fim-a-fim: click no card → detalhe com abas → edição. Todos os testes passam (incluindo page.test.tsx).

Purpose: Completar integração frontend — navegação user-facing funcional, sem quebra de fluxo existente (edição continua funcionando).

Output: 2 arquivos modificados/criados, integração completa, testes passando
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-market-research-frontend/08-CONTEXT.md
@.planning/phases/08-market-research-frontend/08-PATTERNS.md
@.planning/phases/08-market-research-frontend/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 01: Criar página /profiles/[id]/page.tsx (Server Component async)</name>
  <files>src/app/\(shell\)/profiles/\[id\]/page.tsx</files>
  <read_first>
    src/app/\(shell\)/profiles/\[id\]/edit/page.tsx (padrão exato de Server Component async com await params, notFound())
    src/app/actions/profile.ts (getProfile)
    src/app/actions/research.ts (getResearchesByProfileId)
    src/components/profile/profile-detail-tabs.tsx (ProfileDetailTabs interface)
  </read_first>
  <action>
Criar arquivo src/app/(shell)/profiles/[id]/page.tsx com Server Component async:

```typescript
import { notFound } from "next/navigation";
import { getProfile } from "@/app/actions/profile";
import { getResearchesByProfileId } from "@/app/actions/research";
import { ProfileDetailTabs } from "@/components/profile/profile-detail-tabs";

interface ProfileDetailPageProps {
  params: Promise<{ id: string }>;
}

export default async function ProfileDetailPage({
  params,
}: ProfileDetailPageProps) {
  const { id } = await params;

  // Carregar perfil
  const profile = await getProfile(id);
  if (!profile) notFound();

  // Carregar pesquisas vinculadas ao perfil
  const researches = await getResearchesByProfileId(id);

  return (
    <div className="p-8">
      <div className="w-full max-w-4xl">
        <h1 className="text-[1.5rem] font-medium tracking-tight text-on-surface mb-8">
          {profile.title}
        </h1>
        <ProfileDetailTabs profile={profile} researches={researches} />
      </div>
    </div>
  );
}
```

Estrutura:
- Interface ProfileDetailPageProps com params: Promise<{id: string}>
- async function com await params (Next.js 16 pattern)
- getProfile(id) para carregar JobProfile
- notFound() se profile é null (404 response)
- getResearchesByProfileId(id) para listar pesquisas (retorna array, nunca null)
- Renderizar ProfileDetailTabs com profile e researches como props
- Layout idêntico a edit/page.tsx: p-8, max-w-4xl, h1 com title
- Sem modificação de perfil (página puramente leitura + visualização de mercado)

Este é arquivo NOVO (não existe em /profiles/[id]/ antes, apenas /profiles/[id]/edit/page.tsx).
  </action>
  <verify>
    <automated>test -f "src/app/(shell)/profiles/[id]/page.tsx" && npm run build 2>&1 | grep -i error | wc -l</automated>
  </verify>
  <acceptance_criteria>
Arquivo criado em src/app/(shell)/profiles/[id]/page.tsx (caminhoexato). Contém Server Component async export default. await params destructuring. getProfile() + notFound(). getResearchesByProfileId(). ProfileDetailTabs renderizado com profile e researches. Layout com p-8, max-w-4xl, h1 com profile.title. npm run build retorna 0 errors (compila). Testes de page.test.tsx (5 cases) passam (GREEN).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 02: Ajuste cirúrgico em profile-list.tsx — mudar click do card de /edit para /[id]</name>
  <files>src/components/profile/profile-list.tsx</files>
  <read_first>
    src/components/profile/profile-list.tsx (arquivo atual, linhas 70 com onClick)
  </read_first>
  <action>
Fazer mudança cirúrgica em src/components/profile/profile-list.tsx:

**ANTES (linha 70):**
```typescript
onClick={() => router.push(`/profiles/${profile.id}/edit`)}
```

**DEPOIS:**
```typescript
onClick={() => router.push(`/profiles/${profile.id}`)}
```

Remover `/edit` do path — vai para nova rota /profiles/[id] (detail view, não edit).

**IMPORTANTE — Verificar que e.stopPropagation() está intacto nas ações (linhas 88-91):**
```typescript
<div
  className="flex gap-1 shrink-0"
  onClick={(e) => e.stopPropagation()}
>
```

Isso garante que clique nos botões "Editar" e "Deletar" (linhas 92-113) NÃO acionam o onClick do card. Botões devem continuar apontando para `/profiles/${profile.id}/edit` e action de delete — nenhuma mudança neles.

Mudança é PURAMENTE no onClick do card body — linha 70, remover `/edit`.

Arquivo deve permanecer Client Component ("use client"), imports iguais, lógica de delete igual, só a navegação do click no card muda.
  </action>
  <verify>
    <automated>grep -n "router.push.*profiles.*\[id\]" src/components/profile/profile-list.tsx | grep -v edit</automated>
  </verify>
  <acceptance_criteria>
Linha com onClick={() => router.push(\`/profiles/\${profile.id}\`)} existe em profile-list.tsx (sem `/edit`). Linha com onClick={(e) => e.stopPropagation()} na div de ações continua intacta. Botões Editar/Deletar continuam apontando para `/edit` e delete action. npm run test passa (tests de profile-list.test.tsx, se existem). Nenhuma outra linha modificada.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 03: Validação integrativa — fluxo completo click → detalhe → edição</name>
  <files>—</files>
  <read_first>
    src/app/\(shell\)/profiles/\[id\]/page.tsx (criado)
    src/components/profile/profile-list.tsx (modificado)
    src/components/profile/profile-detail-tabs.tsx (criado em Wave 2)
  </read_first>
  <action>
Executar validação integrativa do fluxo:

1. **Navegação da lista para detalhe:**
   - npm run dev (inicia aplicação)
   - Abrir /profiles
   - Click no corpo do card (não no botão Editar)
   - Esperado: página navega para /profiles/[id] (URL muda, renderiza detalhe com abas)

2. **Abas funcionais:**
   - Tela detalhe carrega com aba Perfil visível
   - Click em "Vagas" → renderiza lista de pesquisas (se existem)
   - Click em "Resumo de Mercado" → renderiza stackFrequency, salaryGuide, etc. (se existem)

3. **Botão Editar no detalhe:**
   - Na aba Perfil, clicar botão "Editar" (header direito)
   - Esperado: navega para /profiles/[id]/edit (página de edição)

4. **Botão Editar no card:**
   - Voltar para /profiles
   - Click no botão Editar (pencil icon, canto direito do card)
   - Esperado: navega para /profiles/[id]/edit (página de edição)

5. **Botão Deletar (sem mudança):**
   - Click no botão Deletar (trash icon)
   - Esperado: dialog de confirmação aparece
   - Confirmar delete
   - Esperado: perfil removido da lista, volta para /profiles

Esta task é checkpoint MANUAL — não automatizável (requer browser interaction).

Se tudo passar acima, fluxo está completo e correto. Nenhuma quebra em UI existente.
  </action>
  <verify>
    <automated>npm run test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
Testes automatizados de page.test.tsx (5 cases) + profile-list.test.tsx (se existem) passam (GREEN). Fluxo integrativo manual (5 passos acima) funciona sem erros. Navegação entre detalhe e edição funciona em ambas as direções. Botões Deletar e Editar no card funcionam normalmente. URL muda corretamente em cada navegação.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profileId parameter | Entra em getProfile() e getResearchesByProfileId(); ambas já validam |
| Router navigation | react-router-navigation para /profiles/[id]; URL já segura (UUIDs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | URL parameter [id] | mitigate | getProfile(id) valida no ProfileRepository; getResearchesByProfileId(id) valida em ResearchRepository |
| T-08-07 | Information Disclosure | notFound() quando perfil não existe | mitigate | Retorna 404 (correto); não expõe razão de not found (apenas "not found page") |

</threat_model>

<verification>
After Wave 3 completion (final phase):
- [ ] `npm run test` passes all 28+ tests (Wave 0-3 combined)
- [ ] src/app/(shell)/profiles/[id]/page.tsx exists and compiles
- [ ] src/components/profile/profile-list.tsx modified (click without /edit)
- [ ] npm run build succeeds (0 TypeScript errors)
- [ ] Manual integration test passes: list → detail → edit → delete
- [ ] All three tabs (Perfil, Vagas, Resumo) render without errors
- [ ] notFound() returns 404 for invalid profile ID
</verification>

<success_criteria>
- src/app/(shell)/profiles/[id]/page.tsx created (30+ lines, Server Component async)
- await params destructuring correct (Next.js 16 pattern)
- getProfile() + notFound() implemented
- getResearchesByProfileId() called and passed to ProfileDetailTabs
- Layout matches edit page (p-8, max-w-4xl, h1 with profile.title)
- src/components/profile/profile-list.tsx modified (line 70, /edit removed from onClick)
- e.stopPropagation() on action buttons remains intact
- Buttons Editar/Deletar continue to work correctly
- All tests pass (28+ total across Wave 0-3)
- npm run build: 0 errors
- Manual integration test: click card → detail → edit flow works
- All three tabs render and function correctly
- Requirement coverage: VIZ-01 (tabs, vagas list), VIZ-02 (resumo), VIZ-03 (select previous research)
</success_criteria>

<output>
After completion, create `.planning/phases/08-market-research-frontend/08-04-SUMMARY.md`

Also update `.planning/ROADMAP.md`:
- Mark Phase 8 as Complete ✓ (date: today)
- All 3 requirements (VIZ-01, VIZ-02, VIZ-03) satisfied
- 4 plans executed (08-01 through 08-04)
</output>
