---
phase: "08"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/repositories/research-repository.test.ts
  - src/components/profile/profile-detail-tabs.test.tsx
  - src/components/profile/profile-detail-perfil.test.tsx
  - src/components/profile/profile-detail-vagas.test.tsx
  - src/components/profile/profile-detail-resumo.test.tsx
  - src/app/\(shell\)/profiles/\[id\]/page.test.tsx
  - src/lib/repositories/research-repository.test.ts
autonomous: true
requirements: [VIZ-01, VIZ-02, VIZ-03]
user_setup: []

must_haves:
  truths:
    - "ResearchRepository interface define contrato de listagem e carregamento de pesquisas"
    - "Componentes de detalhe (Perfil, Vagas, Resumo) têm testes RED com casos de uso claros"
    - "Testes de integração cobrem expansão de vagas e seleção de data"
  artifacts:
    - path: "src/lib/repositories/research-repository.test.ts"
      provides: "Testes unitários de ResearchRepository (listByProfileId, getVagas, getResumo)"
      min_lines: 50
    - path: "src/components/profile/profile-detail-tabs.test.tsx"
      provides: "Testes de navegação entre abas"
      min_lines: 30
    - path: "src/components/profile/profile-detail-perfil.test.tsx"
      provides: "Testes de renderização leitura de perfil com omissão de campos vazios"
      min_lines: 40
    - path: "src/components/profile/profile-detail-vagas.test.tsx"
      provides: "Testes de lista de pesquisas, expansão, seleção de data (VIZ-01, VIZ-03)"
      min_lines: 60
    - path: "src/components/profile/profile-detail-resumo.test.tsx"
      provides: "Testes de stackFrequency ranqueado, salaryGuide, empty state (VIZ-02)"
      min_lines: 50
    - path: "src/app/\(shell\)/profiles/\[id\]/page.test.tsx"
      provides: "Testes de Server Component page.tsx com params async, notFound()"
      min_lines: 30
  key_links:
    - from: "test files"
      to: "source files"
      via: "vitest import paths"
      pattern: "from @/lib/repositories"
---

<objective>
Criar stubs de teste (RED) para todos os arquivos de Phase 8 antes de implementação, garantindo cobertura de VIZ-01, VIZ-02, VIZ-03 via testes automatizados.

Purpose: Nyquist compliance — testes escritos primeiro (RED), evitando implementação sem especificação. Permite que executor escreva código com confiança para passar testes.

Output: 6 arquivos de teste com casos failing, prontos para GREEN phase
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-market-research-frontend/08-CONTEXT.md
@.planning/phases/08-market-research-frontend/08-RESEARCH.md
@.planning/phases/08-market-research-frontend/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 01: Criar test stub para ResearchRepository (listByProfileId, getVagas, getResumo)</name>
  <files>src/lib/repositories/research-repository.test.ts</files>
  <read_first>
    src/lib/repositories/profile-repository.ts (padrão de test que será replicado)
    src/lib/profile.ts (tipos JobProfile e ProfileItem para test fixtures)
  </read_first>
  <action>
Criar arquivo de teste com stubs FAILING para ResearchRepository:

Imports necessários:
- `describe`, `it`, `expect`, `beforeEach` de vitest
- `ResearchRepository`, `Research` types (será criado em Wave 1)
- Mock filesystem via `fs.readdirSync`, `fs.existsSync`, `fs.readFileSync` (padrão pattern da codebase)

Casos de teste (todos FAILING — RED):

1. `describe('JsonResearchRepository', ...)`:
   - `it('listByProfileId returns researches sorted by date descending', ...)`:
     Mock fs para retornar arquivos `2026-04-24-vagas.json`, `2026-04-24-resumo.json`, `2026-04-23-vagas.json`
     Esperado: array de 2 Research objects, data DESC (2026-04-24, then 2026-04-23)
   
   - `it('listByProfileId handles file suffix -2, -3 in same day', ...)`:
     Mock fs para retornar `2026-04-24-vagas.json`, `2026-04-24-2-vagas.json` (colisão de data)
     Esperado: apenas 1 Research object (data 2026-04-24) com ambos vagasFile e resumoFile consolidados
   
   - `it('listByProfileId returns empty array when directory not exists', ...)`:
     Mock fs.readdirSync para lançar ENOENT
     Esperado: []
   
   - `it('listByProfileId rejects invalid profileId (path traversal)', ...)`:
     Chamar listByProfileId com "../../../etc/passwd"
     Esperado: throw Error com mensagem sobre ID inválido
   
   - `it('getVagas returns parsed JSON content', ...)`:
     Mock fs para retornar arquivo 2026-04-24-vagas.json com structure: { profileId, jobs: [{title, company, stack}] }
     Esperado: retorna object exato do JSON
   
   - `it('getResumo returns parsed JSON content with all fields', ...)`:
     Mock fs para retornar 2026-04-24-resumo.json com todos os campos: commonTitles, titleAliases, stackFrequency, salaryRange, salaryGuide, emergingStack, commonBehaviors, commonChallenges, archetypes, profileHints
     Esperado: retorna object com todos os campos preservados
   
   - `it('getVagas returns null when file not found', ...)`:
     Mock fs.existsSync para false
     Esperado: null

Não implementar — apenas definir expects() sem corpo das funções (será implementado em Wave 1).
  </action>
  <verify>
    <automated>npm run test -- src/lib/repositories/research-repository.test.ts 2>&1 | grep -E "(FAIL|failing|undefined)" | head -5</automated>
  </verify>
  <acceptance_criteria>
Arquivo existe em src/lib/repositories/research-repository.test.ts com todos os 8 casos de teste listados acima. Quando rodado `npm run test -- src/lib/repositories/research-repository.test.ts`, retorna status FAIL (testes não implementados — RED state desejado). Arquivo não contém implementação de ResearchRepository (será criada em Wave 1).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 02: Criar test stubs para ProfileDetailTabs, ProfileDetailPerfil, ProfileDetailVagas, ProfileDetailResumo</name>
  <files>
    src/components/profile/profile-detail-tabs.test.tsx
    src/components/profile/profile-detail-perfil.test.tsx
    src/components/profile/profile-detail-vagas.test.tsx
    src/components/profile/profile-detail-resumo.test.tsx
  </files>
  <read_first>
    src/components/profile/profile-form.test.tsx (padrão de test para componentes — se existe)
    src/components/profile/profile-list.test.tsx (padrão de test para lista — se existe)
    src/lib/profile.ts (schema JobProfile para fixtures)
    data/research/2386bf16-4519-409c-9188-45068255df75/2026-04-24-resumo.json (estrutura real de resumo)
    data/research/2386bf16-4519-409c-9188-45068255df75/2026-04-24-vagas.json (estrutura real de vagas)
  </read_first>
  <action>
Criar 4 arquivos de teste com stubs FAILING:

### src/components/profile/profile-detail-tabs.test.tsx

Imports: `describe`, `it`, `expect`, `render`, `screen`, `fireEvent` de @testing-library/react

Casos:
1. `it('renders three tabs: Perfil, Vagas, Resumo de Mercado')`:
   Render ProfileDetailTabs com profile mock e researches mock (não vazio)
   Esperado: 3 botões no TabsList com textos "Perfil", "Vagas", "Resumo de Mercado"

2. `it('clicking tab triggers tab change')`:
   Render, clique em "Vagas", esperado: conteúdo de Vagas visível

3. `it('passes profile to ProfileDetailPerfil on tab select')`:
   Render com profile específico, clique "Perfil", esperado: profile renderizado no componente filho

### src/components/profile/profile-detail-perfil.test.tsx

Casos:
1. `it('renders profile title and suggestedTitle')`:
   Render com profile { title: "Dev Java Sr", suggestedTitle: "Senior Engineer" }
   Esperado: ambos os textos visíveis

2. `it('renders Responsabilidades section when not empty')`:
   Render com profile { responsibilities: ["Arquitetura", "Mentoria"] }
   Esperado: seção "Responsabilidades" com ambos os itens em lista bullet

3. `it('omits empty sections (no Responsabilidades if empty)')`:
   Render com profile { responsibilities: [] }
   Esperado: nenhuma seção "Responsabilidades" no DOM

4. `it('renders Qualificações with badge required/desejável')`:
   Render com profile { qualifications: [{text: "5+ Java", required: true}, {text: "Docker", required: false}] }
   Esperado: badge "Obrigatório" para primeiro, "Desejável" para segundo

5. `it('clicking Editar button navigates to /profiles/{id}/edit')`:
   Render, clique "Editar", esperado: window.location.href = `/profiles/{id}/edit`

### src/components/profile/profile-detail-vagas.test.tsx

Casos:
1. `it('renders empty state when researches is empty')`:
   Render ProfileDetailVagas com researches=[]
   Esperado: mensagem "Nenhuma pesquisa de mercado" visível

2. `it('renders list of researches in reverse chronological order')`:
   Render com researches [{ date: "2026-04-24" }, { date: "2026-04-23" }]
   Esperado: 2026-04-24 aparece antes de 2026-04-23 na lista

3. `it('clicking research line expands vagas (accordion)')`:
   Render, clique em linha de pesquisa, esperado: jobs renderizados abaixo

4. `it('can select different research date from dropdown')`:
   Render com 2+ researches, mudar select de data, esperado: lista de vagas atualiza

5. `it('displays job card with title, company, stack, snippet')`:
   Render com vaga { title: "Deve Java", company: "Itau", stack: ["Java", "Spring"], snippet: "..." }
   Esperado: todos os campos visíveis

### src/components/profile/profile-detail-resumo.test.tsx

Casos:
1. `it('renders empty state when researches is empty')`:
   Render ProfileDetailResumo com researches=[]
   Esperado: mensagem vazia visível

2. `it('renders stackFrequency sorted by count descending')`:
   Render com resumo { stackFrequency: { "Java": 15, "Spring": 10, "Go": 5 } }
   Esperado: ordem "Java" (15), "Spring" (10), "Go" (5) — não alfabética

3. `it('renders salaryGuide with source attribution')`:
   Render com resumo { salaryGuide: { min: 10000, max: 18000, sources: [{portal: "Robert Half", year: 2025}] } }
   Esperado: "Robert Half 2025: R$ 10k – R$ 18k"

4. `it('renders commonTitles, commonBehaviors, commonChallenges sections')`:
   Render com resumo contendo todos os campos
   Esperado: 3 seções visíveis com listas

5. `it('renders archetypes sorted by count descending')`:
   Render com resumo { archetypes: [{name: "arquiteto", count: 9}, {name: "especialista", count: 4}] }
   Esperado: ordem arquiteto (9), especialista (4)

Nenhum dos testes deve passar sem implementação — todos inicializam componentes Client mas sem lógica de render.
  </action>
  <verify>
    <automated>npm run test -- src/components/profile/profile-detail-*.test.tsx 2>&1 | grep -c "FAIL"</automated>
  </verify>
  <acceptance_criteria>
4 arquivos criados com stubs completos. Total de 18 casos de teste (5+5+4+4) com expects() sem implementação de componentes (RED). Quando rodado `npm run test -- src/components/profile/profile-detail-*.test.tsx`, retorna FAIL status. Nenhum componente Client deve ser renderizado com sucesso (porque não existem ainda em Wave 1).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 03: Criar test stub para página /profiles/[id] (Server Component)</name>
  <files>src/app/\(shell\)/profiles/\[id\]/page.test.tsx</files>
  <read_first>
    src/app/\(shell\)/profiles/\[id\]/edit/page.tsx (padrão de Server Component async com await params)
    src/lib/repositories/profile-repository.ts (padrão de chamada a repository)
  </read_first>
  <action>
Criar arquivo de teste src/app/(shell)/profiles/[id]/page.test.tsx com stubs FAILING:

Imports:
- Vitest: `describe`, `it`, `expect`, `vi`
- next/navigation mock: `notFound`
- Server Actions: `getProfile` será chamado (criado em Wave 1)

Casos:
1. `it('renders page with valid profile ID')`:
   Mock getProfile() para retornar profile válido
   Mock getResearchesByProfileId() para retornar array de researches
   Esperado: página renderiza ProfileDetailTabs com profile e researches como props

2. `it('calls notFound() when profile not found')`:
   Mock getProfile() para retornar null
   Esperado: notFound() é chamado

3. `it('passes profile.title as heading')`:
   Mock getProfile() para retornar { title: "Dev Java Sr" }
   Esperado: <h1> contém "Dev Java Sr"

4. `it('renders without error when researches is empty array')`:
   Mock getResearchesByProfileId() para retornar []
   Esperado: página renderiza ProfileDetailTabs com researches=[], sem erro

5. `it('properly awaits params async')`:
   Validar que params é Promise<{id: string}>
   Esperado: desconstruição de await params funciona corretamente

Todos os expects() sem implementação de getResearchesByProfileId() (será criada em Wave 1).
  </action>
  <verify>
    <automated>npm run test -- src/app/\(shell\)/profiles/\[id\]/page.test.tsx 2>&1 | grep "FAIL"</automated>
  </verify>
  <acceptance_criteria>
Arquivo criado com 5 casos de teste para Server Component. Mocks de getProfile e getResearchesByProfileId iniciados mas não implementados. Quando rodado, retorna FAIL (RED state). Nenhuma implementação de page.tsx Server Component deve existir (criada em Wave 2).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem profileId | ResearchRepository recebe profileId do usuário (path parameter); deve validar para evitar path traversal (/../ attack) |
| JSON parsing | `-vagas.json` e `-resumo.json` vindos do filesystem; validação de estrutura defensiva |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | ResearchRepository.listByProfileId() | mitigate | Validação de profileId: rejeitar "..", "/", "\\"; usar path.resolve() + startsWith() guard (implementar em Wave 1) |
| T-08-02 | Information Disclosure | fs.readdirSync() em research/{profileId} | mitigate | Garantir que -vagas.json não são expostos via HTTP sem autorização; Server Actions garantem auth via next-auth |
| T-08-03 | Denial of Service | JSON.parse() em -resumo.json malformado | mitigate | try/catch em getResumo(); retornar null se parse falhar — não crashes |

</threat_model>

<verification>
After Wave 0 completion:
- [ ] `npm run test` exits with FAIL status (tests exist, not passing)
- [ ] No RED tests should pass (stubs only, no implementation)
- [ ] File structure matches planned artifacts
- [ ] All require/import paths use @/ aliases correctly
- [ ] Test descriptions match requirements VIZ-01, VIZ-02, VIZ-03
</verification>

<success_criteria>
- 6 test files created in planned locations
- 28 total test cases (RED state — failing)
- All tests use standard vitest + @testing-library/react imports
- No implementation of source files (ResearchRepository, components, page) — only test stubs
- Test descriptions align with Nyquist contract: each test verifies one behavior
- File paths follow project conventions (kebab-case, src/ structure)
</success_criteria>

<output>
After completion, create `.planning/phases/08-market-research-frontend/08-01-SUMMARY.md`
</output>
