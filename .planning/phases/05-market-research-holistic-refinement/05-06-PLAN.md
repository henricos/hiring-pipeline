---
phase: 05-market-research-holistic-refinement
plan: 06
type: execute
wave: 5
depends_on:
  - 05-03
  - 05-04
  - 05-05
files_modified: []
autonomous: false
requirements:
  - IA-01
  - IA-02
  - IA-03
must_haves:
  truths:
    - "Execução de /pesquisar-mercado para Senior P&D Java+Python+TS SP gera dois arquivos: *-vagas.json e *-resumo.json"
    - "Filtro médias+ está ativo e vagas de pequenas empresas são filtradas"
    - "-resumo.json aparece na listagem do Step 2 do /refinar-perfil com data legível"
    - "Sugestões do /refinar-perfil citam explicitamente o stack híbrido e/ou arquétipo de evangelizador"
    - "Step 5 holístico é executado e apresenta findings com opção [A/I/J]"
    - "JSON salvo em data/profiles/ respeita o schema imutável do JobProfile (D-01)"
    - "Perfil renderiza normalmente na web app em /profiles/{id}"
    - "Reexecução do /pesquisar-mercado no mesmo escopo no mesmo dia gera arquivos com sufixo -2"
    - "/abrir-vaga com o perfil refinado funciona corretamente"
    - "Excel gerado pela web app mantém estrutura e rótulos configuráveis"
  artifacts:
    - path: "data/research/{slug}-{date}-vagas.json"
      provides: "Vagas brutas do piloto (Senior P&D Java+Python+TS SP)"
      contains: "\"jobs\""
    - path: "data/research/{slug}-{date}-resumo.json"
      provides: "Resumo executivo + profileHints do piloto"
      contains: "\"profileHints\""
  key_links:
    - from: "/pesquisar-mercado"
      to: "data/research/"
      via: "Step 5 e 6 da skill — node -e salva os dois arquivos"
      pattern: "vagas.json"
    - from: "/refinar-perfil Step 2"
      to: "data/research/*-resumo.json"
      via: "listagem com data legível, carregamento do resumo escolhido"
      pattern: "resumo.json"
    - from: "data/profiles/{id}.json"
      to: "web app /profiles/{id}"
      via: "ProfileRepository lê arquivo; UI renderiza perfil"
      pattern: "profiles/"
---

<objective>
Execução piloto end-to-end do fluxo completo: /pesquisar-mercado → /refinar-perfil (com pesquisa de mercado + aiProfileInstructions P&D) → /abrir-vaga → geração de Excel. Perfil piloto: Engenheiro Sênior P&D Java + Python + TypeScript em São Paulo.

Purpose: Verificar que toda a phase 5 funciona de ponta a ponta — o piloto É a verificação da phase (D-21). Documenta o fluxo como referência viva.

Output: Perfil piloto refinado em data/profiles/, pesquisa real em data/research/, vaga aberta em data/vacancies/, Excel gerado pela web app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-market-research-holistic-refinement/05-CONTEXT.md
@.planning/phases/05-market-research-holistic-refinement/05-01-PORTALS.md
@.agents/skills/pesquisar-mercado/SKILL.md
@.agents/skills/refinar-perfil/SKILL.md
@.agents/skills/abrir-vaga/SKILL.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Executar /pesquisar-mercado e verificar dual-file output</name>

  <read_first>
    - .agents/skills/pesquisar-mercado/SKILL.md (7 steps completos — guia a execução)
    - .planning/phases/05-market-research-holistic-refinement/05-01-PORTALS.md (portais aprovados para o Step 2)
    - .planning/phases/05-market-research-holistic-refinement/05-CONTEXT.md (D-05 — nomenclatura e colisão de nomes)
  </read_first>

  <what-built>
    Esta task executa a skill /pesquisar-mercado para o escopo piloto. O executor deve rodar a skill e acompanhar o fluxo conversacional de 7 steps.
  </what-built>

  <how-to-verify>
    **Executar a skill:**
    1. Iniciar /pesquisar-mercado (via Claude Code ou Cursor)
    2. No Step 1, fornecer o escopo piloto:
       - Cargo: Engenheiro Sênior de Software / Senior Software Engineer
       - Localização: São Paulo
       - Senioridade: Sênior
       - Stacks: Java, Python, TypeScript
       - Profundidade: média (15-25 vagas)
       - Porte: médias+
    3. Acompanhar Steps 2-6 (queries, WebFetch, extração, filtragem, salvamento)

    **Verificações pós-execução:**

    a) Verificar que DOIS arquivos foram criados em data/research/:
    ```bash
    ls -la $DATA_PATH/research/ | grep "$(date +%Y-%m-%d)"
    ```
    Esperado: 2 arquivos — um com sufixo `-vagas.json` e outro `-resumo.json`

    b) Verificar que o arquivo de vagas tem `jobs[]` não-vazio:
    ```bash
    node -e "
    const d = JSON.parse(require('fs').readFileSync('$DATA_PATH/research/$(ls $DATA_PATH/research/ | grep vagas | tail -1)', 'utf8'));
    console.log('jobs count:', d.jobs.length);
    console.log('scope:', JSON.stringify(d.scope));
    "
    ```
    Esperado: `jobs count: >= 5`

    c) Verificar que o resumo tem profileHints com os 4 campos:
    ```bash
    node -e "
    const d = JSON.parse(require('fs').readFileSync('$DATA_PATH/research/$(ls $DATA_PATH/research/ | grep resumo | tail -1)', 'utf8'));
    console.log('has profileHints:', !!d.profileHints);
    console.log('responsibilities:', d.profileHints.responsibilities?.length ?? 0);
    console.log('qualifications:', d.profileHints.qualifications?.length ?? 0);
    console.log('behaviors:', d.profileHints.behaviors?.length ?? 0);
    console.log('challenges:', d.profileHints.challenges?.length ?? 0);
    console.log('vagasFile:', d.vagasFile);
    "
    ```
    Esperado: todos os campos com pelo menos 1 item; `vagasFile` referencia o arquivo de vagas

    d) Verificar filtro de porte — vagas de pequenas empresas não aparecem em jobs[]:
    ```bash
    node -e "
    const d = JSON.parse(require('fs').readFileSync('$DATA_PATH/research/$(ls $DATA_PATH/research/ | grep vagas | tail -1)', 'utf8'));
    const small = d.jobs.filter(j => j.companySize === 'pequena');
    console.log('vagas de pequenas empresas (deve ser 0):', small.length);
    "
    ```

    e) Verificar colisão (reexecutar /pesquisar-mercado no mesmo escopo no mesmo dia):
    - Rodar /pesquisar-mercado novamente com o mesmo escopo
    - Confirmar que novos arquivos saem com sufixo `-2` (ex: `...-2026-04-22-2-vagas.json`)
    ```bash
    ls $DATA_PATH/research/ | grep "$(date +%Y-%m-%d)"
    ```
    Esperado: 4 arquivos (2 originais + 2 com sufixo `-2`)
  </how-to-verify>

  <resume-signal>
    Digite "pesquisa-ok" quando todos os checks acima passarem, descrevendo quais portais foram usados e quantas vagas foram coletadas.
  </resume-signal>

  <acceptance_criteria>
    - Dois arquivos criados: `*-vagas.json` e `*-resumo.json` com o slug do escopo piloto
    - `jobs.length >= 5` no arquivo de vagas
    - `profileHints` presente no resumo com os 4 campos não-vazios
    - `vagasFile` no resumo referencia o arquivo de vagas correspondente
    - Vagas com `companySize: "pequena"` ausentes de `jobs[]` (filtro médias+ ativo)
    - Reexecução gera arquivos com sufixo `-2` (colisão no mesmo dia)
  </acceptance_criteria>

  <done>Skill /pesquisar-mercado executada com sucesso para o escopo piloto — dual-file output validado com filtro de porte e colisão de nomes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Executar /refinar-perfil com pesquisa + Step 5 holístico</name>

  <read_first>
    - .agents/skills/refinar-perfil/SKILL.md (versão evoluída com Step 2 de pesquisa e Step 5 holístico)
    - .planning/phases/05-market-research-holistic-refinement/05-CONTEXT.md (verificação end-to-end itens 3-6)
  </read_first>

  <what-built>
    Esta task executa o /refinar-perfil evoluído (05-04) com a pesquisa gerada na Task 1 como contexto, valida que o Step 2 lista a pesquisa com data legível, que as sugestões refletem o stack híbrido e aiProfileInstructions P&D, e que o Step 5 holístico é executado.
  </what-built>

  <how-to-verify>
    **Executar a skill:**
    1. Iniciar /refinar-perfil
    2. Step 1: selecionar um perfil Java existente (ex: Desenvolvedor Java Pleno)
    3. Step 2 (evoluído): verificar que a pesquisa do piloto aparece na listagem com data legível
       - Esperado: "1. senior-pd-java-python-ts-sp-{data}-resumo.json — hoje"
       - Selecionar a pesquisa (número 1)
    4. Step 3: escolher "Refinar tudo" (modalidade 3)
    5. Steps 4 (ciclo A/R/J): observar que as sugestões citam stack híbrido (Java + Python + TS)
       e/ou arquétipo de evangelizador/IA. Aceitar algumas sugestões.

    **Verificações durante o fluxo:**

    a) Verificar listagem de pesquisas no Step 2:
    - O arquivo `-resumo.json` do piloto aparece na lista (não o `-vagas.json`)
    - A data aparece como "hoje" ou data legível

    b) Verificar qualidade das sugestões no Step 4:
    - Pelo menos 1 sugestão menciona Java, Python ou TypeScript (stack híbrido)
    - Tom das sugestões reflete o contexto P&D/Lyceum (não genérico de mercado)
    - Se aiProfileInstructions foi configurado em 05-05, deve refletir no tom

    c) Verificar Step 5 holístico:
    - Após o ciclo A/R/J completo, o Step 5 é executado (não pula para gravação)
    - A IA apresenta findings (ou diz "Nenhuma incoerência detectada")
    - Se há findings: cada um tem opção [A]plicar / [I]gnorar / [J]ustar
    - Processar pelo menos 1 finding (aceitar, ignorar ou ajustar)

    d) Verificar o JSON gravado (Step 6):
    ```bash
    node -e "
    const fs = require('fs');
    const path = require('path');
    const profiles = fs.readdirSync(path.join(process.env.DATA_PATH || './data', 'profiles'))
      .filter(f => f.endsWith('.json'))
      .map(f => {
        const d = JSON.parse(fs.readFileSync(path.join(process.env.DATA_PATH || './data', 'profiles', f), 'utf8'));
        return { file: f, title: d.title, updatedAt: d.updatedAt, respCount: d.responsibilities?.length };
      })
      .sort((a, b) => new Date(b.updatedAt) - new Date(a.updatedAt));
    console.log('Último perfil modificado:', JSON.stringify(profiles[0], null, 2));
    "
    ```
    - `responsibilities` é `string[]` (não objeto)
    - `qualifications` é `ProfileItem[]` com `{ text, required }` (não string[])
    - `updatedAt` é timestamp recente
    - Não há campos fora do schema de `src/lib/profile.ts`

    e) Verificar que perfil renderiza na web app:
    - Abrir http://localhost:3000/hiring-pipeline/profiles/{id} no browser
    - Página renderiza sem erros de tipo ou crash
    - Os campos aceitos no refino aparecem atualizados
  </how-to-verify>

  <resume-signal>
    Digite "refino-ok" descrevendo: (1) a pesquisa foi listada corretamente no Step 2, (2) as sugestões refletiram stack híbrido/P&D, (3) o Step 5 holístico foi executado, (4) o perfil renderiza normalmente na web app.
  </resume-signal>

  <acceptance_criteria>
    - Pesquisa do piloto aparece no Step 2 com data legível ("hoje")
    - Ao menos 1 sugestão do Step 4 reflete stack Java/Python/TypeScript ou contexto P&D
    - Step 5 holístico é executado com opção [A/I/J] por finding
    - JSON gravado tem `responsibilities` como `string[]` e `qualifications` como `ProfileItem[]`
    - Nenhum campo novo fora do schema de src/lib/profile.ts no JSON gravado
    - Perfil renderiza em /profiles/{id} sem erros
  </acceptance_criteria>

  <done>Fluxo /refinar-perfil evoluído validado com pesquisa de mercado como contexto + Step 5 holístico funcionando corretamente.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Executar /abrir-vaga e gerar Excel — verificar downstream OK</name>

  <read_first>
    - .agents/skills/abrir-vaga/SKILL.md (referência da skill)
    - .planning/phases/05-market-research-holistic-refinement/05-CONTEXT.md (verificação end-to-end itens 8-9)
  </read_first>

  <what-built>
    Esta task completa o piloto executando /abrir-vaga com o perfil refinado na Task 2 e gerando o Excel via web app — confirmando que as mudanças das skills de Phase 5 não quebraram nenhum downstream.
  </what-built>

  <how-to-verify>
    **Executar /abrir-vaga:**
    1. Iniciar /abrir-vaga
    2. Selecionar o perfil refinado na Task 2
    3. Preencher dados mínimos da vaga:
       - Tipo: Recrutamento externo
       - Quantidade: 1
       - Salário: R$ 14k - 20k (ou qualquer faixa)
    4. Confirmar e verificar que a vaga é criada em `data/vacancies/{uuid}.json`

    **Verificar Excel:**
    5. Abrir a web app em /vacancies/{id}/edit
    6. Clicar em "Gerar formulário GH"
    7. Verificar que o download do Excel ocorre sem erro
    8. Abrir o Excel e confirmar:
       - Campos do perfil preenchidos (responsabilidades, qualificações)
       - Rótulos configuráveis presentes (gestor, padrinho, etc. de settings.json)
       - Estrutura da planilha GH está correta

    **Verificar que a web app renderiza normalmente:**
    - /profiles — lista de perfis sem erro
    - /profiles/{id} — perfil piloto renderizado
    - /vacancies — lista de vagas sem erro
    - /vacancies/{id}/edit — formulário de vaga renderizado
  </how-to-verify>

  <resume-signal>
    Digite "piloto-concluido" descrevendo: (1) vaga criada com sucesso, (2) Excel gerado sem erro, (3) campos do perfil aparecem no Excel, (4) web app renderiza normalmente em todas as rotas verificadas.
  </resume-signal>

  <acceptance_criteria>
    - `ls $DATA_PATH/vacancies/ | wc -l` aumentou em 1 após o /abrir-vaga
    - Excel é baixado sem erro pela web app
    - Excel contém dados do perfil refinado (responsabilidades e qualificações não vazias)
    - Nenhuma página da web app crasha ao renderizar perfis/vagas do piloto
    - Phase 5 complete: todos os 10 itens do checklist de verificação do CONTEXT.md passaram
  </acceptance_criteria>

  <done>Piloto end-to-end concluído — /pesquisar-mercado → /refinar-perfil → /abrir-vaga → Excel todos funcionando. Phase 5 validada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /pesquisar-mercado output → /refinar-perfil context | profileHints do resumo injetado como contexto — verificar que não contém dados pessoais |
| data/profiles/{id}.json → web app render | Schema imutável D-01 — qualquer campo fora do schema causa erro de tipo na web app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | perfil JSON gravado pelo /refinar-perfil | mitigate | Verificação explícita: `qualifications` deve ser `ProfileItem[]` não `string[]`; nenhum campo extra fora do schema |
| T-05-06-02 | Information Disclosure | data gerada no piloto | accept | Dados de vagas são públicos; perfil piloto é interno do gestor; nenhum PII de candidatos |
| T-05-06-03 | Denial of Service | colisão de nomes na reexecução | accept | Verificação inclui confirmar que sufixo -2 funciona — comportamento documentado e esperado |
</threat_model>

<verification>
```bash
# Verificar arquivos de pesquisa do piloto
ls $DATA_PATH/research/ | grep "$(date +%Y-%m-%d)"

# Verificar que colisão gera sufixo -2
ls $DATA_PATH/research/ | grep "\-2\-vagas\|\-2\-resumo"

# Verificar último perfil modificado
node -e "
const fs = require('fs');
const path = require('path');
const dir = path.join(process.env.DATA_PATH || './data', 'profiles');
const files = fs.readdirSync(dir).filter(f => f.endsWith('.json'));
const profiles = files.map(f => {
  const d = JSON.parse(fs.readFileSync(path.join(dir, f), 'utf8'));
  return { title: d.title, updatedAt: d.updatedAt, hasStringArrayResp: Array.isArray(d.responsibilities) };
});
const last = profiles.sort((a, b) => new Date(b.updatedAt) - new Date(a.updatedAt))[0];
console.log('Último perfil:', last.title, '| updatedAt:', last.updatedAt, '| resp é array:', last.hasStringArrayResp);
"

# Verificar vaga criada
ls $DATA_PATH/vacancies/ | wc -l
```
</verification>

<success_criteria>
- /pesquisar-mercado gerou dual-file output com vagas reais e resumo com profileHints
- Filtro médias+ funcionou (sem vagas de pequenas empresas em jobs[])
- Colisão no mesmo dia gera sufixo -2 corretamente
- /refinar-perfil listou a pesquisa com data legível e carregou o -resumo.json
- Sugestões do /refinar-perfil refletiram stack híbrido ou contexto P&D
- Step 5 holístico executado com findings e padrão [A/I/J]
- JSON gravado respeita schema imutável do JobProfile (D-01)
- Web app renderiza perfil normalmente sem erros de tipo
- /abrir-vaga criou vaga com o perfil refinado
- Excel gerado pela web app tem dados do perfil e rótulos configuráveis
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-market-research-holistic-refinement/05-06-SUMMARY.md` contendo:
- Registro completo do piloto (portais usados, vagas coletadas, perfil refinado)
- Resultado de cada verificação do checklist do CONTEXT.md
- Notas sobre qualidade das sugestões e findings holísticos
- Indicação de que a Phase 5 está completa e pronta para o bump SemVer via /fechar-versao
</output>
