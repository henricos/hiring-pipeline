---
phase: 05-market-research-holistic-refinement
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - .agents/skills/refinar-perfil/SKILL.md
autonomous: true
requirements:
  - IA-01
  - IA-02
  - IA-03
must_haves:
  truths:
    - "Step 2 do /refinar-perfil pergunta sobre pesquisa de mercado e lista apenas *-resumo.json ordenados por data desc"
    - "Ao escolher pesquisa, apenas o -resumo.json é carregado (não o -vagas.json)"
    - "Prompts dos Steps 3-4 recebem 3 contextos empilhados: aiProfileInstructions + pesquisa + perfil"
    - "Novo Step 5 holístico detecta 4 tipos de incoerência com padrão [A]plicar/[I]gnorar/[J]ustar"
    - "Step de gravação é renumerado de 5 para 6 após inserção do holístico"
    - "Step de conclusão é renumerado de 6 para 7"
    - "A skill continua retrocompatível: pular pesquisa funciona igual ao fluxo original"
    - "Apenas .agents/skills/refinar-perfil/SKILL.md é modificado (apontamentos .claude/ e .cursor/ não mudam)"
  artifacts:
    - path: ".agents/skills/refinar-perfil/SKILL.md"
      provides: "Skill /refinar-perfil evoluída com contexto de mercado + Step 5 holístico"
      contains: "### Step 5: Revisão Holística"
  key_links:
    - from: ".agents/skills/refinar-perfil/SKILL.md Step 2"
      to: "DATA_PATH/research/*-resumo.json"
      via: "ls $DATA_PATH/research/*-resumo.json + seleção por número"
      pattern: "resumo.json"
    - from: ".agents/skills/refinar-perfil/SKILL.md Step 5"
      to: "perfil em memória (pós Step 4)"
      via: "revisão holística dos 4 campos antes de salvar"
      pattern: "holística"
---

<objective>
Evoluir a skill /refinar-perfil para suportar: (1) carregamento opcional de pesquisa de mercado no Step 2, (2) prompts com três contextos empilhados nos Steps 3-4, e (3) novo Step 5 de revisão holística com 4 tipos de incoerência e padrão [A/I/J]. Apenas a fonte de verdade em .agents/skills/refinar-perfil/SKILL.md é modificada — os apontamentos em .claude/ e .cursor/ já apontam para ela e não precisam de alteração.

Purpose: Completar os requisitos IA-03 (contexto P&D/Lyceum) com pesquisa real de mercado como contexto variável e garantir coerência interna do perfil antes do save final.

Output: `.agents/skills/refinar-perfil/SKILL.md` atualizado com Step 2 evoluído, prompts de 3 contextos, e novo Step 5 holístico (Steps 5→6, 6→7).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-market-research-holistic-refinement/05-CONTEXT.md
@.agents/skills/refinar-perfil/SKILL.md
@.agents/skills/pesquisar-mercado/SKILL.md
</context>

<interfaces>
<!-- Campos do JobProfile que profileHints pode referenciar (src/lib/profile.ts — IMUTÁVEL D-01) -->
<!-- Os contextos empilhados dos Steps 3-4 devem orientar a IA a usar APENAS estes campos -->

```typescript
interface JobProfile {
  title: string;
  experienceLevel: ExperienceLevel; // "< 1 ano" | "1-3 anos" | "3-5 anos" | "5-10 anos" | "> 10 anos"
  responsibilities: string[];
  qualifications: ProfileItem[]; // { text: string, required: boolean }
  behaviors: string[];
  challenges: string[];
  // ... outros campos de identificação — não modificados pela skill
}
```

<!-- Estrutura do -resumo.json carregado no Step 2 (output do /pesquisar-mercado — 05-03) -->
```json
{
  "slug": "...",
  "createdAt": "ISO8601",
  "vagasFile": "...",
  "summary": { "commonTitles": [], "stackFrequency": {}, "trends": [], ... },
  "profileHints": {
    "responsibilities": [], "qualifications": [], "behaviors": [], "challenges": [],
    "suggestedTitle": "...", "suggestedExperienceLevel": "..."
  }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Evoluir Step 2 — listagem de pesquisas e carregamento do -resumo.json</name>
  <files>.agents/skills/refinar-perfil/SKILL.md</files>

  <read_first>
    - .agents/skills/refinar-perfil/SKILL.md (Step 2 atual — seção "### Step 2: Carregar Perfil e Contexto de Área")
    - .planning/phases/05-market-research-holistic-refinement/05-CONTEXT.md (D-11, D-12, D-13, D-22 — decisões de Step 2 e prompts)
  </read_first>

  <action>
Modificar o Step 2 atual da skill `/refinar-perfil` para incluir a pergunta sobre pesquisa de mercado.

**Conteúdo atual do Step 2** (lido de .agents/skills/refinar-perfil/SKILL.md):
- Carrega perfil selecionado
- Carrega settings.json
- Exibe resumo do perfil e aiProfileInstructions

**Modificações a fazer no Step 2:**

Após exibir o resumo atual do perfil (e antes de avançar para o Step 3), inserir o seguinte bloco:

```markdown
**Pergunta sobre pesquisa de mercado (opcional):**

```bash
ls -t $DATA_PATH/research/*-resumo.json 2>/dev/null
```

Se houver arquivos `-resumo.json`:
```
Pesquisas de mercado disponíveis:
1. senior-pd-java-python-ts-sp-2026-04-22-resumo.json — hoje
2. senior-pd-java-python-ts-sp-2026-04-21-resumo.json — ontem
3. dev-java-pleno-sp-2026-04-18-resumo.json — 4 dias atrás

Carregar pesquisa como contexto? (número ou Enter para pular)
```

Para calcular a data legível (ex: "hoje", "ontem", "N dias atrás"):
```bash
node -e "
const files = require('fs').readdirSync(require('path').join(process.env.DATA_PATH || './data', 'research'))
  .filter(f => f.endsWith('-resumo.json'))
  .sort().reverse();
const today = new Date().toISOString().split('T')[0];
files.forEach((f, i) => {
  const match = f.match(/(\d{4}-\d{2}-\d{2})/);
  const fileDate = match ? match[1] : null;
  let label = fileDate;
  if (fileDate) {
    const diff = Math.floor((new Date(today) - new Date(fileDate)) / (1000 * 60 * 60 * 24));
    if (diff === 0) label = 'hoje';
    else if (diff === 1) label = 'ontem';
    else label = diff + ' dias atrás';
  }
  console.log((i + 1) + '. ' + f + ' — ' + label);
});
"
```

Se o gestor escolher um número:
- Carregar APENAS o arquivo `-resumo.json` correspondente (NÃO carregar o `-vagas.json` — é arquivo de auditoria)
- Registrar o conteúdo do resumo como `marketResearch` em memória para uso nos Steps 3-4

Se o gestor pular (Enter sem número):
- `marketResearch = null`
- Fluxo continua exatamente como antes (retrocompatível)

Se não houver arquivos `-resumo.json`:
- Exibir: `(Nenhuma pesquisa de mercado disponível — execute /pesquisar-mercado para criar uma)`
- `marketResearch = null`
- Fluxo continua normalmente

Exibir ao final do Step 2:
```
Perfil carregado: {title}
...
Contexto de área: {aiProfileInstructions}
Pesquisa de mercado: {nome-do-arquivo ou "(não carregada)"}
```
```

**Também atualizar os system prompts dos Steps 3-4** para incluir os três contextos empilhados quando pesquisa estiver carregada (D-12, D-13):

Substituir os system prompts atuais (que têm apenas aiProfileInstructions) pelo seguinte padrão nos Steps 3 e 4:

```markdown
**Contexto empilhado (quando marketResearch não é null):**
- System prompt 1 (fixo): "Você é um especialista em recrutamento para {title}. {aiProfileInstructions}"
- System prompt 2 (variável): "Pesquisa de mercado atual para este cargo:\n{JSON.stringify(marketResearch.profileHints, null, 2)}\n\nTendências: {marketResearch.summary.trends.join('; ')}"
- System prompt 3: o perfil atual sendo refinado

**Instrução de priorização (quando pesquisa presente):**
"Priorize os dados da pesquisa de mercado ao sugerir melhorias. Quando a pesquisa indicar tendências de stack ou arquétipo, expresse essa riqueza dentro dos 4 campos descritivos do perfil (responsibilities, qualifications, behaviors, challenges) — NÃO invente novos campos."

**Quando marketResearch é null:** usar apenas System prompt 1 (comportamento original)
```

Atualizar também a seção **Notes for Agent** para documentar:
- Carregamento apenas do -resumo.json (não do -vagas.json)
- Comportamento de fallback quando pesquisa não selecionada
- Priorização da pesquisa nos prompts quando presente
  </action>

  <verify>
    <automated>grep -c "resumo.json" .agents/skills/refinar-perfil/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep "resumo.json" .agents/skills/refinar-perfil/SKILL.md` retorna pelo menos 2 matches
    - `grep "dias atrás" .agents/skills/refinar-perfil/SKILL.md` retorna ao menos 1 match (data legível)
    - `grep "vagas.json" .agents/skills/refinar-perfil/SKILL.md` NÃO aparece na instrução de carregamento (apenas resumo é carregado)
    - `grep "marketResearch" .agents/skills/refinar-perfil/SKILL.md` retorna pelo menos 3 matches (declaração + uso nos steps 3-4 + notes)
    - `grep "três contextos\|contextos empilhados\|System prompt" .agents/skills/refinar-perfil/SKILL.md` retorna match
    - Step 2 ainda contém carregamento do perfil e settings (retrocompatível — não removeu comportamento original)
  </acceptance_criteria>

  <done>Step 2 evoluído com listagem de pesquisas disponíveis, data legível, carregamento exclusivo do -resumo.json e prompts de três contextos empilhados documentados.</done>
</task>

<task type="auto">
  <name>Task 2: Inserir Step 5 holístico e renumerar Steps 5→6 e 6→7</name>
  <files>.agents/skills/refinar-perfil/SKILL.md</files>

  <read_first>
    - .agents/skills/refinar-perfil/SKILL.md (após Task 1 — Steps 3-5 atuais para entender o que renumerar)
    - .planning/phases/05-market-research-holistic-refinement/05-CONTEXT.md (D-14 — 4 tipos de incoerência, padrão [A/I/J], sem limite de findings)
  </read_first>

  <action>
Inserir o novo Step 5 holístico ENTRE o Step 4 (ciclo A/R/J) e o Step 5 atual (confirmar e gravar), e renumerar os steps existentes.

**Renumeração obrigatória:**
- Step 5 atual "Confirmar e Gravar" → passa a ser **Step 6**
- Step 6 atual "Confirmar Conclusão" → passa a ser **Step 7**

**Novo Step 5: Revisão Holística**

Inserir o seguinte conteúdo como novo Step 5 na skill:

```markdown
### Step 5: Revisão Holística

Com o perfil completo pós-ciclo A/R/J em memória, solicitar à IA que detecte incoerências internas. A IA lê o perfil na íntegra e identifica os seguintes tipos de incoerência:

**Tipo 1 — Lacunas responsabilidades × qualificações:**
Responsabilidade cita habilidade/área (ex: "arquitetar soluções de IA generativa", "liderar time técnico") mas nenhuma qualificação correspondente existe.

**Tipo 2 — Redundâncias entre campos:**
Mesmo ponto dito de formas ligeiramente diferentes em responsibilities E qualifications, ou em behaviors E challenges.

**Tipo 3 — Descalibração título × conteúdo:**
O `title` diz "Sênior" mas as responsabilidades parecem de Pleno, ou o `experienceLevel` está desalinhado com o nível de complexidade das responsabilidades.

**Tipo 4 — Lacunas comportamentais:**
As responsabilidades exigem liderança, mentoria ou comunicação com stakeholders, mas nenhum `behavior` correspondente existe no perfil.

**Prompt para a IA (executar com perfil completo como contexto):**
- System: "Você é um especialista em coerência de perfis de vaga. Analise o perfil abaixo e identifique inconsistências internas nos seguintes tipos: (1) responsabilidades sem qualificação correspondente, (2) redundâncias entre campos, (3) desalinhamento título/experiência vs. conteúdo, (4) responsabilidades de liderança sem competência comportamental correspondente."
- Pedido: "Retorne cada finding numerado, descrevendo o problema e sugerindo uma correção específica. Sem limite de findings — liste todas as incoerências encontradas."

**Exibição dos findings:**

```
── Revisão Holística ────────────────────────────────
Analisando coerência interna do perfil "{title}"...

Incoerências encontradas:

1. [Lacuna R×Q] Responsabilidade cita "integração com LLMs" mas nenhuma qualificação
   de experiência com APIs de IA está presente.
   Sugestão: adicionar qualificação "[Obrigatório] Experiência com LLMs e APIs de IA generativa"

2. [Redundância] "Comunicação clara com stakeholders" aparece em behaviors e como
   responsabilidade descrita de forma idêntica.
   Sugestão: remover da responsabilidade (manter em behaviors, que é mais adequado).

3. [Descalibração] Title é "Engenheiro Sênior" mas experienceLevel é "1-3 anos".
   Sugestão: ajustar experienceLevel para "5-10 anos" ou revisar o title.

Por item, escolha:
  [A]plicar sugestão  [I]gnorar  [J]ustar (descrever ajuste)
```

**Processar decisão por item:**
- `A` → aplicar a sugestão da IA diretamente ao perfil em memória
- `I` → ignorar; prosseguir para o próximo finding
- `J` → pedir ao gestor que descreva o ajuste; aplicar o ajuste informado; perguntar: "[A]ceitar ajuste? (S/N)"

**Sem limite de findings:** A IA lista TODAS as incoerências encontradas. Gestor decide por item.

Se a IA não encontrar incoerências:
```
Nenhuma incoerência detectada. Perfil coerente para gravação.
```

Ao final dos findings, exibir resumo:
```
Holística concluída.
Findings aplicados: {N}  |  Ignorados: {M}  |  Ajustados: {P}
```

Prosseguir para Step 6 (gravação).
```

**Atualizar seção Notes for Agent** para documentar:
- Step 5 holístico analisa o perfil APÓS o ciclo A/R/J (não antes)
- [A/I/J] é o mesmo padrão do Step 4 — gestor já familiarizado
- Sem limite de findings — IA lista todas as incoerências
- Applying [A] modifica o perfil em memória; gravação real acontece no Step 6

**Atualizar seção Troubleshooting** para incluir:
- "IA lista findings irrelevantes" → usar [I] para ignorar; considerar ajustar aiProfileInstructions
  </action>

  <verify>
    <automated>grep -c "### Step" .agents/skills/refinar-perfil/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "### Step" .agents/skills/refinar-perfil/SKILL.md` retorna 7 (Steps 1 a 7)
    - `grep "### Step 5: Revisão Holística" .agents/skills/refinar-perfil/SKILL.md` retorna match
    - `grep "### Step 6: Confirmar e Gravar" .agents/skills/refinar-perfil/SKILL.md` retorna match (renumerado)
    - `grep "### Step 7: Confirmar Conclusão" .agents/skills/refinar-perfil/SKILL.md` retorna match (renumerado)
    - `grep "Lacuna R×Q\|Redundância\|Descalibração\|Lacunas comportamentais" .agents/skills/refinar-perfil/SKILL.md` retorna ao menos 3 matches (4 tipos de incoerência)
    - `grep "\[A\]plicar\|Aplicar sugestão" .agents/skills/refinar-perfil/SKILL.md` retorna ao menos 2 matches (Step 4 e Step 5)
    - `grep "Holística concluída\|holística" .agents/skills/refinar-perfil/SKILL.md` retorna ao menos 1 match
    - Apontamentos .claude/skills/refinar-perfil/SKILL.md e .cursor/skills/refinar-perfil/SKILL.md NÃO foram modificados (verificar via git diff ou ls -la)
  </acceptance_criteria>

  <done>Step 5 holístico inserido com 4 tipos de incoerência e padrão [A/I/J]; Steps 5→6 e 6→7 renumerados corretamente. Skill /refinar-perfil completamente evoluída conforme D-11 a D-14.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DATA_PATH/research/*-resumo.json → prompt context | Conteúdo do resumo é injetado no prompt — validar que não contém dados pessoais de candidatos |
| perfil em memória → gravação Step 6 | Perfil modificado pelo Step 5 holístico deve respeitar o schema imutável D-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | profileHints injetado no prompt | mitigate | Documentar no Notes: IA deve usar apenas os 4 campos descritivos do JobProfile ao aplicar sugestões do holístico — nunca inventar campos novos |
| T-05-04-02 | Information Disclosure | ls $DATA_PATH/research/*-resumo.json | accept | Conteúdo do resumo é de vagas públicas — nenhum PII de candidatos |
| T-05-04-03 | Tampering | [A]plicar no Step 5 modifica perfil em memória | accept | Gravação real só no Step 6, após confirmação explícita do gestor — mesma proteção já existente |
</threat_model>

<verification>
```bash
# Verificar total de steps
grep -c "### Step" .agents/skills/refinar-perfil/SKILL.md

# Verificar Step 5 holístico existe
grep "### Step 5: Revisão Holística" .agents/skills/refinar-perfil/SKILL.md

# Verificar Steps 6 e 7 (renumerados)
grep "### Step 6:\|### Step 7:" .agents/skills/refinar-perfil/SKILL.md

# Verificar carregamento de pesquisa
grep "resumo.json" .agents/skills/refinar-perfil/SKILL.md | wc -l

# Confirmar que apontamentos não foram modificados (devem ter data anterior)
ls -la .claude/skills/refinar-perfil/SKILL.md .cursor/skills/refinar-perfil/SKILL.md
```
</verification>

<success_criteria>
- .agents/skills/refinar-perfil/SKILL.md tem 7 steps (1 a 7)
- Step 2 lista pesquisas com data legível e carrega apenas -resumo.json
- Steps 3-4 documentam prompts com três contextos empilhados
- Step 5 tem os 4 tipos de incoerência com padrão [A/I/J]
- Steps 6 e 7 são os antigos 5 e 6 renumerados (conteúdo original preservado)
- .claude/skills/refinar-perfil/SKILL.md e .cursor/skills/refinar-perfil/SKILL.md NÃO foram alterados
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-market-research-holistic-refinement/05-04-SUMMARY.md`
</output>
