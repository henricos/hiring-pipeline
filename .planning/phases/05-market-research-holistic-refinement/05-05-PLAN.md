---
phase: 05-market-research-holistic-refinement
plan: 05
type: execute
wave: 4
depends_on:
  - "05-02"
  - "05-03"
  - "05-04"
files_modified: []
autonomous: false
requirements:
  - IA-01
  - IA-02
  - IA-03

must_haves:
  truths:
    - "/pesquisar-mercado gerou JSON com jobs[] não-vazio + summary + profileHints para Senior P&D Java+Python+TS SP"
    - "/refinar-perfil carregou a pesquisa gerada + aiProfileInstructions e produziu sugestões com referência a stack híbrido e arquétipo"
    - "Step 5 holístico foi ativado e exibiu findings (ou confirmou perfil internamente coerente)"
    - "Perfil salvo em data/profiles/ respeita o schema JobProfile imutável (sem campos extras)"
    - "Perfil renderiza normalmente na web app /profiles/{id} (sem erros TypeScript)"
    - "/abrir-vaga funcionou com o perfil refinado — downstream OK"
    - "Excel gerado com sucesso pela web app — rótulos e estrutura GH válidos"
    - "Segunda rodada de /pesquisar-mercado no mesmo escopo no mesmo dia gerou arquivo com sufixo -2"
  artifacts:
    - path: "data/research/{slug}-{YYYY-MM-DD}.json"
      provides: "Primeira pesquisa real do sistema"
      contains: "profileHints"
    - path: "data/research/{slug}-{YYYY-MM-DD}-2.json"
      provides: "Validação do mecanismo de colisão de slug (D-05)"
      contains: "profileHints"
  key_links:
    - from: "/pesquisar-mercado output"
      to: "/refinar-perfil Step 2"
      via: "gestor escolhe pesquisa por número na lista"
      pattern: "research.*json"
    - from: "/refinar-perfil Step 5 holístico"
      to: "data/profiles/{id}.json"
      via: "gestor decide por item antes do save final"
      pattern: "Revisão Holística"
    - from: "data/profiles/{id}.json"
      to: "/profiles/{id} na web app"
      via: "JSON lido pelo ProfileRepository"
      pattern: "responsibilities.*qualifications.*behaviors.*challenges"
---

<objective>
Piloto end-to-end: rodar o fluxo completo /pesquisar-mercado → /refinar-perfil → /abrir-vaga para gerar o perfil Senior P&D Java+Python+TS que motivou a Phase 5. Este plano é a verificação da phase (D-21).

Purpose: Validar que todos os entregáveis da Phase 5 funcionam integrados em cenário real. O piloto é documentação viva — primeira pesquisa e primeiro refino holístico reais do sistema.

Output: data/research/senior-pd-java-python-ts-sp-{data}.json + data/profiles/{id}.json refinado + confirmação end-to-end de todos os artefatos da Phase 5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-market-research-holistic-refinement/05-CONTEXT.md
@.planning/phases/05-market-research-holistic-refinement/05-02-SUMMARY.md
@.planning/phases/05-market-research-holistic-refinement/05-03-SUMMARY.md
@.planning/phases/05-market-research-holistic-refinement/05-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Executar /pesquisar-mercado e verificar JSON gerado</name>
  <read_first>
    - .planning/phases/05-market-research-holistic-refinement/05-CONTEXT.md (verificação ponto 1-2 e ponto 10)
    - .agents/skills/pesquisar-mercado/SKILL.md (skill a executar)
  </read_first>
  <what-built>
Invocar `/pesquisar-mercado` com o seguinte escopo de piloto:
- Cargo: Senior P&D (ou "Engenheiro Sênior de Software P&D")
- Localização: São Paulo (padrão)
- Senioridade: Sênior
- Stacks-chave: Java, Python, TypeScript
- Indústria: (opcional — pular ou usar "tecnologia/EdTech" se quiser mais contexto)
- Profundidade: média (15-25 vagas, ~15min)

Após a skill concluir, executar as verificações abaixo.
  </what-built>
  <how-to-verify>
    Verificações após execução da skill:
    1. Arquivo JSON existe: `ls $DATA_PATH/research/senior-pd*` (confirmar que existe arquivo com data de hoje)
    2. JSON tem jobs[] não-vazio: `node -e "const d=JSON.parse(require('fs').readFileSync('$DATA_PATH/research/{ARQUIVO}.json')); console.log('jobs:', d.jobs.length, 'portais:', d.sources.map(s=>s.portal+':'+s.status).join(', '))"`
    3. JSON tem summary com stackFrequency: `node -e "const d=JSON.parse(require('fs').readFileSync('$DATA_PATH/research/{ARQUIVO}.json')); console.log(JSON.stringify(d.summary.stackFrequency, null, 2))"`
    4. JSON tem profileHints com qualifications no formato correto: `node -e "const d=JSON.parse(require('fs').readFileSync('$DATA_PATH/research/{ARQUIVO}.json')); console.log(JSON.stringify(d.profileHints.qualifications.slice(0,3), null, 2))"` — deve mostrar objetos `{ text, required }`, NÃO strings
    5. Executar novamente /pesquisar-mercado com o MESMO escopo → confirmar que o novo arquivo tem sufixo `-2` no nome (D-05)
  </how-to-verify>
  <resume-signal>Digite "aprovado" após confirmar: arquivo JSON existe + jobs[] não-vazio + profileHints.qualifications usa formato { text, required } + arquivo -2 gerado na segunda rodada. Ou descreva o que falhou.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Executar /refinar-perfil com pesquisa + aiProfileInstructions e verificar ciclo completo</name>
  <read_first>
    - .planning/phases/05-market-research-holistic-refinement/05-CONTEXT.md (verificação pontos 3-6)
    - .agents/skills/refinar-perfil/SKILL.md (skill evoluída — steps 1-6)
  </read_first>
  <what-built>
Invocar `/refinar-perfil` escolhendo um perfil Java existente (ex: dev-java-pleno ou equivalente disponível em $DATA_PATH/profiles/).

No Step 2:
- Carregar a pesquisa gerada na Task 1 (escolher pelo número da lista)
- Confirmar que o aiProfileInstructions da área P&D/Lyceum aparece no resumo

No Step 3: escolher "Refinar tudo" (modalidade 3) para exercitar o fluxo completo.

No Step 4 (ciclo A/R/J):
- Verificar que as sugestões mencionam stack híbrido (Java + Python + TS) e/ou arquétipo de P&D
- Verificar que o tom reflete o aiProfileInstructions da área

Chegar até o Step 5 (Revisão Holística):
- Verificar que o Step 5 executa e exibe findings (ou confirma coerência)
- Aceitar/ignorar/ajustar pelo menos 1 finding para validar o ciclo A/I/J

Concluir salvamento no Step 6.
  </what-built>
  <how-to-verify>
    Verificações após execução:
    1. Step 2 listou arquivos de research com data legível (D-22) — o arquivo da Task 1 apareceu
    2. Step 4 sugestões citaram explicitamente stack híbrido ou arquétipo de P&D (não sugestão genérica sem contexto)
    3. Step 5 foi ativado e exibiu findings numerados (ou mensagem "Nenhuma inconsistência detectada")
    4. Arquivo de perfil atualizado em $DATA_PATH/profiles/{id}.json:
       - `node -e "const d=JSON.parse(require('fs').readFileSync(process.argv[1])); console.log('title:', d.title, '| responsibilities:', d.responsibilities.length, '| qualifications:', d.qualifications.length, '| behaviors:', d.behaviors.length, '| challenges:', d.challenges.length)" $DATA_PATH/profiles/{id}.json`
       - Confirmar que qualifications é array de objetos { text, required } (não string[])
    5. Abrir /profiles/{id} na web app — renderiza sem erros
    6. Abrir /profiles/{id}/edit na web app — campos carregam corretamente
  </how-to-verify>
  <resume-signal>Digite "aprovado" após confirmar: Step 5 ativado + sugestões com contexto de mercado + perfil salvo com schema correto + web app renderiza sem erros. Ou descreva o que falhou.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Executar /abrir-vaga e gerar Excel para validar downstream</name>
  <read_first>
    - .planning/phases/05-market-research-holistic-refinement/05-CONTEXT.md (verificação pontos 7-9)
    - .agents/skills/abrir-vaga/SKILL.md (skill downstream)
  </read_first>
  <what-built>
Validar que o downstream continua funcional após as evoluções da Phase 5.

Passos:
1. Invocar `/abrir-vaga` usando o perfil refinado na Task 2 — completar o fluxo até salvar a vaga em data/vacancies/
2. Acessar a vaga criada na web app e clicar em "Gerar formulário GH"
3. Verificar que o Excel é gerado e baixado com sucesso
4. Abrir o Excel gerado e confirmar que os rótulos configuráveis e a estrutura da planilha GH continuam válidos
  </what-built>
  <how-to-verify>
    Verificações:
    1. /abrir-vaga criou arquivo em $DATA_PATH/vacancies/ — `ls $DATA_PATH/vacancies/` mostra novo arquivo
    2. Vaga acessível na web app em /vacancies/{id}/edit — sem erros de carregamento
    3. Botão "Gerar formulário GH" na web app retorna download de arquivo .xlsx
    4. Excel gerado abre sem erros e contém os dados da vaga
    5. Os campos de rótulos configuráveis (Requisitos: / Diferenciais:) aparecem corretamente
  </how-to-verify>
  <resume-signal>Digite "aprovado" após confirmar: /abrir-vaga funcionou + Excel gerado e válido + rótulos configuráveis corretos. Ou descreva o que falhou.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Piloto → data/profiles/ | Perfil real modificado pelo fluxo completo — dados de produção (dev) afetados |
| Piloto → data/vacancies/ | Vaga real criada no sistema de desenvolvimento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | Perfil modificado incorretamente pelo Step 5 | mitigate | Verificação pós-save: `node -e` para inspecionar o JSON. Se qualifications virou string[] (bug de regressão), restaurar via `git checkout` no repo de dados. |
| T-05-05-02 | Information Disclosure | JSON de pesquisa com dados de candidatos | accept | A skill /pesquisar-mercado foi instruída (D-08) a não persistir dados pessoais. O JSON contém apenas dados de vagas/empresas. Verificação: `grep -i "cpf\|email\|telefone\|candidato" $DATA_PATH/research/*.json` deve retornar vazio. |
</threat_model>

<verification>
Checklist end-to-end da Phase 5 (D-21 — este piloto é a verificação):

[ ] 1. /pesquisar-mercado executado → $DATA_PATH/research/senior-pd-*-{data}.json existe
[ ] 2. JSON tem jobs[] não-vazio + summary com stackFrequency + profileHints com qualifications como ProfileItem[]
[ ] 3. /refinar-perfil executado com pesquisa carregada → sugestões refletem stack híbrido e aiProfileInstructions
[ ] 4. Step 5 holístico ativado → findings exibidos e resolvidos via A/I/J
[ ] 5. Perfil salvo em data/profiles/{id}.json respeita schema JobProfile (qualifications como ProfileItem[], não string[])
[ ] 6. Perfil renderiza em /profiles/{id} sem erros TypeScript
[ ] 7. /abrir-vaga funcionou com o perfil refinado
[ ] 8. Excel gerado com sucesso — rótulos e estrutura GH válidos
[ ] 9. Segunda rodada de /pesquisar-mercado no mesmo escopo → arquivo com sufixo -2 (D-05)
</verification>

<success_criteria>
- Todos os 9 pontos do checklist de verificação passaram
- Phase 5 declarada Complete em STATE.md
- /fechar-versao pode ser executada pelo gestor para fechar v1.0 (D-20)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-market-research-holistic-refinement/05-05-SUMMARY.md`

Atualizar .planning/STATE.md:
- Phase 5 Status: Complete ✓ {data}
- Current Focus: Phase 5 completa — /fechar-versao disponível para bump SemVer v1.0

Atualizar .planning/ROADMAP.md:
- [ ] Phase 5 → [x] Phase 5: Market Research & Holistic Profile Refinement ✓ {data}
- Plans: marcar todos os 5 planos como [x]
</output>
