---
phase: 03-vacancy-gh-form
plan: "10"
type: execute
wave: 2
depends_on: ["03-09"]
files_modified:
  - src/lib/excel-generator.ts
  - src/lib/vacancy.ts
  - src/app/actions/vacancy.ts
  - src/components/vacancy/vacancy-form.tsx
autonomous: true
requirements: [VAG-01, VAG-03]
gap_closure: true
gaps_addressed: [GAP-05]

must_haves:
  truths:
    - "Ao escolher 'Das 08h às 17h', apenas o checkbox 08h-17h fica marcado no Excel"
    - "Ao escolher 'Das 09h às 18h', apenas o checkbox 09h-18h fica marcado no Excel"
    - "Ao escolher 'Outro', nenhum checkbox fica marcado e o texto do campo workScheduleOther preenche a célula Z18"
    - "No formulário web, campo de texto livre aparece apenas quando 'Outro' está selecionado"
    - "Vagas existentes sem workScheduleOther não quebram (campo opcional)"
  artifacts:
    - path: "src/lib/vacancy.ts"
      provides: "Campo workScheduleOther opcional na interface Vacancy"
      contains: "workScheduleOther?: string"
    - path: "src/lib/excel-generator.ts"
      provides: "CHECKBOX_GROUPS.workSchedule integrado e lógica de Outro"
      contains: "workSchedule"
    - path: "src/components/vacancy/vacancy-form.tsx"
      provides: "Input condicional para texto livre de Outro"
    - path: "src/app/actions/vacancy.ts"
      provides: "Extração de workScheduleOther do FormData"
  key_links:
    - from: "vacancy-form.tsx"
      to: "vacancy.workScheduleOther"
      via: "hidden input name='workScheduleOther' quando workSchedule==='Outro'"
      pattern: "workScheduleOther"
    - from: "excel-generator.ts"
      to: "ctrlProp3/ctrlProp4"
      via: "CHECKBOX_GROUPS.workSchedule"
      pattern: "workSchedule.*ctrlProp"
---

<objective>
Corrigir workSchedule: adicionar campo workScheduleOther, ajustar formulário web e gerador Excel.

Purpose: O campo workSchedule tem 3 opções (08h17h / 09h18h / Outro). A opção "Outro" exige texto livre que atualmente não tem campo dedicado. No Excel, os checkboxes 08h e 09h precisam ser controlados via ctrlProps (como os demais grupos em 03-09). A célula Z18 deve ser preenchida com o texto de "Outro" quando aplicável.

Output: workScheduleOther? adicionado ao schema, formulário mostra campo de texto condicional, gerador marca checkbox correto e preenche Z18 para "Outro".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-vacancy-gh-form/03-09-SUMMARY.md

<interfaces>
<!-- Estado após plano 03-09 -->

De src/lib/vacancy.ts (estado atual):
```typescript
export type WorkSchedule = "Das 08h às 17h" | "Das 09h às 18h" | "Outro";

export interface Vacancy {
  workSchedule: WorkSchedule;
  // workScheduleOther NÃO EXISTE ainda — adicionar como opcional
}
```

De src/lib/excel-generator.ts (após 03-09):
```typescript
const CELL_MAPPING = {
  workSchedule: "Z18",  // Atualmente Z18 recebe o VALOR do enum (ex: "Das 08h às 17h")
  // A lógica correta: Z18 só deve receber texto quando workSchedule === "Outro"
};

const CHECKBOX_GROUPS = {
  // workSchedule NÃO ESTÁ nos grupos ainda — adicionar em 03-10
  // workMode, requestType, experienceLevel, englishLevel, spanishLevel já estão
};
```

Checkboxes de workSchedule (row 18 sheet, descobertos na inspeção do template):
```
ctrlProp3 = Das 08h às 17h (desmarcado no template)
ctrlProp4 = Das 09h às 18h (CHECKED por resíduo no template)
Outro = sem checkbox; preencher célula Z18 com texto
```

De src/app/actions/vacancy.ts (trecho relevante):
```typescript
const workSchedule = (formData.get("workSchedule") as string) || "Das 08h às 17h";
// workScheduleOther NÃO É extraído ainda
```

De src/components/vacancy/vacancy-form.tsx (trecho relevante):
```tsx
// workSchedule é um Select com as 3 opções
// quando workSchedule === "Outro", deve aparecer Input para workScheduleOther
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar workScheduleOther ao schema Vacancy e às actions</name>
  <files>src/lib/vacancy.ts, src/app/actions/vacancy.ts</files>
  <read_first>
    - src/lib/vacancy.ts (ler completo)
    - src/app/actions/vacancy.ts (ler completo)
  </read_first>
  <action>
**Em src/lib/vacancy.ts:**

Adicionar campo `workScheduleOther` como opcional na interface Vacancy, após `workSchedule`:
```typescript
workSchedule: WorkSchedule;
workScheduleOther?: string;  // Texto livre quando workSchedule === "Outro"
```

Não alterar o tipo WorkSchedule nem as constantes — eles já estão corretos.

**Em src/app/actions/vacancy.ts:**

Na função `createVacancy`, após a linha que extrai `workSchedule`, adicionar:
```typescript
const workScheduleOther = workSchedule === "Outro"
  ? (formData.get("workScheduleOther") as string | null) || undefined
  : undefined;
```

E no objeto `vacancy` literal (seção de criação), adicionar:
```typescript
workScheduleOther,
```

Na função `updateVacancy`, após a linha `vacancy.workSchedule = ...`, adicionar:
```typescript
vacancy.workScheduleOther = vacancy.workSchedule === "Outro"
  ? (formData.get("workScheduleOther") as string | null) || undefined
  : undefined;
```
  </action>
  <verify>
    <automated>
      grep -n "workScheduleOther" /home/henrico/github/henricos/hiring-pipeline/src/lib/vacancy.ts
      # deve mostrar linha com "workScheduleOther?: string"
    </automated>
    <automated>
      grep -c "workScheduleOther" /home/henrico/github/henricos/hiring-pipeline/src/app/actions/vacancy.ts
      # deve retornar >= 2 (createVacancy e updateVacancy)
    </automated>
    <automated>
      cd /home/henrico/github/henricos/hiring-pipeline && npx tsc --noEmit 2>&1 | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "workScheduleOther\?: string" src/lib/vacancy.ts retorna 1 resultado
    - grep "workScheduleOther" src/app/actions/vacancy.ts retorna >= 2 resultados
    - createVacancy extrai workScheduleOther condicionalmente (apenas quando workSchedule==="Outro")
    - updateVacancy atualiza workScheduleOther condicionalmente
    - npx tsc --noEmit passa sem erros em vacancy.ts e actions/vacancy.ts
  </acceptance_criteria>
  <done>Schema Vacancy tem workScheduleOther opcional; actions extraem e persistem o campo corretamente.</done>
</task>

<task type="auto">
  <name>Task 2: Ajustar formulário web e gerador Excel para workSchedule</name>
  <files>src/components/vacancy/vacancy-form.tsx, src/lib/excel-generator.ts</files>
  <read_first>
    - src/components/vacancy/vacancy-form.tsx (ler completo)
    - src/lib/excel-generator.ts (ler completo — verificar estado após 03-08 e 03-09)
  </read_first>
  <action>
**Em src/components/vacancy/vacancy-form.tsx:**

1. Adicionar estado para workScheduleOther:
```tsx
const [workScheduleOther, setWorkScheduleOther] = useState(
  vacancy?.workScheduleOther ?? ""
);
```

2. Após o Select de workSchedule (que já existe), adicionar campo condicional:
```tsx
{/* Campo condicional: texto livre quando workSchedule === "Outro" */}
{workSchedule === "Outro" && (
  <div className="space-y-1.5 ml-6 transition-all duration-150">
    <Label htmlFor="workScheduleOther" className={LABEL_CLASS}>
      Descrever horário de trabalho
    </Label>
    <Input
      id="workScheduleOther"
      name="workScheduleOther"
      value={workScheduleOther}
      onChange={(e) => setWorkScheduleOther(e.target.value)}
      placeholder="Ex: Das 07h30 às 16h30"
      className={INPUT_CLASS}
    />
  </div>
)}
```

O campo de texto só é enviado quando workSchedule==="Outro" porque só aparece nesse caso.
Manter o `<input type="hidden" name="workSchedule" value={workSchedule}>` que já existe.

**Em src/lib/excel-generator.ts:**

1. Adicionar `workSchedule` ao CHECKBOX_GROUPS (após o grupo `requestType` existente):
```typescript
workSchedule: {
  options: {
    "Das 08h às 17h": "ctrlProp3",
    "Das 09h às 18h": "ctrlProp4",
    "Outro":           null,  // sem checkbox; texto vai para célula Z18
  },
  allGroup: ["ctrlProp3", "ctrlProp4"],
},
```

2. Alterar o CELL_MAPPING de workSchedule para que Z18 seja preenchido APENAS quando "Outro":
No CELL_MAPPING, remover a entrada `workSchedule: "Z18"` e adicionar:
```typescript
workScheduleOther: "Z18",  // Preenche célula Z18 somente quando "Outro" (GAP-05)
```

3. No `cellValues`, substituir:
```typescript
// ANTES:
[CELL_MAPPING.workSchedule]: vacancy.workSchedule,
// DEPOIS:
[CELL_MAPPING.workScheduleOther]: vacancy.workSchedule === "Outro"
  ? (vacancy.workScheduleOther ?? "")
  : "",
```

4. Adicionar bloco de workSchedule em `applyCheckboxGroups()`, após o bloco de `requestType`:
```typescript
// workSchedule
{
  const group = CHECKBOX_GROUPS.workSchedule;
  group.allGroup.forEach(cp => setCtrlPropChecked(zip, cp, false));
  const target = group.options[vacancy.workSchedule];
  if (target) setCtrlPropChecked(zip, target, true);
}
```

5. Atualizar a assinatura de `applyCheckboxGroups` para aceitar `vacancy: Vacancy` — confirmar que ela já recebe vacancy. Se a função recebe `(zip, vacancy, profile)`, está correto.
  </action>
  <verify>
    <automated>
      grep -n "workScheduleOther\|workSchedule.*Outro\|ctrlProp3\|ctrlProp4" /home/henrico/github/henricos/hiring-pipeline/src/lib/excel-generator.ts | head -20
    </automated>
    <automated>
      grep -n "workScheduleOther" /home/henrico/github/henricos/hiring-pipeline/src/components/vacancy/vacancy-form.tsx
      # deve retornar >= 2 resultados (useState + Input)
    </automated>
    <automated>
      grep -n "workSchedule.*Outro\|Outro.*workSchedule" /home/henrico/github/henricos/hiring-pipeline/src/components/vacancy/vacancy-form.tsx
      # deve mostrar renderização condicional do input
    </automated>
    <automated>
      cd /home/henrico/github/henricos/hiring-pipeline && npx tsc --noEmit 2>&1 | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "workScheduleOther" src/lib/excel-generator.ts retorna >= 2 resultados (CELL_MAPPING + cellValues)
    - grep "CHECKBOX_GROUPS.workSchedule\|workSchedule:" src/lib/excel-generator.ts mostra grupo com ctrlProp3/ctrlProp4
    - grep "workSchedule === .Outro." src/components/vacancy/vacancy-form.tsx retorna resultado (renderização condicional)
    - grep "workScheduleOther" src/components/vacancy/vacancy-form.tsx retorna >= 3 resultados (useState, value prop, name prop)
    - Quando workSchedule==="Das 08h às 17h", célula Z18 deve ficar vazia e ctrlProp3 marcado
    - Quando workSchedule==="Outro", ctrlProp3/4 desmarcados, Z18 = texto de workScheduleOther
    - npx tsc --noEmit passa sem erros
  </acceptance_criteria>
  <done>Formulário web exibe campo de texto condicional para "Outro". Gerador marca checkbox correto de horário e preenche Z18 apenas quando "Outro".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| FormData → workScheduleOther | Campo de texto livre — pode conter caracteres especiais XML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10-01 | Tampering | workScheduleOther → Z18 | mitigate | Valor passa por escapeXml() existente antes de inserção no XML (fluxo cellValues já aplica escapeXml) |
| T-03-10-02 | Elevation | workScheduleOther optional field | accept | Campo é opcional e string — sem impacto de segurança além de XSS via Excel (aceito, arquivo é download interno) |
</threat_model>

<verification>
Após execução:
1. Criar vaga com workSchedule="Das 08h às 17h" → gerar Excel → célula Z18 vazia, checkbox 08h marcado, 09h desmarcado
2. Criar vaga com workSchedule="Das 09h às 18h" → gerar Excel → célula Z18 vazia, checkbox 09h marcado, 08h desmarcado
3. Criar vaga com workSchedule="Outro" e workScheduleOther="Das 07h às 16h" → gerar Excel → ambos checkboxes desmarcados, Z18="Das 07h às 16h"
4. Verificar que o formulário mostra campo de texto apenas quando "Outro" está selecionado
</verification>

<success_criteria>
- workScheduleOther persistido e lido do JSON
- Checkboxes de workSchedule corretos no Excel gerado
- Célula Z18 populada somente para opção "Outro"
- TypeScript compila sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-vacancy-gh-form/03-10-SUMMARY.md`
</output>
