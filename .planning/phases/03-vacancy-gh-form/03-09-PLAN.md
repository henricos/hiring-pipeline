---
phase: 03-vacancy-gh-form
plan: "09"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/excel-generator.ts
autonomous: true
requirements: [VAG-03]
gap_closure: true
gaps_addressed: [GAP-07, GAP-09, GAP-10]

must_haves:
  truths:
    - "Ao escolher 'Presencial', apenas o checkbox Presencial fica marcado no Excel"
    - "Ao escolher 'Remoto', apenas o checkbox Remoto fica marcado no Excel"
    - "Ao escolher '5-10 anos', apenas o checkbox 5-10 anos fica marcado (não '3-5 anos' também)"
    - "Ao escolher inglês 'Intermediário', apenas o checkbox Intermediário fica marcado"
    - "Ao escolher espanhol 'Avançado', apenas o checkbox Avançado fica marcado"
    - "Checkboxes com estado residual do template são sempre resetados antes de marcar a opção correta"
  artifacts:
    - path: "src/lib/excel-generator.ts"
      provides: "Funções setCtrlPropChecked() e applyCheckboxGroups() e CHECKBOX_GROUPS map"
      exports: ["generateVacancyForm"]
  key_links:
    - from: "generateVacancyForm"
      to: "xl/ctrlProps/ctrlPropN.xml"
      via: "zip.updateFile() para cada ctrlProp do grupo"
      pattern: "applyCheckboxGroups|setCtrlPropChecked"
---

<objective>
Corrigir lógica de checkboxes VML para workMode, experienceLevel, englishLevel e spanishLevel.

Purpose: O template XLSX tem checkboxes com estado residual de uso anterior (ctrlProp69/70 para Remoto/Híbrido CHECKED, ctrlProp15/16 para 3-5/5-10 anos CHECKED, ctrlProp25/26 para Inglês Básico/Intermediário CHECKED, etc.). O gerador atual só modifica sheet1.xml e nunca toca nos ctrlProps, então os resíduos persistem, causando múltiplos checkboxes marcados ao mesmo tempo.

Abordagem: Implementar função `applyCheckboxGroups()` que:
1. Limpa TODOS os ctrlProps do grupo (remove atributo `checked="Checked"`)
2. Marca apenas o ctrlProp correspondente à opção escolhida (adiciona `checked="Checked"`)

Output: generateVacancyForm corretamente opera sobre xl/ctrlProps/*.xml para todos os grupos de checkbox.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Mapeamento definitivo de ctrlProps derivado de inspeção do template em 2026-04-21 -->

**CHECKBOX_GROUPS** — mapeamento canônico para uso no gerador:

```typescript
// Estrutura de um grupo de checkbox:
// { options: Record<string, string | null>, allGroup: string[] }
// - options: valor da aplicação → ctrlProp a marcar (null = sem checkbox)
// - allGroup: todos os ctrlProps do grupo (limpar TODOS antes de marcar)

const CHECKBOX_GROUPS = {
  // workMode: Presencial/Remoto/Híbrido (row 23 sheet)
  // Template tem Remoto(ctrlProp69) e Híbrido(ctrlProp70) CHECKED por resíduo
  workMode: {
    options: {
      "Presencial": "ctrlProp68",
      "Remoto":     "ctrlProp69",
      "Híbrido":    "ctrlProp70",
    },
    allGroup: ["ctrlProp68", "ctrlProp69", "ctrlProp70"],
  },

  // requestType: Recrutamento interno/externo (row 4)
  // Template tem Recrutamento externo(ctrlProp6) CHECKED por padrão
  requestType: {
    options: {
      "Recrutamento interno": "ctrlProp5",
      "Recrutamento externo": "ctrlProp6",
    },
    allGroup: ["ctrlProp5", "ctrlProp6"],
  },

  // experienceLevel: 5 opções (row 29)
  // Template tem 3-5 anos(ctrlProp15) e 5-10 anos(ctrlProp16) CHECKED por resíduo
  experienceLevel: {
    options: {
      "< 1 ano":   "ctrlProp13",
      "1-3 anos":  "ctrlProp14",
      "3-5 anos":  "ctrlProp15",
      "5-10 anos": "ctrlProp16",
      "> 10 anos": "ctrlProp17",
    },
    allGroup: ["ctrlProp13", "ctrlProp14", "ctrlProp15", "ctrlProp16", "ctrlProp17"],
  },

  // englishLevel: 4 opções de checkbox (Fluente usa célula U37 pois não há checkbox para Fluente)
  // Template tem Básico(ctrlProp26) e Intermediário(ctrlProp25) CHECKED por resíduo
  englishLevel: {
    options: {
      "Não exigido":   "ctrlProp27",
      "Básico":        "ctrlProp26",
      "Intermediário": "ctrlProp25",
      "Avançado":      "ctrlProp24",
      "Fluente":       null,  // sem checkbox — registrar em célula de texto U37
    },
    allGroup: ["ctrlProp24", "ctrlProp25", "ctrlProp26", "ctrlProp27"],
  },

  // spanishLevel: 4 opções de checkbox (Fluente usa célula U39)
  // Template tem Básico(ctrlProp30) e Intermediário(ctrlProp29) CHECKED por resíduo
  // ctrlProp36,38,40 são shapes duplicados sobrepostos — sempre limpar também
  spanishLevel: {
    options: {
      "Não exigido":   "ctrlProp31",
      "Básico":        "ctrlProp30",
      "Intermediário": "ctrlProp29",
      "Avançado":      "ctrlProp28",
      "Fluente":       null,  // sem checkbox — registrar em célula de texto U39
    },
    allGroup: ["ctrlProp28", "ctrlProp29", "ctrlProp30", "ctrlProp31",
               "ctrlProp36", "ctrlProp38", "ctrlProp40"],  // extras sempre limpar
  },
};
```

**Formato XML de um ctrlProp** (lido de xl/ctrlProps/ctrlPropN.xml):
```xml
<!-- Desmarcado: -->
<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<formControlPr xmlns="http://schemas.microsoft.com/office/spreadsheetml/2009/9/main" objectType="CheckBox" lockText="1"/>

<!-- Marcado: -->
<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<formControlPr xmlns="http://schemas.microsoft.com/office/spreadsheetml/2009/9/main" objectType="CheckBox" checked="Checked" lockText="1"/>
```

**Regra de modificação:**
- Para DESMARCAR: remover `checked="Checked"` do atributo da tag formControlPr
- Para MARCAR: adicionar `checked="Checked"` antes de `lockText="1"`
- Preservar todos os outros atributos intactos
- Preservar o XML declaration `<?xml version="1.0" ...?>`

**Já disponível no gerador:**
```typescript
import AdmZip from "adm-zip";
// zip já está carregado como const zip = new AdmZip(templatePath);
// Para ler: zip.getEntry("xl/ctrlProps/ctrlProp68.xml").getData().toString("utf-8")
// Para atualizar: zip.updateFile("xl/ctrlProps/ctrlProp68.xml", Buffer.from(xml, "utf-8"))
```

**Campos do perfil que usam checkboxes (lidos de profile):**
```typescript
profile.experienceLevel  // ExperienceLevel enum
profile.englishLevel     // LanguageLevel enum
profile.spanishLevel     // LanguageLevel enum
```

**Campos da vaga que usam checkboxes (lidos de vacancy):**
```typescript
vacancy.requestType  // RequestType enum
vacancy.workMode     // WorkMode enum
// workSchedule tratado em plano 03-10
```

**CELL_MAPPING para Fluente (células de texto fallback):**
Quando englishLevel="Fluente" → escrever "Fluente" em célula U37 (já no CELL_MAPPING como englishLevel)
Quando spanishLevel="Fluente" → escrever "Fluente" em célula U39 (já no CELL_MAPPING como spanishLevel)
Nesses casos, os checkboxes do grupo devem ficar todos desmarcados.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar applyCheckboxGroups() e integrar ao generateVacancyForm()</name>
  <files>src/lib/excel-generator.ts</files>
  <read_first>
    - src/lib/excel-generator.ts (ler completo — ~177 linhas após plano 03-08)
    - src/lib/profile.ts (confirmar tipos ExperienceLevel, LanguageLevel)
    - src/lib/vacancy.ts (confirmar tipos RequestType, WorkMode)
  </read_first>
  <action>
Em src/lib/excel-generator.ts, adicionar as seguintes implementações:

**1. Adicionar CHECKBOX_GROUPS após o CELL_MAPPING existente:**

```typescript
// ─── Mapeamento de grupos de checkbox VML ────────────────────────────────────
// Cada grupo define:
//   options: valor aplicação → ctrlProp a marcar (null = sem checkbox)
//   allGroup: todos os ctrlProps do grupo — limpar TODOS antes de marcar qualquer um
//
// Fonte: inspeção via AdmZip do template em 2026-04-21.
// ctrlPropN corresponde ao arquivo xl/ctrlProps/ctrlPropN.xml no ZIP.
// Template tem vários checkboxes marcados por resíduo de uso anterior:
//   ctrlProp69(Remoto), ctrlProp70(Híbrido), ctrlProp15(3-5 anos),
//   ctrlProp16(5-10 anos), ctrlProp25(Inglês Inter), ctrlProp26(Inglês Básico),
//   ctrlProp29(Espanhol Inter), ctrlProp30(Espanhol Básico)
interface CheckboxGroup {
  options: Record<string, string | null>;
  allGroup: string[];
}

const CHECKBOX_GROUPS: Record<string, CheckboxGroup> = {
  workMode: {
    options: {
      "Presencial": "ctrlProp68",
      "Remoto":     "ctrlProp69",
      "Híbrido":    "ctrlProp70",
    },
    allGroup: ["ctrlProp68", "ctrlProp69", "ctrlProp70"],
  },
  requestType: {
    options: {
      "Recrutamento interno": "ctrlProp5",
      "Recrutamento externo": "ctrlProp6",
    },
    allGroup: ["ctrlProp5", "ctrlProp6"],
  },
  experienceLevel: {
    options: {
      "< 1 ano":   "ctrlProp13",
      "1-3 anos":  "ctrlProp14",
      "3-5 anos":  "ctrlProp15",
      "5-10 anos": "ctrlProp16",
      "> 10 anos": "ctrlProp17",
    },
    allGroup: ["ctrlProp13", "ctrlProp14", "ctrlProp15", "ctrlProp16", "ctrlProp17"],
  },
  englishLevel: {
    options: {
      "Não exigido":   "ctrlProp27",
      "Básico":        "ctrlProp26",
      "Intermediário": "ctrlProp25",
      "Avançado":      "ctrlProp24",
      "Fluente":       null,
    },
    allGroup: ["ctrlProp24", "ctrlProp25", "ctrlProp26", "ctrlProp27"],
  },
  spanishLevel: {
    options: {
      "Não exigido":   "ctrlProp31",
      "Básico":        "ctrlProp30",
      "Intermediário": "ctrlProp29",
      "Avançado":      "ctrlProp28",
      "Fluente":       null,
    },
    // ctrlProp36,38,40 são shapes duplicados sobrepostos — sempre limpar
    allGroup: ["ctrlProp28", "ctrlProp29", "ctrlProp30", "ctrlProp31",
               "ctrlProp36", "ctrlProp38", "ctrlProp40"],
  },
};
```

**2. Adicionar função `setCtrlPropChecked` antes de `generateVacancyForm`:**

```typescript
/**
 * Modifica o estado checked de um arquivo ctrlProp dentro do ZIP.
 * - checked=true:  adiciona checked="Checked" antes de lockText="1"
 * - checked=false: remove checked="Checked" se presente
 * Preserva todos os outros atributos e o XML declaration.
 */
function setCtrlPropChecked(zip: AdmZip, ctrlPropName: string, checked: boolean): void {
  const entryPath = `xl/ctrlProps/${ctrlPropName}.xml`;
  const entry = zip.getEntry(entryPath);
  if (!entry) return; // ctrlProp não encontrado — silenciosamente ignorar

  let xml = entry.getData().toString("utf-8");

  // Remover checked="Checked" se existir (normalize para estado desmarcado)
  xml = xml.replace(/\s+checked="Checked"/, "");

  if (checked) {
    // Inserir checked="Checked" antes de lockText="1"
    xml = xml.replace(/(\s+lockText=)/, ' checked="Checked"$1');
  }

  zip.updateFile(entryPath, Buffer.from(xml, "utf-8"));
}
```

**3. Adicionar função `applyCheckboxGroups` antes de `generateVacancyForm`:**

```typescript
/**
 * Aplica o estado correto para todos os grupos de checkbox do template.
 * Para cada grupo:
 *   1. Desmarca todos os ctrlProps do allGroup
 *   2. Marca apenas o ctrlProp correspondente ao valor escolhido (se não for null)
 */
function applyCheckboxGroups(
  zip: AdmZip,
  vacancy: Vacancy,
  profile: JobProfile
): void {
  // workMode
  {
    const group = CHECKBOX_GROUPS.workMode;
    group.allGroup.forEach(cp => setCtrlPropChecked(zip, cp, false));
    const target = group.options[vacancy.workMode];
    if (target) setCtrlPropChecked(zip, target, true);
  }

  // requestType
  {
    const group = CHECKBOX_GROUPS.requestType;
    group.allGroup.forEach(cp => setCtrlPropChecked(zip, cp, false));
    const target = group.options[vacancy.requestType];
    if (target) setCtrlPropChecked(zip, target, true);
  }

  // experienceLevel
  {
    const group = CHECKBOX_GROUPS.experienceLevel;
    group.allGroup.forEach(cp => setCtrlPropChecked(zip, cp, false));
    const target = group.options[profile.experienceLevel];
    if (target) setCtrlPropChecked(zip, target, true);
  }

  // englishLevel
  {
    const group = CHECKBOX_GROUPS.englishLevel;
    group.allGroup.forEach(cp => setCtrlPropChecked(zip, cp, false));
    const target = group.options[profile.englishLevel];
    if (target) setCtrlPropChecked(zip, target, true);
    // Fluente não tem checkbox — o valor já foi escrito em U37 via CELL_MAPPING
  }

  // spanishLevel
  {
    const group = CHECKBOX_GROUPS.spanishLevel;
    group.allGroup.forEach(cp => setCtrlPropChecked(zip, cp, false));
    const target = group.options[profile.spanishLevel];
    if (target) setCtrlPropChecked(zip, target, true);
    // Fluente não tem checkbox — o valor já foi escrito em U39 via CELL_MAPPING
  }
}
```

**4. Chamar `applyCheckboxGroups` dentro de `generateVacancyForm`** logo após a atualização do sheet1.xml e antes de `zip.writeZip()`:

```typescript
  // Aplicar checkboxes VML (ctrlProps) — limpar resíduos e marcar opção correta
  applyCheckboxGroups(zip, vacancy, profile);

  // Gravar o ZIP modificado no caminho de saída (D-08, D-09)
  zip.writeZip(outputPath);
```

**Importante:** O CELL_MAPPING já tem entradas para englishLevel (U37) e spanishLevel (U39) que escrevem o valor de texto. Para Fluente, o texto será escrito normalmente via cellValues. Para os outros níveis, o texto será escrito E o checkbox marcado. Isso é correto — o Excel mostra tanto o checkbox visual quanto o texto na célula de resultado da seleção.

Não remover as entradas de englishLevel/spanishLevel do CELL_MAPPING existente.
  </action>
  <verify>
    <automated>
      grep -n "applyCheckboxGroups\|setCtrlPropChecked\|CHECKBOX_GROUPS" /home/henrico/github/henricos/hiring-pipeline/src/lib/excel-generator.ts | wc -l
      # deve retornar >= 10 linhas
    </automated>
    <automated>
      cd /home/henrico/github/henricos/hiring-pipeline && npx tsc --noEmit 2>&1 | head -30
      # deve ter 0 erros
    </automated>
    <automated>
      grep -n "applyCheckboxGroups(zip" /home/henrico/github/henricos/hiring-pipeline/src/lib/excel-generator.ts
      # deve aparecer dentro de generateVacancyForm antes de zip.writeZip
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "function applyCheckboxGroups" src/lib/excel-generator.ts retorna 1 resultado
    - grep "function setCtrlPropChecked" src/lib/excel-generator.ts retorna 1 resultado
    - grep "CHECKBOX_GROUPS" src/lib/excel-generator.ts retorna >= 2 resultados (definição + uso)
    - grep "applyCheckboxGroups(zip" src/lib/excel-generator.ts retorna 1 resultado dentro de generateVacancyForm
    - A chamada a applyCheckboxGroups() aparece ANTES de zip.writeZip()
    - npx tsc --noEmit passa sem erros relacionados a excel-generator.ts
    - CHECKBOX_GROUPS.workMode.allGroup tem exatamente 3 elementos: ctrlProp68/69/70
    - CHECKBOX_GROUPS.experienceLevel.allGroup tem exatamente 5 elementos
    - CHECKBOX_GROUPS.spanishLevel.allGroup tem 7 elementos (inclui ctrlProp36/38/40)
    - grep "requestType" src/lib/excel-generator.ts retorna >= 2 resultados (CHECKBOX_GROUPS.requestType + bloco em applyCheckboxGroups)
    - CHECKBOX_GROUPS.requestType.allGroup tem 2 elementos: ctrlProp5 e ctrlProp6
  </acceptance_criteria>
  <done>generateVacancyForm modifica ctrlProps para todos os grupos de checkbox; checkboxes residuais do template são limpos antes de marcar a opção correta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| vacancy/profile data → ctrlProps XML | Valores de enum da aplicação controlam quais arquivos XML são modificados no ZIP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09-01 | Tampering | setCtrlPropChecked path | accept | ctrlPropName é sempre hardcoded em CHECKBOX_GROUPS — nunca vem de input externo |
| T-03-09-02 | Denial of Service | ctrlProp entry not found | accept | Falha silenciosa (return early) — template corrompido seria detectado na validação do ZIP |
</threat_model>

<verification>
Após execução:
1. Gerar formulário Excel com vacancy.workMode="Presencial" → apenas checkbox Presencial marcado
2. Gerar com workMode="Remoto" → apenas Remoto marcado
3. Gerar com profile.experienceLevel="5-10 anos" → apenas 5-10 anos marcado (não 3-5 também)
4. Gerar com profile.englishLevel="Intermediário" → apenas Intermediário marcado (não Básico também)
5. Gerar com profile.spanishLevel="Avançado" → apenas Avançado marcado
</verification>

<success_criteria>
- Cada grupo de checkbox tem exatamente 1 opção marcada no Excel gerado
- Nenhum checkbox residual do template persiste no output
- TypeScript compila sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-vacancy-gh-form/03-09-SUMMARY.md`
</output>
