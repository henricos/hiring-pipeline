---
phase: 03-vacancy-gh-form
plan: "08"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/excel-generator.ts
autonomous: true
requirements: [VAG-03]
gap_closure: true
gaps_addressed: [GAP-06, GAP-08]

must_haves:
  truths:
    - "Campo Data (AH4) é preenchido com a data de abertura da vaga no formato DD/MM/YYYY"
    - "Campo data prevista de contratação (K24) é preenchido no formato DD/MM/YYYY"
    - "Nenhuma data aparece em formato ISO no Excel gerado"
  artifacts:
    - path: "src/lib/excel-generator.ts"
      provides: "Função toExcelDate() e CELL_MAPPING atualizado com openedAt → AH4"
      exports: ["generateVacancyForm", "toExcelDate"]
  key_links:
    - from: "generateVacancyForm"
      to: "cellValues[CELL_MAPPING.openedAt]"
      via: "toExcelDate(vacancy.openedAt)"
      pattern: "toExcelDate.*openedAt"
    - from: "generateVacancyForm"
      to: "cellValues[CELL_MAPPING.expectedHireDate]"
      via: "toExcelDate(vacancy.expectedHireDate)"
      pattern: "toExcelDate.*expectedHireDate"
---

<objective>
Corrigir formatação de datas e adicionar data de abertura no Excel gerado.

Purpose: Datas em ISO (YYYY-MM-DD) causam leitura incorreta no Excel. O campo "Data" do template (AH4) representa a data de abertura da vaga e não está sendo preenchido.

Output: Função `toExcelDate()` exportada, dois campos de data no CELL_MAPPING, todas as datas formatadas DD/MM/YYYY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Estado atual de src/lib/excel-generator.ts -->

Tipos relevantes de src/lib/vacancy.ts:
```typescript
export interface Vacancy {
  expectedHireDate: string; // ISO 8601 (data) ex: "2026-05-30"
  openedAt: string;         // ISO 8601 ex: "2026-04-21T10:00:00.000Z"
}
```

CELL_MAPPING atual (trecho relevante):
```typescript
const CELL_MAPPING: Record<string, string> = {
  workSchedule: "Z18",
  expectedHireDate: "K24",
  // openedAt: NÃO EXISTE AINDA — adicionar como "AH4"
};
```

cellValues atual (trecho relevante):
```typescript
[CELL_MAPPING.expectedHireDate]: vacancy.expectedHireDate,  // BUG: ISO string direta
```

Nota: openedAt tem valor ISO com time component "2026-04-21T10:00:00.000Z"
— toExcelDate deve extrair apenas a parte da data antes do "T" e então formatar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar toExcelDate() e corrigir campos de data no gerador</name>
  <files>src/lib/excel-generator.ts</files>
  <read_first>
    - src/lib/excel-generator.ts (ler completo — 177 linhas)
    - src/lib/vacancy.ts (confirmar tipos de expectedHireDate e openedAt)
  </read_first>
  <action>
Em src/lib/excel-generator.ts, realizar as seguintes mudanças:

1. Adicionar a função `toExcelDate` logo após `escapeXml` (antes do CELL_MAPPING):
```typescript
/**
 * Converte string ISO 8601 (YYYY-MM-DD ou YYYY-MM-DDTHH:mm:ss.sssZ) para DD/MM/YYYY.
 * Retorna string vazia se input for falsy ou inválido.
 */
export function toExcelDate(isoStr: string | undefined | null): string {
  if (!isoStr) return "";
  // Extrair apenas a parte da data (antes do "T" se houver)
  const datePart = isoStr.split("T")[0];
  const [year, month, day] = datePart.split("-");
  if (!year || !month || !day) return isoStr; // fallback se formato inesperado
  return `${day}/${month}/${year}`;
}
```

2. Adicionar no CELL_MAPPING a entrada para openedAt:
```typescript
openedAt: "AH4",
```
Inserir após a linha `expectedHireDate: "K24"`, dentro do grupo "Grupo 2: Dados Específicos da Vaga".
Adicionar comentário: `// Data de abertura da vaga (GAP-06)`

3. No objeto `cellValues`, atualizar as duas entradas de data:
- Alterar:
  ```typescript
  [CELL_MAPPING.expectedHireDate]: vacancy.expectedHireDate,
  ```
  Para:
  ```typescript
  [CELL_MAPPING.expectedHireDate]: toExcelDate(vacancy.expectedHireDate),
  ```
- Adicionar entrada para openedAt (após replacedPerson ou próximo dos campos de data):
  ```typescript
  [CELL_MAPPING.openedAt]: toExcelDate(vacancy.openedAt),
  ```

Não alterar mais nada no arquivo — as demais correções (checkboxes VML) são escopo do plano 03-09.
  </action>
  <verify>
    <automated>
      grep -n "toExcelDate" /home/henrico/github/henricos/hiring-pipeline/src/lib/excel-generator.ts | grep -c "toExcelDate"
      # deve retornar >= 4 (definição + export + 2 usos)
    </automated>
    <automated>
      grep -n "openedAt.*AH4\|AH4.*openedAt" /home/henrico/github/henricos/hiring-pipeline/src/lib/excel-generator.ts
      # deve mostrar linha com AH4 no CELL_MAPPING
    </automated>
    <automated>
      cd /home/henrico/github/henricos/hiring-pipeline && npx tsc --noEmit 2>&1 | head -20
      # deve ter 0 erros de tipo
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "export function toExcelDate" src/lib/excel-generator.ts retorna 1 resultado
    - grep "openedAt.*\"AH4\"" src/lib/excel-generator.ts retorna 1 resultado dentro do CELL_MAPPING
    - grep "toExcelDate(vacancy.expectedHireDate)" src/lib/excel-generator.ts retorna 1 resultado em cellValues
    - grep "toExcelDate(vacancy.openedAt)" src/lib/excel-generator.ts retorna 1 resultado em cellValues
    - npx tsc --noEmit passa sem erros de tipo relacionados ao excel-generator.ts
    - toExcelDate("2026-04-21T10:30:00.000Z") retorna "21/04/2026"
    - toExcelDate("2026-05-30") retorna "30/05/2026"
    - toExcelDate("") retorna ""
    - toExcelDate(undefined) retorna ""
  </acceptance_criteria>
  <done>CELL_MAPPING tem openedAt→AH4, cellValues usa toExcelDate() para expectedHireDate e openedAt, função exportada com comportamento correto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| vacancy.openedAt → AH4 | Valor ISO do banco de dados formatado antes de inserção no XML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-01 | Tampering | toExcelDate input | accept | Entrada vem de dados internos da aplicação; fallback para string vazia se formato inválido |
</threat_model>

<verification>
Após execução do plano, verificar manualmente:
1. Gerar formulário Excel de uma vaga real e abrir no LibreOffice/Excel
2. Confirmar que célula AH4 mostra data no formato DD/MM/YYYY (data de abertura)
3. Confirmar que célula K24 mostra data prevista de contratação no formato DD/MM/YYYY
4. Confirmar que nenhuma célula de data contém formato ISO
</verification>

<success_criteria>
- toExcelDate exportada e coberta por casos de teste unitário (se TDD estiver habilitado)
- AH4 preenchido com data de abertura em DD/MM/YYYY
- K24 preenchido com data prevista em DD/MM/YYYY
- TypeScript compila sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-vacancy-gh-form/03-08-SUMMARY.md`
</output>
