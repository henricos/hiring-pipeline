---
phase: 03-vacancy-gh-form
plan: 06
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05"]
files_modified:
  - src/components/vacancy/vacancy-list.tsx
  - src/app/actions/vacancy.ts
  - src/app/(shell)/vacancies/[id]/edit/page.tsx
  - src/components/vacancy/vacancy-status-select.tsx
autonomous: true
gap_closure: true
requirements: [VAG-02, VAG-04]

must_haves:
  truths:
    - "A lista /vacancies não exibe botão ChevronRight de avanço de status"
    - "A página de edição exibe um Select de status sempre visível no topo"
    - "O Select de status chama changeVacancyStatus ao trocar a opção selecionada"
    - "A página de edição exibe apenas um botão 'Gerar formulário GH' (sem botão Regenerar)"
  artifacts:
    - path: "src/components/vacancy/vacancy-list.tsx"
      provides: "Lista de vagas sem botão de avanço de status"
      exports: ["VacancyList"]
    - path: "src/components/vacancy/vacancy-status-select.tsx"
      provides: "Componente Client de seleção de status com Server Action"
      exports: ["VacancyStatusSelect"]
    - path: "src/app/actions/vacancy.ts"
      provides: "Action changeVacancyStatus(id, newStatus)"
      exports: ["changeVacancyStatus"]
    - path: "src/app/(shell)/vacancies/[id]/edit/page.tsx"
      provides: "Página de edição com VacancyStatusSelect e botão único de formulário GH"
  key_links:
    - from: "src/components/vacancy/vacancy-status-select.tsx"
      to: "src/app/actions/vacancy.ts"
      via: "changeVacancyStatus server action"
      pattern: "changeVacancyStatus"
    - from: "src/app/(shell)/vacancies/[id]/edit/page.tsx"
      to: "src/components/vacancy/vacancy-status-select.tsx"
      via: "import e uso do componente"
      pattern: "VacancyStatusSelect"
---

<objective>
Fechar GAP-01, GAP-02 e GAP-03 com ajustes de UI na lista de vagas e na página de edição.

Purpose: Remover controles de status da lista (onde não devem estar), substituir o botão de reversão por um Select completo na edição, e consolidar os dois botões de formulário GH em um único botão.

Output: Lista de vagas sem ChevronRight; página de edição com Select de status + botão único "Gerar formulário GH".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-vacancy-gh-form/03-CONTEXT.md

<interfaces>
<!-- Tipos relevantes de src/lib/vacancy.ts -->
```typescript
export type VacancyStatus = "Aberta" | "Em andamento" | "Encerrada";
export const VACANCY_STATUSES: VacancyStatus[] = ["Aberta", "Em andamento", "Encerrada"];
export interface Vacancy {
  id: string;
  status: VacancyStatus;
  // ...
}
```

<!-- Actions existentes em src/app/actions/vacancy.ts -->
```typescript
// Já existe:
export async function advanceVacancyStatus(vacancyId: string): Promise<void>
export async function revertVacancyStatus(vacancyId: string): Promise<void>

// A criar (GAP-02):
export async function changeVacancyStatus(
  vacancyId: string,
  newStatus: VacancyStatus
): Promise<void>
```

<!-- Componente Select disponível em src/components/ui/select.tsx -->
// Importar: Select, SelectContent, SelectItem, SelectTrigger, SelectValue
// "use client" — Radix-based, já instalado
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GAP-01 — Remover botão de avanço de status de vacancy-list.tsx</name>
  <files>src/components/vacancy/vacancy-list.tsx</files>
  <read_first>
    - src/components/vacancy/vacancy-list.tsx (estado atual completo — ver importações e bloco ChevronRight)
  </read_first>
  <action>
Remover os seguintes elementos de `src/components/vacancy/vacancy-list.tsx`:

1. Da linha de imports, remover `ChevronRight` de `"lucide-react"`:
   ```diff
   - import { Pencil, Trash2, ChevronRight } from "lucide-react";
   + import { Pencil, Trash2 } from "lucide-react";
   ```

2. Remover `advanceVacancyStatus` do import de actions:
   ```diff
   - import { deleteVacancy, advanceVacancyStatus } from "@/app/actions/vacancy";
   + import { deleteVacancy } from "@/app/actions/vacancy";
   ```

3. Remover a função `handleAdvanceStatus` inteira (linhas 55-60 aproximadamente):
   ```diff
   - function handleAdvanceStatus(vacancyId: string) {
   -   startTransition(async () => {
   -     await advanceVacancyStatus(vacancyId);
   -     router.refresh();
   -   });
   - }
   ```

4. Remover o bloco JSX do botão ChevronRight dentro do `{/* Ações */}` div:
   ```diff
   - {/* Avançar status (oculto quando Encerrada) */}
   - {vacancy.status !== "Encerrada" && (
   -   <Button
   -     size="icon"
   -     variant="ghost"
   -     className="min-h-[40px] min-w-[40px] text-on-surface/50 hover:text-on-surface"
   -     aria-label={`Avançar status da vaga ${vacancyTitle}`}
   -     onClick={() => handleAdvanceStatus(vacancy.id)}
   -     disabled={isPending}
   -     title="Avançar status"
   -   >
   -     <ChevronRight className="w-4 h-4" />
   -   </Button>
   - )}
   ```

Após as remoções, verificar se `isPending` e `startTransition` ainda são utilizados pelo handler de exclusão — se sim, manter; se não, remover também.

O `useTransition` e `isPending` continuam sendo usados pelo `handleConfirmDelete`, portanto devem ser mantidos.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - "grep 'ChevronRight' src/components/vacancy/vacancy-list.tsx retorna vazio"
    - "grep 'advanceVacancyStatus' src/components/vacancy/vacancy-list.tsx retorna vazio"
    - "grep 'handleAdvanceStatus' src/components/vacancy/vacancy-list.tsx retorna vazio"
    - "grep 'import.*Pencil.*Trash2' src/components/vacancy/vacancy-list.tsx retorna match (imports restantes intactos)"
    - "npx tsc --noEmit não retorna erros"
  </acceptance_criteria>
  <done>A lista /vacancies renderiza apenas os botões Editar e Excluir para cada vaga — sem botão de avanço de status.</done>
</task>

<task type="auto">
  <name>Task 2: GAP-02 + GAP-03 — Adicionar changeVacancyStatus, criar VacancyStatusSelect e refatorar página de edição</name>
  <files>
    src/app/actions/vacancy.ts,
    src/components/vacancy/vacancy-status-select.tsx,
    src/app/(shell)/vacancies/[id]/edit/page.tsx
  </files>
  <read_first>
    - src/app/actions/vacancy.ts (actions existentes — advanceVacancyStatus e revertVacancyStatus para referência de padrão)
    - src/app/(shell)/vacancies/[id]/edit/page.tsx (estado atual completo — seção de status e seção de formulário GH)
    - src/components/ui/select.tsx (API do componente Select)
    - src/lib/vacancy.ts (VACANCY_STATUSES, VacancyStatus)
  </read_first>
  <action>
**Passo A — Adicionar `changeVacancyStatus` em `src/app/actions/vacancy.ts`**

Adicionar ao final do arquivo (antes do fechamento), seguindo o padrão das actions existentes:

```typescript
// Altera o status da vaga para qualquer estado válido (GAP-02)
export async function changeVacancyStatus(
  vacancyId: string,
  newStatus: VacancyStatus
): Promise<void> {
  try {
    const vacancy = await vacancyRepository.findById(vacancyId);
    if (!vacancy) return;

    vacancy.status = newStatus;
    if (newStatus === "Encerrada") {
      vacancy.closedAt = new Date().toISOString();
    } else {
      vacancy.closedAt = undefined;
    }

    await vacancyRepository.save(vacancy);
  } catch (error) {
    console.error("Falha ao alterar status da vaga:", error);
  }
}
```

Adicionar `VacancyStatus` ao import existente de `@/lib/vacancy`:
```diff
- import type { Vacancy } from "@/lib/vacancy";
+ import type { Vacancy, VacancyStatus } from "@/lib/vacancy";
```

---

**Passo B — Criar `src/components/vacancy/vacancy-status-select.tsx`**

Criar novo arquivo (componente Client):

```typescript
"use client";

import { useTransition } from "react";
import { useRouter } from "next/navigation";
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";
import { changeVacancyStatus } from "@/app/actions/vacancy";
import { VACANCY_STATUSES } from "@/lib/vacancy";
import type { VacancyStatus } from "@/lib/vacancy";

interface VacancyStatusSelectProps {
  vacancyId: string;
  currentStatus: VacancyStatus;
}

export function VacancyStatusSelect({
  vacancyId,
  currentStatus,
}: VacancyStatusSelectProps) {
  const router = useRouter();
  const [isPending, startTransition] = useTransition();

  function handleStatusChange(newStatus: string) {
    startTransition(async () => {
      await changeVacancyStatus(vacancyId, newStatus as VacancyStatus);
      router.refresh();
    });
  }

  return (
    <div className="flex items-center gap-3">
      <Select
        value={currentStatus}
        onValueChange={handleStatusChange}
        disabled={isPending}
      >
        <SelectTrigger className="w-[180px]">
          <SelectValue placeholder="Selecionar status" />
        </SelectTrigger>
        <SelectContent>
          {VACANCY_STATUSES.map((status) => (
            <SelectItem key={status} value={status}>
              {status}
            </SelectItem>
          ))}
        </SelectContent>
      </Select>
      {isPending && (
        <span className="text-sm text-muted-foreground">Salvando...</span>
      )}
    </div>
  );
}
```

---

**Passo C — Refatorar `src/app/(shell)/vacancies/[id]/edit/page.tsx`**

Substituir a seção de status e a seção de formulário GH:

1. Adicionar import do novo componente no topo do arquivo:
   ```typescript
   import { VacancyStatusSelect } from "@/components/vacancy/vacancy-status-select";
   ```

2. Remover os imports que deixarão de ser usados:
   - `revertVacancyStatus` (removido da seção de status)
   - `Button` (verificar se ainda usado — se a seção GH usar `<a>` direto, remover)
   
   Atenção: `Button` pode ser necessário para outros usos. Verificar após as substituições.

3. Substituir a seção de status atual (bloco `{vacancy.status !== "Aberta" && (...)}`):
   ```diff
   - {vacancy.status !== "Aberta" && (
   -   <section className="mt-8 pt-8 border-t border-border/20">
   -     <h2 className="text-base font-medium text-on-surface mb-1">
   -       Status da vaga
   -     </h2>
   -     <p className="text-sm text-muted-foreground mb-4">
   -       Status atual: <strong>{vacancy.status}</strong>
   -     </p>
   -     <form action={revertStatusAction}>
   -       <Button type="submit" variant="outline" size="sm">
   -         ← Voltar para{" "}
   -         {vacancy.status === "Encerrada" ? "Em andamento" : "Aberta"}
   -       </Button>
   -     </form>
   -   </section>
   - )}
   + <section className="mt-8 pt-8 border-t border-border/20">
   +   <h2 className="text-base font-medium text-on-surface mb-1">
   +     Status da vaga
   +   </h2>
   +   <p className="text-sm text-muted-foreground mb-4">
   +     Altere o status da vaga conforme o andamento do processo seletivo.
   +   </p>
   +   <VacancyStatusSelect vacancyId={id} currentStatus={vacancy.status} />
   + </section>
   ```

4. Substituir a seção de formulário GH — manter apenas um botão "Gerar formulário GH" que sempre usa `?regen=1`:
   ```diff
   - <div className="flex gap-3">
   -   <Button asChild variant="default">
   -     <a
   -       href={`${apiPrefix}/api/vacancies/${vacancy.id}/form`}
   -       target="_blank"
   -       rel="noopener noreferrer"
   -     >
   -       Gerar formulário GH
   -     </a>
   -   </Button>
   -   <Button asChild variant="outline">
   -     <a
   -       href={`${apiPrefix}/api/vacancies/${vacancy.id}/form?regen=1`}
   -       target="_blank"
   -       rel="noopener noreferrer"
   -     >
   -       Regenerar
   -     </a>
   -   </Button>
   - </div>
   + <div className="flex gap-3">
   +   <Button asChild variant="default">
   +     <a
   +       href={`${apiPrefix}/api/vacancies/${vacancy.id}/form?regen=1`}
   +       target="_blank"
   +       rel="noopener noreferrer"
   +     >
   +       Gerar formulário GH
   +     </a>
   +   </Button>
   + </div>
   ```

5. Remover a linha `const revertStatusAction = revertVacancyStatus.bind(null, id);` que não é mais usada.

6. Remover `revertVacancyStatus` do import de actions:
   ```diff
   - import { updateVacancy, revertVacancyStatus } from "@/app/actions/vacancy";
   + import { updateVacancy } from "@/app/actions/vacancy";
   ```

Verificar se `Button` ainda é usado após as mudanças (está sendo usado no botão "Gerar formulário GH") — manter o import.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - "grep 'changeVacancyStatus' src/app/actions/vacancy.ts retorna a definição da função"
    - "grep 'VacancyStatus' src/app/actions/vacancy.ts retorna import de tipo"
    - "ls src/components/vacancy/vacancy-status-select.tsx confirma arquivo criado"
    - "grep 'VacancyStatusSelect' src/app/(shell)/vacancies/[id]/edit/page.tsx retorna match"
    - "grep 'revertVacancyStatus' src/app/(shell)/vacancies/[id]/edit/page.tsx retorna vazio"
    - "grep 'revertStatusAction' src/app/(shell)/vacancies/[id]/edit/page.tsx retorna vazio"
    - "grep 'Regenerar' src/app/(shell)/vacancies/[id]/edit/page.tsx retorna vazio"
    - "grep 'regen=1' src/app/(shell)/vacancies/[id]/edit/page.tsx retorna match (único botão usa regen=1)"
    - "grep 'vacancy.status !== .Aberta.' src/app/(shell)/vacancies/[id]/edit/page.tsx retorna vazio (bloco condicional removido)"
    - "npx tsc --noEmit não retorna erros"
  </acceptance_criteria>
  <done>
    - changeVacancyStatus disponível como Server Action
    - VacancyStatusSelect renderiza Select com os 3 estados sempre visível na edição
    - Página de edição tem apenas um botão "Gerar formulário GH" que usa ?regen=1
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Select onValueChange → changeVacancyStatus | Valor vem do cliente; validado como VacancyStatus no servidor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Tampering | changeVacancyStatus recebe valor de status arbitrário | mitigate | Cast `as VacancyStatus` é suficiente para enum fechado; o Select só exibe os 3 valores válidos. Adicionar guard: `if (!VACANCY_STATUSES.includes(newStatus)) return;` na action |
</threat_model>

<verification>
Após execução dos dois tasks:

1. `grep 'ChevronRight' src/components/vacancy/vacancy-list.tsx` → sem resultado
2. `grep 'advanceVacancyStatus' src/components/vacancy/vacancy-list.tsx` → sem resultado
3. `ls src/components/vacancy/vacancy-status-select.tsx` → arquivo existe
4. `grep 'changeVacancyStatus' src/app/actions/vacancy.ts` → definição presente
5. `grep 'VacancyStatusSelect' src/app/(shell)/vacancies/[id]/edit/page.tsx` → presente
6. `grep 'Regenerar' src/app/(shell)/vacancies/[id]/edit/page.tsx` → sem resultado
7. `grep 'regen=1' src/app/(shell)/vacancies/[id]/edit/page.tsx` → único match no botão "Gerar formulário GH"
8. `npx tsc --noEmit` → sem erros
</verification>

<success_criteria>
- [ ] vacancy-list.tsx sem ChevronRight, sem advanceVacancyStatus
- [ ] vacancy-status-select.tsx criado com Select dos 3 estados
- [ ] changeVacancyStatus adicionado em vacancy.ts actions
- [ ] edit/page.tsx usa VacancyStatusSelect (sempre visível, sem condicional de status)
- [ ] edit/page.tsx tem exatamente um botão de formulário GH usando ?regen=1
- [ ] npx tsc --noEmit sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-vacancy-gh-form/03-06-SUMMARY.md`

Incluir:
- GAPs fechados: GAP-01, GAP-02, GAP-03
- Arquivos modificados: vacancy-list.tsx, vacancy.ts (actions), edit/page.tsx
- Arquivo criado: vacancy-status-select.tsx
- Custo estimado de contexto: ~20%
</output>
