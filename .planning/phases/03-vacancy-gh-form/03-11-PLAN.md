---
phase: 03-vacancy-gh-form
plan: "11"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(shell)/vacancies/[id]/edit/page.tsx
  - src/app/(shell)/vacancies/new/page.tsx
autonomous: true
requirements: [VAG-01, VAG-02]
gap_closure: true
gaps_addressed: [GAP-11]

must_haves:
  truths:
    - "Na página de edição, Status da vaga e Formulário GH estão side by side dentro do mesmo container visual do formulário"
    - "O card do formulário tem cor de fundo sutil que evidencia o container"
    - "Na página de nova vaga, o layout do card é consistente com a página de edição"
    - "O layout é responsivo: side by side em telas médias/largas, empilhado em telas pequenas"
  artifacts:
    - path: "src/app/(shell)/vacancies/[id]/edit/page.tsx"
      provides: "Layout reestruturado com seção Ações (Status + Formulário GH) side by side"
    - path: "src/app/(shell)/vacancies/new/page.tsx"
      provides: "Card wrapper com bg-surface-container consistente com página de edição"
  key_links:
    - from: "edit/page.tsx"
      to: "VacancyStatusSelect + botão Gerar formulário GH"
      via: "grid grid-cols-1 md:grid-cols-2 gap-6 inside div.bg-surface-container"
      pattern: "grid.*cols-2\|md:grid-cols"
---

<objective>
Corrigir layout das páginas de vaga: unir seções Status e Formulário GH side by side, adicionar cor de fundo ao container.

Purpose: Status da vaga e Formulário GH ficavam desalinhados (fora do container do formulário). O operador quer uma seção "Ações" unificada lado a lado, e um background sutil para evidenciar o card do formulário.

Output: edit/page.tsx com seção Ações em grid 2 colunas responsivo + cor de fundo no card; new/page.tsx com mesmo card wrapper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Estado atual dos arquivos de layout -->

De src/app/(shell)/vacancies/[id]/edit/page.tsx (estrutura atual):
```tsx
<div className="p-8">
  <div className="w-full max-w-3xl">
    <h1>Editar vaga</h1>
    <VacancyForm ... />          {/* card interno bg-white com px-8 py-8 */}
    <section className="mt-8 pt-8 border-t ...">  {/* Status — FORA do card */}
      <VacancyStatusSelect ... />
    </section>
    <section className="mt-8 pt-8 border-t ...">  {/* Formulário GH — FORA do card */}
      <Button>Gerar formulário GH</Button>
    </section>
  </div>
</div>
```

De src/components/vacancy/vacancy-form.tsx (VacancyForm):
```tsx
// O componente renderiza seu próprio card:
<div className="bg-white rounded-md px-8 py-8 max-w-3xl">
  <form ...>
    ...
  </form>
</div>
```

**Padrão visual do projeto (Tailwind 4 + tema customizado):**
- bg-white = superfície padrão de card
- bg-surface-container-low = superfície ligeiramente elevada (tom mais suave)
- text-on-surface = texto padrão
- border-border/20 = divisor sutil

**Design goal:**
- O card do formulário (VacancyForm) não muda (é um componente compartilhado)
- A página edit/page.tsx deve envolver o formulário + seções Ações em um container com bg-surface-container-low
- Seção Ações: div com `grid grid-cols-1 md:grid-cols-2 gap-6` contendo Status e Formulário GH lado a lado
- Cada card da seção Ações tem `bg-white rounded-md p-6`

**Nota sobre new/page.tsx:**
new/page.tsx não tem seções de Status/GH (vaga ainda não existe). Apenas adicionar o mesmo wrapper visual (bg-surface-container-low) ao redor do formulário para consistência visual com a página de edição.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reestruturar layout de edit/page.tsx e new/page.tsx</name>
  <files>src/app/(shell)/vacancies/[id]/edit/page.tsx, src/app/(shell)/vacancies/new/page.tsx</files>
  <read_first>
    - src/app/(shell)/vacancies/[id]/edit/page.tsx (ler completo)
    - src/app/(shell)/vacancies/new/page.tsx (ler completo)
    - src/components/vacancy/vacancy-form.tsx (verificar className do card raiz do componente)
  </read_first>
  <action>
**Em src/app/(shell)/vacancies/[id]/edit/page.tsx:**

Substituir toda a estrutura após o `<h1>` pelo seguinte layout:

```tsx
{/* Container visual com fundo sutil — envolve formulário + ações */}
<div className="bg-surface-container-low rounded-md p-0">
  <VacancyForm
    profiles={profiles}
    vacancy={vacancy}
    onSubmitAction={submitWithId}
  />

  {/* ── Seção de Ações: Status e Formulário GH lado a lado ── */}
  <div className="px-8 pb-8 pt-2">
    <div className="grid grid-cols-1 md:grid-cols-2 gap-6 pt-6 border-t border-border/20">
      {/* Card: Status da vaga */}
      <div className="bg-white rounded-md p-6">
        <h2 className="text-base font-medium text-on-surface mb-1">
          Status da vaga
        </h2>
        <p className="text-sm text-muted-foreground mb-4">
          Altere o status da vaga conforme o andamento do processo seletivo.
        </p>
        <VacancyStatusSelect vacancyId={id} currentStatus={vacancy.status} />
      </div>

      {/* Card: Formulário GH */}
      <div className="bg-white rounded-md p-6">
        <h2 className="text-base font-medium text-on-surface mb-1">
          Formulário GH
        </h2>
        <p className="text-sm text-muted-foreground mb-4">
          Gere o formulário de requisição de pessoal pronto para envio ao GH/Werecruiter.
        </p>
        <Button asChild variant="default">
          <a
            href={`${apiPrefix}/api/vacancies/${vacancy.id}/form?regen=1`}
            target="_blank"
            rel="noopener noreferrer"
          >
            Gerar formulário GH
          </a>
        </Button>
      </div>
    </div>
  </div>
</div>
```

Remover as duas `<section>` antigas (Status e Formulário GH) que ficavam fora do card.
Manter todos os imports existentes (VacancyStatusSelect, Button, etc.).

**Em src/app/(shell)/vacancies/new/page.tsx:**

Envolver o `<Suspense>` em um container com fundo sutil para consistência:

```tsx
export default function NewVacancyPage() {
  return (
    <div className="p-8">
      <div className="w-full max-w-3xl">
        <h1 className="text-[1.5rem] font-medium tracking-tight text-on-surface mb-8">
          Abrir nova vaga
        </h1>
        <div className="bg-surface-container-low rounded-md">
          <Suspense fallback={<div className="p-8">Carregando formulário...</div>}>
            <NewVacancyForm />
          </Suspense>
        </div>
      </div>
    </div>
  );
}
```

Nota: VacancyForm já renderiza seu próprio card bg-white internamente. O wrapper bg-surface-container-low cria contraste sutil sem duplicar o card.

**Verificar que VacancyForm.tsx não precisa de ajuste:** O componente tem `max-w-3xl` no seu div raiz — isso vai conflitar com o wrapper. Remover o `max-w-3xl` do componente VacancyForm apenas se ele estiver no div mais externo (`<div className="bg-white rounded-md px-8 py-8 max-w-3xl">`). Se estiver, mudar para `<div className="bg-white rounded-md px-8 py-8 w-full">` para deixar a constraint de largura nas páginas.
  </action>
  <verify>
    <automated>
      grep -n "grid.*cols-2\|md:grid-cols-2" /home/henrico/github/henricos/hiring-pipeline/src/app/\(shell\)/vacancies/\[id\]/edit/page.tsx
      # deve retornar resultado com md:grid-cols-2
    </automated>
    <automated>
      grep -n "surface-container-low" /home/henrico/github/henricos/hiring-pipeline/src/app/\(shell\)/vacancies/\[id\]/edit/page.tsx /home/henrico/github/henricos/hiring-pipeline/src/app/\(shell\)/vacancies/new/page.tsx
      # deve retornar resultado em ambos os arquivos
    </automated>
    <automated>
      # Verificar que as duas sections antigas foram removidas de edit/page.tsx
      grep -n "section.*mt-8\|<section" /home/henrico/github/henricos/hiring-pipeline/src/app/\(shell\)/vacancies/\[id\]/edit/page.tsx
      # deve retornar 0 resultados (sections antigas removidas)
    </automated>
    <automated>
      cd /home/henrico/github/henricos/hiring-pipeline && npx tsc --noEmit 2>&1 | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "md:grid-cols-2" src/app/\(shell\)/vacancies/\[id\]/edit/page.tsx retorna 1 resultado
    - grep "bg-surface-container-low" src/app/\(shell\)/vacancies/\[id\]/edit/page.tsx retorna 1 resultado
    - grep "bg-surface-container-low" src/app/\(shell\)/vacancies/new/page.tsx retorna 1 resultado
    - Não existem mais tags `<section>` em edit/page.tsx (verificar com grep)
    - VacancyStatusSelect e Button de Gerar formulário GH estão ambos dentro do grid (não em sections separadas)
    - npx tsc --noEmit passa sem erros
  </acceptance_criteria>
  <done>edit/page.tsx tem seção Ações com grid 2 colunas side by side; new/page.tsx tem wrapper bg-surface-container-low; seções antigas de section removidas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| vacancy.id → href do botão | ID da vaga usado na URL da API — sem mudança de comportamento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11-01 | Information Disclosure | Layout change | accept | Mudança puramente visual — sem novo dado exposto |
</threat_model>

<verification>
Após execução, verificar no browser:
1. Abrir /vacancies/[id]/edit — confirmar Status e Formulário GH lado a lado com fundo sutil
2. Abrir /vacancies/new — confirmar card com fundo sutil consistente
3. Verificar responsividade: em viewport estreito as duas colunas se empilham
4. Verificar que Status select e botão Gerar funcionam normalmente após reposicionamento
</verification>

<success_criteria>
- Status da vaga e Formulário GH no mesmo container, lado a lado
- Card com cor de fundo sutil (bg-surface-container-low) visível
- Layout responsivo (md:grid-cols-2)
- TypeScript compila sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-vacancy-gh-form/03-11-SUMMARY.md`
</output>
